"""Per-day lesson manuscripts: beginner-level explanation data.

Each entry preserves the Day-specific manuscript: core narrative, predict steps,
guided reason, classified debug explanation, four quiz explanations and a
concrete boundary sentence. The renderer (generate-curriculum.py) places these
texts; it never invents lesson content from titles.
"""

from __future__ import annotations


MANUSCRIPTS: dict[int, dict] = {
    2: {
        'notes': [
            '`name = "요섭"`은 연습용 입력을 미리 정해 둔 것으로, 변수 `name`에는 글자 요섭이 들어가고 `print("안녕,", name)`은 두 값을 차례로 읽어 화면에 보냅니다. 쉼표로 분리된 인수 사이에는 `print`가 공백을 넣으므로 결과는 `안녕, 요섭`입니다. `print("name")`으로 바꾸면 따옴표 속 글자 그대로 `name`이 나옵니다. 변수의 이름과 변수에 들어 있는 값을 구별하세요.',
            '실제 입력을 받으려면 `name = input("이름: ")`처럼 바꿀 수 있습니다. `input`은 입력한 글자를 문자열로 돌려줍니다. 아무것도 입력하지 않고 엔터를 누르면 빈 문자열이 들어오므로, 입력이 비었을 때 어떤 인사말을 보여 줄지도 직접 정해 보세요.',
        ],
        'predict': '1. name에 문자열을 저장합니다. 2. print의 두 인수를 차례로 평가합니다. 3. 쉼표 사이 공백을 넣어 한 줄로 출력합니다. `name="요섭" → print가 name을 읽음 → 안녕, 요섭` 흐름으로 실제 출력 `안녕, 요섭`이 됩니다. 핵심 `name`은 저장된 값을 읽는 자리에 쓰입니다.',
        'guided': '원본은 `안녕, 요섭`, 수정본은 `안녕, 요섭!`이다. 첫 변경 줄 `name = "요섭"`에서 `name = "요섭!"`로 새 값을 만들면, print가 바뀐 값을 읽어 공백을 넣어 출력하므로 느낌표가 마지막에 붙는다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `"익명" = "요섭"`입니다. 첫 줄에서 따옴표로 감싼 글자에 값을 대입하려 하므로 실행 전에 SyntaxError라는 컴파일 오류가 나고 프로그램이 시작되지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 고친 줄 `name = "요섭"`에서는 따옴표 없는 이름에 값을 저장하므로 `입력값을 읽어 출력함` 동작이 지켜집니다. 정상 코드는 저장→읽기→공백 출력의 3단계를 따라 `안녕, 요섭`을 출력합니다.',
        'qout': '먼저 name에 문자열 요섭이 저장됩니다. 이어서 print가 두 인수 `안녕,`과 name의 값을 차례로 평가하고 사이 공백을 넣어 한 줄로 출력하므로 `안녕, 요섭`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 코드에 문법 오류가 없어 정상 실행되기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '입력 name은 상자 속 글자라는 뜻은, name이라는 이름표가 요섭이라는 문자열 값을 가리키고 있다는 뜻입니다. print는 그 값을 읽어 화면에 보내는 동작만 합니다. `변수 이름을 따옴표로 감싸 값 대신 글자 name을 출력함`은 반대 사례인데, 따옴표가 있으면 이름표가 아니라 글자 자체가 되어 저장·읽기 동작이 성립하지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `변수 이름을 따옴표로 감싸 값 대신 글자 name을 출력함`입니다. 잘못된 코드 `"익명" = "요섭"`은 SyntaxError라는 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `입력값을 읽어 출력함`이라는 의미와 입력·출력 계약입니다. C에서는 문자 배열과 형식 지정으로, Rust에서는 println!의 자리 표시자로 같은 동작을 구현하고, 실행 결과 `안녕, 요섭`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '아무것도 입력하지 않고 엔터를 누르면 빈 문자열이 들어가므로, 빈 입력일 때의 인사말도 직접 정해 보세요.',
    },
    3: {
        'notes': [
            'C 프로그램은 `main` 함수의 첫 줄부터 실행됩니다. `#include <stdio.h>`는 `printf`를 사용한다는 선언을 컴파일러가 알 수 있게 합니다. `printf("study: %d\\n", 30)`에서 `%d` 자리에 정수 `30`이 들어가고 `\\n`은 다음 줄로 내려갑니다. `return 0`은 프로그램이 정상적으로 끝났다는 뜻입니다. 중괄호는 `main`의 시작과 끝을 표시합니다.',
            '`printf("%d %d\\n", 10, 20)`이면 빈자리 두 곳에 10과 20이 순서대로 들어갑니다. 형식 지정자는 `printf`가 받는 실제 인수의 개수와 타입에 맞춰야 합니다. `puts`는 문자열을 한 줄 출력할 때 쓸 수 있지만 `%d` 같은 숫자 빈자리를 처리하지는 않습니다.',
        ],
        'predict': '1. stdio.h가 printf 선언을 제공합니다. 2. main 내부에서 %d에 정수 30을 채웁니다. 3. return 0은 정상 종료를 알립니다. `main 시작 → %d에 30 대입 → study: 30 출력 → 종료` 흐름으로 실제 출력 `study: 30`이 됩니다. 핵심 `printf`는 형식 빈자리에 값을 채워 출력하는 자리에 쓰입니다.',
        'guided': '원본은 `study: 30`, 수정본은 `study: 31`이다. 첫 변경 줄 `printf("study: %d\\n", 30);`에서 `printf("study: %d\\n", 31);`로 새 값을 만들면, %d 자리에 31이 들어가 출력 문자열이 `study: 31`이 된다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `puts("study: %d\\n", 30);`입니다. `puts`는 문자열 하나만 받는 출력 함수이므로 인수 두 개를 전달한 이 줄에서 컴파일 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 고친 줄 `printf("study: %d\\n", 30);`에서는 `%d` 빈자리에 정수 30을 채우는 `정수를 지정한 형식으로 출력함` 동작이 지켜집니다. 정상 코드는 선언 제공→빈자리 채우기→정상 종료의 3단계를 따라 `study: 30`을 출력합니다.',
        'qout': '먼저 main이 시작되고 stdio.h의 printf 선언을 씁니다. 이어서 %d 자리에 정수 30이 들어가 `study: 30` 한 줄이 출력되고, return 0으로 정상 종료하므로 최종 출력 `study: 30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'main은 프로그램 진입문이라는 뜻은, 운영체제가 이 함수부터 실행을 시작한다는 뜻입니다. printf는 형식 문자열의 빈자리에 값을 채워 출력하므로 `%d`와 30이 만나 `study: 30`이 됩니다. `형식 지정자 %d에 정수 인수를 전달하지 않음`은 반대 사례인데, 빈자리에 맞는 값이 없으면 약속한 출력이 만들어지지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `형식 지정자 %d에 정수 인수를 전달하지 않음`입니다. 잘못된 코드 `puts("study: %d\\n", 30);`은 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `정수를 지정한 형식으로 출력함`이라는 의미와 입력·출력 계약입니다. Python에서는 print의 인수로, Rust에서는 println!의 자리 표시자로 같은 동작을 구현하고, 실행 결과 `study: 30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`printf("%d %d\\n", 10, 20)`처럼 빈자리 수와 인수 개수가 다르면 약속한 출력이 나오지 않으므로, 개수와 타입을 먼저 맞추세요.',
    },
    4: {
        'notes': [
            '`minutes`라는 이름과 값 `20`을 연결했다는 뜻이 바인딩입니다. `let minutes = 20`은 기본적으로 변경할 수 없는 바인딩입니다. 그 뒤에 `minutes += 10`을 적으면 컴파일 단계에서 오류가 납니다. `let mut minutes = 20`처럼 `mut`를 붙였을 때만 같은 이름에 새 값 30을 넣을 수 있습니다.',
            '`let minutes = 20; let minutes = minutes + 10;`도 30이 되지만 이 경우에는 같은 바인딩을 고친 것이 아니라 새 바인딩이 이전 이름을 가리는 섀도잉입니다. 두 방법의 결과가 같더라도 뜻은 다릅니다. 코드에서 같은 이름의 값을 계속 바꿀 의도라면 `mut`를, 새 계산 결과를 다시 붙일 의도라면 새 `let`을 고려하세요.',
        ],
        'predict': '1. let mut로 변경 가능한 정수 바인딩을 만듭니다. 2. +=가 기존 20에 10을 더합니다. 3. println!이 현재 30을 표시합니다. `minutes=20 → minutes=30 → 30 출력` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `mut`는 같은 바인딩의 값을 바꿀 권한을 주는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `let mut minutes = 20;`에서 `let mut minutes = 21;`로 시작 값을 바꾸면, +=가 21에 10을 더해 31이 되고 println!이 그대로 보여 준다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `let  minutes = 20;`입니다. `mut`가 빠져 변경 불가 바인딩이 되었는데 다음 줄에서 `minutes += 10`으로 같은 바인딩을 바꾸려 하므로 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 고친 줄 `let mut minutes = 20;`에서는 `mut`가 `변경 가능한 바인딩에 값을 갱신함` 동작을 지켜줍니다. 정상 코드는 바인딩 생성→값 갱신→표시의 3단계를 따라 `30`을 출력합니다.',
        'qout': '먼저 let mut로 값 20의 변경 가능 바인딩이 만들어집니다. 이어서 +=가 20에 10을 더해 30으로 갱신하고, println!이 현재 값 30을 표시하므로 출력 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 정상 코드는 컴파일과 실행이 모두 되기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'let은 이름을 새 값에 묶는다는 뜻이고, mut는 같은 바인딩의 값을 바꿀 권한을 준다는 뜻입니다. `mut 없이 같은 바인딩을 다시 대입하려고 함`은 반대 사례인데, 권한 없이 갱신하려 하면 컴파일 단계에서 막히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `mut 없이 같은 바인딩을 다시 대입하려고 함`입니다. 잘못된 코드는 `mut`가 빠져 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `변경 가능한 바인딩에 값을 갱신함`이라는 의미와 입력·출력 계약입니다. C에서는 변경 가능 변수 선언으로, Python에서는 재대입으로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '같은 이름으로 새 `let`을 쓰면 섀도잉이 되어 이전 바인딩은 가려집니다. 바꿀 의도인지 가릴 의도인지에 따라 `mut`와 새 `let`을 구별하세요.',
    },
    5: {
        'notes': [
            '`20 + 5 * 2`를 왼쪽부터 무조건 계산하면 50이라고 생각하기 쉽습니다. 실제 순서는 `5 * 2 = 10`, 이어서 `20 + 10 = 30`입니다. 곱셈·나눗셈은 덧셈·뺄셈보다 먼저 계산됩니다. `(20 + 5) * 2`처럼 괄호를 쓰면 괄호 안 25를 먼저 만들어 50이 됩니다.',
            '분 단위 비용이 기본 20에 추가 작업 5분씩 2회라고 생각해 보면 30이 자연스럽습니다. 괄호 하나만 달라져도 문제의 뜻이 달라질 수 있습니다. `20 + 5 * 3`은 몇 분인지 손으로 구한 뒤 실행 결과와 비교하세요.',
        ],
        'predict': '1. 5 곱하기 2가 10이 됩니다. 2. 20에 10을 더해 minutes에 저장합니다. 3. print가 30을 출력합니다. `5*2=10 → 20+10=30 → 30 출력` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `5 * 2`는 곱셈을 먼저 끝내는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `minutes = 20 + 5 * 2`에서 `minutes = 21 + 5 * 2`로 바꾸면, 곱셈 10은 그대로이고 21에 10을 더해 31이 저장된 뒤 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `minutes = 20 + (20 + 5) * 2`입니다. 괄호 안 25를 먼저 만들고 2를 곱해 50을 얻은 뒤 20을 더하므로, 실행은 되지만 결과는 70이 됩니다. 오류 분류는 잘못된 출력입니다. 원하는 `30`이 나오지 않은 첫 순간은 곱셈 순서를 어긴 괄호 계산입니다. 고친 줄 `minutes = 20 + 5 * 2`에서는 `5 * 2`가 먼저 계산되는 `연산자 우선순위대로 계산함` 동작이 지켜집니다. 정상 코드는 곱셈→덧셈→출력의 3단계를 따라 `30`을 출력합니다.',
        'qout': '먼저 5 곱하기 2가 10이 됩니다. 이어서 20에 10을 더해 minutes에 30이 저장되고, print가 저장된 값을 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 문법·실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '곱셈을 먼저 끝낸 뒤 덧셈을 한다는 뜻은, `5 * 2`가 `20 +`보다 먼저 하나의 값 10으로 확정된다는 뜻입니다. 원하는 순서가 다르면 괄호로 묶는다고 덧붙인 것은, 괄호가 우선순위를 바꾸는 유일한 수단이기 때문입니다. `왼쪽부터 20+5를 먼저 계산한다고 생각함`은 반대 사례인데, 그렇게 계산하면 50이라는 다른 값이 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `왼쪽부터 20+5를 먼저 계산한다고 생각함`입니다. 잘못된 코드는 실행은 되지만 70이라는 잘못된 출력을 냅니다. 출력이 있다는 이유로 맞다고 보지 말고, 곱셈이 먼저 계산됐는지 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `연산자 우선순위대로 계산함`이라는 의미와 입력·출력 계약입니다. C·Rust에서도 같은 식은 같은 순서로 계산되므로, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`20 + 5 * 3`을 손으로 먼저 구해 보세요. 곱셈 15를 먼저 끝내면 35가 되어야 합니다.',
    },
    6: {
        'notes': [
            'C에서 `int total = 5`를 `2`로 나누면 정수끼리 계산하므로 `5 / 2`는 2입니다. 그 뒤에 `double average`에 저장해도 이미 잃어버린 0.5는 돌아오지 않습니다. `(double)total / 2`처럼 나누기 전에 한쪽을 실수로 바꾸면 `5.0 / 2.0`을 계산해 2.5를 얻습니다.',
            '`(double)(total / 2)`는 괄호 속 정수 나눗셈이 먼저 일어나므로 2.0입니다. 이 두 식의 차이를 꼭 손으로 계산해 보세요. `%.1f`는 계산을 바꾸는 연산이 아니라 실수 출력 때 소수 첫째 자리까지 보여 주라는 형식입니다. 0으로 나누는 입력도 별도로 막아야 합니다.',
        ],
        'predict': '1. total을 int 5로 시작합니다. 2. 캐스트 후 5.0/2.0을 계산합니다. 3. %.1f로 소수 한 자리 2.5를 출력합니다. `int 5 → double 5.0 → 5.0/2=2.5` 흐름으로 실제 출력 `2.5`가 됩니다. 핵심 `(double)total`은 나누기 전에 피연산자를 실수로 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `2.5`, 수정본은 `3.0`이다. 첫 변경 줄 `int total = 5;`에서 `int total = 6;`으로 시작 값을 바꾸면, 캐스트 뒤 6.0/2.0 계산을 거쳐 3.0이 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `double average = total / 2;`입니다. 캐스트가 없어 정수 나눗셈 `5 / 2`가 먼저 일어나 2가 되고, 그 2가 double에 저장되어 `2.0`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 0.5를 잃는 첫 순간은 나누기 전 변환을 빠뜨린 나눗셈입니다. 고친 줄 `double average = (double)total / 2;`에서는 나누기 전에 실수로 바꾸는 동작이 지켜집니다. 정상 코드는 선언→실수 변환→나눗셈→출력의 순서로 `2.5`를 출력합니다.',
        'qout': '먼저 total에 정수 5가 저장됩니다. 이어서 캐스트로 5.0이 되어 2.0으로 나누면 2.5가 되고, %.1f가 소수 첫째 자리까지 보여 주므로 출력 `2.5`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '피연산자 하나를 double로 바꾸면 나눗셈 전체가 실수 나눗셈이 된다는 뜻은, `5.0 / 2.0`처럼 계산 자체가 실수 연산으로 수행된다는 뜻입니다. `정수끼리 먼저 나누고 결과만 double에 저장함`은 반대 사례인데, 이미 버려진 0.5는 저장 단계에서 되살릴 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `정수끼리 먼저 나누고 결과만 double에 저장함`입니다. 잘못된 코드는 실행은 되지만 `2.0`이라는 잘못된 출력을 냅니다. `%.1f`는 보여 주는 형식일 뿐 계산을 고치지 못합니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `나누기 전에 실수로 변환함`이라는 의미와 입력·출력 계약입니다. Python 나눗셈은 기본 실수 연산이고 Rust는 타입을 맞춰야 하므로, 실행 결과 `2.5`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '0으로 나누는 입력은 별도로 막아야 합니다. 나누는 값이 0일 때 어떤 오류 메시지를 보여 줄지도 직접 정해 보세요.',
    },
    7: {
        'notes': [
            '이번에는 세 동작을 섞지 말고 분리하세요. 먼저 `5 * 2 = 10`, 다음 `minutes = 20 + 10 = 30`, 마지막 `f"{minutes}분"`의 중괄호를 현재 값 30으로 바꿔 `30분`을 출력합니다. 앞의 `f`는 중괄호 안 표현식을 계산해 문자열에 끼워 넣으라는 표시입니다.',
            '`print("{minutes}분")`에서는 `f`가 없으므로 중괄호가 그대로 출력됩니다. `minutes = 15`로 바꾼 뒤 `f"{minutes + 5}분"`을 출력하면 20분입니다. 원래 변수 값 15 자체가 바뀌는 것은 아닙니다. 계산 결과와 문자열 모양을 별개로 생각해 보세요.',
        ],
        'predict': '1. 5*2를 먼저 계산합니다. 2. minutes에 30을 넣습니다. 3. f-string이 30을 문자열에 끼워 넣습니다. `5*2=10 → minutes=30 → 30분 출력` 흐름으로 실제 출력 `30분`이 됩니다. 핵심 `f"`는 중괄호를 계산 결과로 치환하라는 표시 자리에 쓰입니다.',
        'guided': '원본은 `30분`, 수정본은 `31분`이다. 첫 변경 줄 `minutes = 20 + 5 * 2`에서 `minutes = 21 + 5 * 2`로 바꾸면, 21에 10을 더한 31이 저장된 뒤 f-string이 그대로 끼워 넣어 출력한다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print("{minutes}분")`입니다. 앞의 `f`가 없어 중괄호 안 표현식이 계산되지 않고 글자 그대로 출력되므로, 실행은 되지만 결과는 `{minutes}분`이 됩니다. 오류 분류는 잘못된 출력입니다. 계산 결과가 문자열에 들어가지 않는 첫 순간은 f 표시를 빠뜨린 출력 줄입니다. 고친 줄 `print(f"{minutes}분")`에서는 `계산한 값을 문자열에 삽입함` 동작이 지켜집니다. 정상 코드는 계산→저장→삽입의 3단계를 따라 `30분`을 출력합니다.',
        'qout': '먼저 5*2가 계산되어 minutes에 30이 저장됩니다. 이어서 f-string의 중괄호가 현재 값 30으로 치환되어 `30분`이 출력되므로 최종 출력 `30분`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 문법·실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '값을 계산해 변수에 넣고 f-string의 중괄호를 실제 값으로 치환한다는 뜻은, 계산·저장·삽입의 세 단계가 순서대로 일어난다는 뜻입니다. `문자열 앞 f를 빼고 {minutes}를 글자 그대로 출력함`은 반대 사례인데, f가 없으면 치환 단계가 생략되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `문자열 앞 f를 빼고 {minutes}를 글자 그대로 출력함`입니다. 잘못된 코드는 실행은 되지만 `{minutes}분`이라는 잘못된 출력을 냅니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `계산한 값을 문자열에 삽입함`이라는 의미와 입력·출력 계약입니다. C의 형식 지정과 Rust의 format! 자리 표시자로 같은 동작을 구현하고, 실행 결과 `30분`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`minutes = 15`일 때 `f"{minutes + 5}분"`은 20분이지만 원래 변수 값 15는 그대로입니다. 계산 결과와 저장된 값을 구별하세요.',
    },
    8: {
        'notes': [
            '`&&`는 두 물음에 모두 예라고 답해야 참입니다. 예를 들어 학습 시간이 충분한지 `minutes >= 30`을 확인하고, 방해 없이 집중했는지 `focused`를 확인한다면 둘 중 하나라도 거짓일 때 전체는 거짓입니다. `!focused`는 집중 여부를 뒤집습니다. 참은 거짓으로, 거짓은 참으로 바뀝니다.',
            '`true && false`는 `false`, `true || false`는 `true`입니다. `&&`의 왼쪽이 이미 거짓이면 오른쪽을 평가할 필요가 없어서 건너뛸 수 있습니다. 조건을 읽을 때는 두 변수의 현재 값부터 적고, 각각의 참·거짓을 구한 뒤 전체 식을 판단하세요.',
        ],
        'predict': '1. ready는 true입니다. 2. !focused는 !false이므로 true입니다. 3. true && true가 true입니다. `ready=true → !focused=true → true 출력` 흐름으로 실제 출력 `true`가 됩니다. 핵심 `&& !focused`는 두 조건을 모두 읽어 하나의 참·거짓으로 확정하는 자리에 쓰입니다.',
        'guided': '원본은 `true`, 수정본은 `true!`이다. 첫 변경 줄 `println!("{}", ready && !focused);`에서 `println!("{}!", ready && !focused);`로 형식 문자열에 느낌표를 더하면, 계산된 참·거짓 값은 그대로이고 화면 모양에만 느낌표가 붙는다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `println!("{}", ready || focused);`입니다. `||`는 하나만 참이어도 참이므로, 이번 입력에서는 우연히 같은 `true`가 출력됩니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. `focused`가 true인 입력에서는 잘못된 식은 true, 정상 식 `ready && !focused`는 false가 되어 갈라집니다. 고친 줄 `println!("{}", ready && !focused);`에서는 `두 조건의 논리곱을 구함` 동작이 지켜집니다. 정상 코드는 값 읽기→뒤집기→논리곱의 순서로 `true`를 출력합니다.',
        'qout': '먼저 ready에 true, focused에 false가 저장됩니다. 이어서 !focused가 true가 되고, true && true가 true이므로 출력 `true`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': '&&는 두 조건이 모두 참이어야 한다는 뜻이고, !는 조건의 참·거짓을 뒤집는다는 뜻입니다. `&&와 ||를 혼동해 하나만 참이어도 통과시킴`은 반대 사례인데, ||는 둘 중 하나만 참이어도 참이 되어 검사 조건이 느슨해지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `&&와 ||를 혼동해 하나만 참이어도 통과시킴`입니다. 잘못된 코드는 이번 입력에서는 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, focused가 true인 입력을 넣으면 결과가 갈라지는 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `두 조건의 논리곱을 구함`이라는 의미와 입력·출력 계약입니다. C의 &&와 !, Python의 and와 not으로 같은 동작을 구현하고, true·false 조합표로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`true && false`는 `false`가 됩니다. 왼쪽이 거짓일 때 오른쪽을 건너뛰는지, 두 변수의 값을 표로 적어 확인하세요.',
    },
    9: {
        'notes': [
            '`f"{score:.1f}"`는 `score`에 담긴 실수를 보여 주는 모양만 바꿉니다. `:.1f`는 소수점 아래 한 자리로 표시하라는 뜻이라 2.75 같은 값은 한 자리로 표시됩니다. 원래 변수의 정밀도가 바뀌거나 다시 저장되지는 않습니다.',
            '저장된 값을 계산에 계속 쓰고 싶다면 형식화한 글자와 숫자를 혼동하지 마세요. `formatted = f"{score:.1f}"`의 결과는 문자열입니다. `score + 1`은 숫자 계산이지만 `formatted + "점"`은 글자를 잇는 동작입니다. 어떤 값을 화면에 보여 주고 어떤 값을 계산할지 분리하세요.',
        ],
        'predict': '1. score는 2.75로 저장됩니다. 2. 표시할 때 :.1f가 소수 한 자리를 선택합니다. 3. 반올림한 2.8을 문자열에 넣습니다. `score=2.75 → 표시 자리수 1 → 평균 2.8` 흐름으로 실제 출력 `평균 2.8`이 됩니다. 핵심 `:.1f`는 저장된 값을 바꾸지 않고 표시 자리만 정하는 자리에 쓰입니다.',
        'guided': '원본은 `평균 2.8`, 수정본은 `평균 3.8`이다. 첫 변경 줄 `score = 2.75`에서 `score = 3.75`로 저장 값을 바꾸면, 같은 :.1f 표시를 거쳐 3.8이 문자열에 들어간다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(f"평균 {score}")`입니다. `:.1f`가 없어 자리수 지정이 빠지므로, 실행은 되지만 결과는 `평균 2.75`가 됩니다. 오류 분류는 잘못된 출력입니다. 표시 형식이 어긋나는 첫 순간은 자리수 지정 없는 출력 줄입니다. 고친 줄 `print(f"평균 {score:.1f}")`에서는 `표시 형식을 조정함` 동작이 지켜집니다. 정상 코드는 저장→자리 선택→삽입의 3단계를 따라 `평균 2.8`을 출력합니다.',
        'qout': '먼저 score에 2.75가 저장됩니다. 이어서 :.1f가 소수 첫째 자리를 선택해 2.8로 반올림하고, 문자열에 넣어 `평균 2.8`이 출력되므로 최종 출력 `평균 2.8`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 문법·실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'f-string의 :.1f는 실수 표시를 소수 첫째 자리로 반올림해 형식화한다는 뜻이고, 저장된 score 값은 그대로라는 뜻은 표시와 저장이 분리된다는 뜻입니다. `출력 형식을 바꾸면 변수에 저장된 실제 값도 바뀐다고 생각함`은 반대 사례인데, 형식 지정은 보여 주는 모양에만 영향을 주기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `출력 형식을 바꾸면 변수에 저장된 실제 값도 바뀐다고 생각함`입니다. 잘못된 코드는 실행은 되지만 `평균 2.75`라는 잘못된 출력을 냅니다. 자리수 지정이 빠졌는지 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `표시 형식을 조정함`이라는 의미와 입력·출력 계약입니다. C의 %.1f 형식과 Rust의 {:.1} 자리 표시자로 같은 동작을 구현하고, 실행 결과 `평균 2.8`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`formatted = f"{score:.1f}"`의 결과는 문자열이므로, 숫자 계산에 다시 쓰기 전에 타입을 확인하세요.',
    },
    10: {
        'notes': [
            '`INT_MAX`는 C가 현재 실행 환경의 `int`에 허용하는 가장 큰 정수입니다. `limits.h`에 정의된 이 값을 숫자로 외우기보다 이 컴퓨터에서 `int`의 끝이라고 이해하세요. 정수 타입은 저장할 수 있는 값의 범위가 있고, 그 범위 밖 연산은 특히 부호 있는 정수에서 문제가 됩니다.',
            '`INT_MAX + 1`로 다음 숫자를 시험하지 마세요. C에서 부호 있는 정수 오버플로는 정의되지 않은 동작입니다. 범위를 검사해야 한다면 더하기 전에 `value > INT_MAX - delta`인지 확인합니다. 화면에 `1`이 보이면 비교식이 참이어서 C가 참을 정수 1로 출력한 것입니다.',
        ],
        'predict': '1. limits.h가 범위 매크로를 제공합니다. 2. INT_MAX가 0보다 큰지 비교합니다. 3. 참을 1로 출력합니다. `INT_MAX는 양수 → 비교 결과 참 → 1 출력` 흐름으로 실제 출력 `1`이 됩니다. 핵심 `INT_MAX`는 비교의 왼쪽 값을 읽는 자리에 쓰입니다.',
        'guided': '원본은 `1`, 수정본도 `1`이다. 첫 변경 줄 `printf("%d\\n", INT_MAX > 0);`에서 `printf("%d\\n", INT_MAX > 1);`로 비교 대상만 바꾸면, INT_MAX가 0보다도 1보다도 크므로 비교 결과는 그대로 참이다. 값이 실제로 달라진 것은 비교 대상뿐이고 최종 출력은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `printf("%d\\n", INT_MIN > 0);`입니다. 왼쪽 피연산자가 음수 범위의 끝 INT_MIN이므로 `INT_MIN > 0` 비교는 거짓이 되어 `0`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 비교 결과가 뒤집히는 첫 순간은 왼쪽 값을 바꾼 비교식입니다. 고친 줄 `printf("%d\\n", INT_MAX > 0);`에서는 양수 끝값과 0을 비교하는 `정수 범위를 확인함` 동작이 지켜집니다. `INT_MAX + 1`을 실제로 계산해서 범위를 시험하지 마세요. 부호 있는 정수 오버플로는 정의되지 않은 동작이므로, 검사는 더하기 전에 `value > INT_MAX - delta`처럼 수행합니다. 정상 코드는 매크로 제공→양수 비교→참 출력의 3단계를 따라 `1`을 출력합니다.',
        'qout': '먼저 limits.h가 INT_MAX 매크로를 제공합니다. 이어서 INT_MAX가 0보다 큰지 비교하면 참이 되고, C는 참을 정수 1로 출력하므로 최종 출력 `1`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'limits.h의 INT_MAX는 현재 플랫폼의 int 최댓값을 표현한다는 뜻이고, 양수 비교는 참이면 1이라는 뜻은 C가 참·거짓을 1·0으로 출력한다는 뜻입니다. `int 최대치에 1을 더해도 안전하게 감싼다고 단정함`은 반대 사례인데, 부호 있는 오버플로는 정의되지 않은 동작이라 감쌈을 약속할 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `int 최대치에 1을 더해도 안전하게 감싼다고 단정함`입니다. 잘못된 코드는 실행은 되지만 `0`이라는 잘못된 출력을 냅니다. 비교식의 두 피연산자가 무엇인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `정수 범위를 확인함`이라는 의미와 입력·출력 계약입니다. Python과 Rust에서는 정수 범위를 타입과 검사로 다루므로, 실행 결과 `1`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '최대값 검사는 더하기 전에 `value > INT_MAX - delta`처럼 수행하세요. `INT_MAX + 1`을 직접 계산하는 시험은 금지입니다.',
    },
    11: {
        'notes': [
            '`u32`는 0부터 양수까지 담는 32비트 정수입니다. `let minutes: u32 = 30`은 타입을 직접 적은 경우이고, `let seconds = minutes * 60`처럼 연산 상대를 보고 타입을 추론할 수도 있습니다. 음수 `-1`은 `u32`로 표현할 수 없습니다.',
            '분을 초로 바꿀 때는 `30 * 60 = 1800`인지 먼저 계산해 보세요. 엄청 큰 값을 곱하면 정수 범위를 넘을 수 있으므로 실무에서는 `checked_mul(60)`처럼 실패를 다룰 방법이 필요합니다. Rust의 타입 추론은 아무 타입이나 된다는 뜻이 아니라 문맥에서 정해진 한 타입을 컴파일러가 찾아준다는 뜻입니다.',
        ],
        'predict': '1. minutes를 u32 30으로 선언합니다. 2. u32끼리 30*60을 계산합니다. 3. seconds에 1800을 저장해 출력합니다. `minutes: u32=30 → seconds=1800 → 출력` 흐름으로 실제 출력 `1800`이 됩니다. 핵심 `u32`는 음수를 담지 않는 정수 타입을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `1800`, 수정본은 `1860`이다. 첫 변경 줄 `let minutes: u32 = 30;`에서 `let minutes: u32 = 31;`로 시작 값을 바꾸면, 31*60 계산을 거쳐 1860이 저장된 뒤 출력된다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `let minutes: i32 = 30;`입니다. `i32`로도 30*60=1800 계산이 되므로 이번 입력에서는 화면 출력이 같습니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. `u32`가 막아야 할 음수 입력이 들어오면 두 타입의 동작이 갈라집니다. 고친 줄 `let minutes: u32 = 30;`에서는 음수를 담지 않는 `타입 추론과 정수 연산을 사용함` 동작이 지켜집니다. 정상 코드는 타입 선언→추론 계산→저장 출력의 3단계를 따라 `1800`을 출력합니다.',
        'qout': '먼저 minutes가 u32 30으로 선언됩니다. 이어서 u32끼리 30*60을 계산해 seconds에 1800이 저장되고, println!이 표시하므로 출력 `1800`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 타입과 계산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'u32는 음수가 없는 32비트 정수라는 뜻이고, seconds의 타입은 minutes를 사용하는 식에서 추론된다는 뜻은 컴파일러가 문맥에서 정해진 한 타입을 찾아준다는 뜻입니다. `서로 다른 정수 타입을 암묵적으로 섞어도 된다고 생각함`은 반대 사례인데, 타입이 다르면 섞을 때 명시적 변환이 필요하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `서로 다른 정수 타입을 암묵적으로 섞어도 된다고 생각함`입니다. 잘못된 코드는 이번 입력에서는 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 음수 입력에서 갈라지는 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `타입 추론과 정수 연산을 사용함`이라는 의미와 입력·출력 계약입니다. C의 부호 있는·없는 정수 선언과 Python의 동적 타입으로 같은 동작을 구현하고, 실행 결과 `1800`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`30 * 60 = 1800`을 먼저 손으로 계산하세요. 엄청 큰 값을 곱하면 범위를 넘을 수 있으므로 `checked_mul(60)` 같은 실패 처리도 고려하세요.',
    },
    12: {
        'notes': [
            '`30 <= minutes <= 60`은 `minutes`가 30 이상이고 동시에 60 이하인지 검사합니다. 30과 60도 포함됩니다. 29는 왼쪽 조건이 거짓이고 61은 오른쪽 조건이 거짓입니다. 29, 30, 60, 61 네 값을 넣어 표를 만들어 보세요.',
            'Python에서 이처럼 이어 쓴 비교는 중간 값에 대해 양쪽 조건을 확인합니다. C나 Rust의 같은 문법으로 옮기지 말고 각각 `minutes >= 30 && minutes <= 60` 같은 논리곱으로 적어야 합니다. 경계에서 무엇을 포함할지 문제 문장부터 확정하세요.',
        ],
        'predict': '1. 30 <= 45가 참입니다. 2. 45 <= 60도 참입니다. 3. 두 조건을 연결해 True입니다. `30<=45 참 → 45<=60 참 → True` 흐름으로 실제 출력 `True`가 됩니다. 핵심 `<=`는 양쪽 경계를 포함해 비교하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `True`를 출력한다. 46도 30 이상 60 이하 구간 안에 있어 비교 결과가 그대로 True입니다. 값이 실제로 달라진 것은 입력뿐이고 최종 출력은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(30 < 30 <= 60)`입니다. 입력을 경계값 30으로 두면 `30 < 30`이 거짓이 되어 전체가 False가 됩니다. 오류 분류는 잘못된 출력입니다. 경계 포함이 깨지는 첫 순간은 왼쪽 비교식입니다. 고친 줄 `print(30 <= 30 <= 60)`에서는 양쪽 경계를 포함하는 동작이 지켜져 True가 됩니다. 29·30·60·61 표를 만들면, 29는 왼쪽 거짓, 30과 60은 양쪽 참, 61은 오른쪽 거짓이 됩니다. 정상 입력 45에서는 `<`와 `<=`가 모두 참이라 차이가 드러나지 않으므로, 경계값으로 시험해야 합니다.',
        'qout': '먼저 30 <= 45를 평가하면 참입니다. 이어서 45 <= 60도 참이므로 두 조건을 연결해 True가 되고, 출력 `True`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 비교 연산은 정상 실행되기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '`30 <= minutes <= 60`은 왼쪽·오른쪽 비교를 모두 만족해야 참이라는 뜻입니다. `<= 대신 <를 써서 정확히 30 또는 60을 빠뜨림`은 반대 사례인데, 경계값을 포함하지 않으면 30과 60에서 결과가 뒤집히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `<= 대신 <를 써서 정확히 30 또는 60을 빠뜨림`입니다. 잘못된 코드는 실행은 되지만 경계 입력에서는 False라는 잘못된 출력을 냅니다. 45 같은 안쪽 값이 아니라 29·30·60·61 표로 시험하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `양쪽 경계를 포함해 비교함`이라는 의미와 입력·출력 계약입니다. C·Rust에서는 논리곱으로 적어야 하므로, 경계값 실행 결과 `True`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '29, 30, 60, 61 네 값을 넣어 표를 만드세요. 30과 60을 포함하는지가 핵심입니다.',
    },
    13: {
        'notes': [
            '조건문은 두 길 중 실행할 길을 하나 고릅니다. `if minutes >= 30:`이 참이면 들여쓴 첫 블록으로, 거짓이면 `else:` 블록으로 갑니다. 30은 참 쪽에 들어가고 29는 거짓 쪽에 들어갑니다. 실행하지 않은 블록의 `print`는 아예 호출되지 않습니다.',
            '들여쓰기는 꾸미기가 아니라 어느 줄이 어느 갈래에 속하는지 정합니다. 예를 들어 조건 밖으로 `print("완료")`를 빼면 어느 쪽을 택해도 출력됩니다. 29, 30, 31을 각각 넣고 실행 순서를 종이에 화살표로 그려 보세요.',
        ],
        'predict': '1. minutes에 45를 넣습니다. 2. 45>=30이 참입니다. 3. if 블록을 출력하고 else 블록은 건너뜁니다. `minutes=45 → 조건 참 → 목표 달성` 흐름으로 실제 출력 `목표 달성`이 됩니다. 핵심 `>= 30`은 어느 갈래를 탈지 정하는 조건 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `목표 달성`을 출력한다. 46도 30 이상이라 같은 갈래를 타 출력이 같습니다. 값이 실제로 달라진 것은 입력뿐이고 최종 출력은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `if minutes < 30:`입니다. 45에 대해 `< 30`은 거짓이므로 else 갈래로 가서 `조금 더`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 갈래가 뒤집히는 첫 순간은 조건식입니다. 고친 줄 `if minutes >= 30:`에서는 `조건에 따른 한 경로 선택` 동작이 지켜집니다. 정상 코드는 대입→조건 참→if 블록 출력의 순서로 `목표 달성`을 출력하고, else 블록은 호출되지 않습니다.',
        'qout': '먼저 minutes에 45가 저장됩니다. 이어서 45>=30이 참이므로 if 블록이 실행되어 `목표 달성`이 출력되고, else 블록은 건너뜁니다. 따라서 출력 `목표 달성`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 조건문 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 if 블록의 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'if는 조건이 참인 블록만 실행하고 else는 거짓일 때만 실행한다는 뜻이며, 두 블록을 동시에 실행하지 않는다는 뜻은 한 번의 실행에서 호출되는 print가 하나라는 뜻입니다. `들여쓰기를 지워 else 블록도 실행될 거라 생각함`은 반대 사례인데, 들여쓰기가 갈래 소속을 정하므로 블록을 함께 실행할 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `들여쓰기를 지워 else 블록도 실행될 거라 생각함`입니다. 잘못된 코드는 실행은 되지만 `조금 더`라는 잘못된 출력을 냅니다. 어느 블록이 호출됐는지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `조건에 따른 한 경로 선택`이라는 의미와 입력·출력 계약입니다. C의 if·else와 Rust의 if·match로 같은 동작을 구현하고, 실행 결과 `목표 달성`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '29, 30, 31을 각각 넣어 실행 순서를 화살표로 그리세요. 조건 밖의 `print("완료")`는 어느 갈래에서도 출력됩니다.',
    },
    14: {
        'notes': [
            '`>= 30`의 이상에는 정확히 30도 포함됩니다. 조건 오류는 대개 30을 빼먹거나 `>`를 `>=`로 혼동할 때 생깁니다. 테스트 세 개를 한 묶음으로 만드세요. 바로 아래인 29, 경계인 30, 바로 위인 31입니다.',
            '출력만 확인하면 왜 그 갈래로 갔는지 놓치기 쉽습니다. 각 값에 대해 `minutes >= 30`의 참·거짓부터 적고, 어느 `printf`가 실행되는지 쓰세요. `if`의 조건이 거짓일 때와 참일 때의 동작을 모두 말할 수 있어야 복습이 끝납니다.',
        ],
        'predict': '1. minutes가 정확히 30입니다. 2. 30>=30은 참입니다. 3. 달성을 출력하며 else는 실행하지 않습니다. `minutes=30 → 경계 포함 → 달성` 흐름으로 실제 출력 `달성`이 됩니다. 핵심 `>= 30`은 경계를 포함해 갈래를 정하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `달성`을 출력한다. 31도 30 이상이라 같은 갈래를 타 출력이 같습니다. 값이 실제로 달라진 것은 입력뿐이고 최종 출력은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `if (minutes > 30) puts("달성");`입니다. 30에 대해 `> 30`은 거짓이므로 else로 가서 `미달`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 경계 포함이 깨지는 첫 순간은 비교 연산자입니다. 고친 줄 `if (minutes >= 30) puts("달성");`에서는 `경계가 포함되는 분기 검증` 동작이 지켜집니다. 정상 코드는 대입→경계 포함 판정→달성 출력의 순서로 `달성`을 출력합니다.',
        'qout': '먼저 minutes에 30이 저장됩니다. 이어서 30>=30이 참이므로 달성 쪽 printf가 실행되고 else는 건너뛰어 출력 `달성`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 조건문 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 달성 쪽 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '`30 이상은 30도 포함합니다`는 경계값 자체가 참 쪽에 속한다는 뜻이고, `조건의 경계값과 경계 양쪽 값을 세 개의 테스트로 묶습니다`는 29·30·31로 묶어 시험한다는 뜻입니다. `>로 바꿔 30분을 미달로 잘못 판정함`은 반대 사례인데, 경계를 빠뜨리면 30에서 갈래가 뒤집히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `>로 바꿔 30분을 미달로 잘못 판정함`입니다. 잘못된 코드는 실행은 되지만 `미달`이라는 잘못된 출력을 냅니다. 29·30·31 묶음으로 시험하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `경계가 포함되는 분기 검증`이라는 의미와 입력·출력 계약입니다. Python의 이어 쓴 비교와 Rust의 범위 조건으로 같은 동작을 구현하고, 경계값 실행 결과 `달성`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '29·30·31 묶음으로 시험하세요. 출력만 보지 말고 각 값의 참·거짓부터 적으세요.',
    },
    15: {
        'notes': [
            '`match`는 한 값을 여러 경우와 맞춰 보고 해당 갈래를 실행합니다. 예제의 `2 =>`는 값이 2일 때 들어가는 갈래입니다. `_ =>`는 앞선 경우에 맞지 않은 나머지 값입니다. `match`가 값을 돌려주도록 작성했다면 각 갈래의 결과가 호환되는 타입이어야 합니다.',
            '`match 3`이면 `2` 갈래는 건너뛰고 나머지 갈래로 갑니다. 정확히 한 가지 경우만 처리합니다. Rust는 가능한 경우를 다루도록 요구하므로 `_`가 없고 일부 숫자만 다뤘다면 빠진 경우에 대해 컴파일 오류가 납니다. 갈래마다 어떤 입력이 들어오는지 적어 보세요.',
        ],
        'predict': '1. level은 2입니다. 2. 1 패턴을 건너뛰고 2 패턴을 선택합니다. 3. label에 기본을 저장해 출력합니다. `level=2 → 2 arm 선택 → 기본` 흐름으로 실제 출력 `기본`이 됩니다. 핵심 `2 =>`는 값 2가 들어오는 갈래를 정하는 자리에 쓰입니다.',
        'guided': '원본은 `기본`, 수정본은 `심화`이다. 첫 변경 줄 `let level = 2;`에서 `let level = 3;`으로 입력 값을 바꾸면, 1과 2 갈래를 모두 건너뛰고 나머지 갈래가 선택되어 label이 달라진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `let label = match level { 1 => "입문", 3 => "기본", _ => "심화" };`입니다. 입력 2에 대해 1 갈래도 3 갈래도 맞지 않으므로 나머지 갈래가 선택되어 `심화`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 갈래 선택이 어긋나는 첫 순간은 2 패턴 자리입니다. 고친 줄의 `2 =>`에서는 `완전한 패턴 분기` 동작이 지켜집니다. `_`가 아예 없고 일부 숫자만 다뤘다면 그때는 컴파일 오류가 나므로, 이번 잘못된 코드와 구별하세요. 정상 코드는 대입→위에서 아래 비교→첫 일치 선택의 순서로 `기본`을 출력합니다.',
        'qout': '먼저 level에 2가 저장됩니다. 이어서 match가 1 패턴을 건너뛰고 2 패턴을 선택해 label에 기본이 저장되고, println!이 표시하므로 출력 `기본`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 모든 갈래가 있어 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'match는 위에서 아래로 비교해 처음 맞는 arm을 선택한다는 뜻이고, _는 나머지 모든 값을 받는다는 뜻은 어떤 입력이 와도 갈래가 정해진다는 뜻입니다. `모든 패턴을 덮지 않아 컴파일이 실패함`은 반대 사례인데, _ 없이 일부만 다루면 빠진 경우 때문에 컴파일이 막히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `모든 패턴을 덮지 않아 컴파일이 실패함`입니다. 이번 잘못된 코드는 _가 있어 실행은 되지만 `심화`라는 잘못된 출력을 냅니다. 갈래마다 어떤 입력이 들어오는지 적어 보세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `완전한 패턴 분기`라는 의미와 입력·출력 계약입니다. C의 switch·default와 Python의 if·elif·else로 같은 동작을 구현하고, 실행 결과 `기본`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`match 3`이면 2 갈래는 건너뛰고 나머지 갈래로 갑니다. 각 갈래에 어떤 입력이 들어오는지 표로 적어 보세요.',
    },
    16: {
        'notes': [
            '누적 합계는 빈 통에 값을 하나씩 붓는 과정입니다. `total = 0`에서 시작하고 반복에서 `total += minutes`를 실행할 때마다 지금까지 본 항목의 합이 됩니다. 예를 들어 `[10, 20, 30]`이면 시작 0, 첫 반복 10, 둘째 30, 셋째 60입니다.',
            '반복문 바깥에서만 `print(total)`을 하면 최종 합계가 한 번 출력됩니다. 안쪽에서 찍으면 중간 합계 10, 30, 60이 차례로 나옵니다. 빈 리스트면 반복문 본문이 실행되지 않고 합계는 0입니다. `total`을 매번 0으로 다시 만들면 누적이 깨진다는 점도 확인하세요.',
        ],
        'predict': '1. total=0에서 시작합니다. 2. 10·20·30을 차례로 더합니다. 3. 루프가 끝나면 60을 출력합니다. `total=0 → 10 → 30 → 60` 흐름으로 실제 출력 `60`이 됩니다. 핵심 `total += minutes`는 지금까지의 합에 새 값을 더해 저장하는 자리에 쓰입니다.',
        'guided': '원본은 `60`, 수정본은 `61`이다. 첫 변경 줄 `total = 0`에서 `total = 1`로 시작 값을 바꾸면, 이후 세 번의 덧셈이 그대로 이어져 최종 합이 1만큼 커진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `total = minutes`입니다. 매 반복마다 지금까지의 합이 아니라 현재 항목으로 덮어쓰므로, 마지막 항목 30만 남고 `30`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 누적이 깨지는 첫 순간은 첫 반복의 대입입니다. 고친 줄 `total += minutes`에서는 `반복 누적 합계` 동작이 지켜집니다. 정상 코드는 0 시작→10→30→60의 순서로 합을 쌓아 `60`을 출력합니다.',
        'qout': '먼저 total이 0에서 시작됩니다. 이어서 10·20·30을 차례로 더해 10, 30, 60으로 쌓이고, 루프가 끝난 뒤 print가 최종 합을 출력하므로 `60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 반복문 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'total은 지금까지 읽은 항목의 합이라는 뜻은, 매 반복 후에도 그 불변식이 유지된다는 뜻입니다. `반복마다 total을 새 값으로 덮어써 마지막 항목만 남김`은 반대 사례인데, 덮어쓰면 이전 합이 사라져 불변식이 깨지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `반복마다 total을 새 값으로 덮어써 마지막 항목만 남김`입니다. 잘못된 코드는 실행은 되지만 `30`이라는 잘못된 출력을 냅니다. 중간 합계 10, 30, 60을 손으로 적어 보세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `반복 누적 합계`라는 의미와 입력·출력 계약입니다. C의 for 누적과 Rust의 iterator 합으로 같은 동작을 구현하고, 실행 결과 `60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 리스트면 본문이 실행되지 않고 합계는 0입니다. print를 안쪽에 두면 10, 30, 60이 차례로 나옵니다.',
    },
    17: {
        'notes': [
            '`while (n > 0)`은 조건이 참인 동안 반복합니다. `n--`는 현재 `n`을 1 줄여 언젠가 0으로 만드는 역할입니다. 3부터 시작하면 본문 실행 때 n이 3, 2, 1로 바뀌고 0이 되면 다음 검사에서 멈춥니다.',
            '`n--`를 빼면 조건이 계속 참이어서 끝나지 않을 수 있습니다. 반대로 `n`이 처음부터 0이면 본문은 한 번도 실행되지 않습니다. 반복을 읽을 때 무엇을 반복하는가, 어떤 값이 바뀌는가, 왜 반드시 끝나는가를 각각 답하세요.',
        ],
        'predict': '1. n=3이고 조건이 참입니다. 2. 3·2·1을 출력할 때마다 n을 줄입니다. 3. n=0에서 종료합니다. `n=3 → 2 → 1 → 0 종료` 흐름으로 실제 출력 `3 2 1`이 됩니다. 핵심 `n--`는 조건을 거짓 쪽으로 갱신하는 자리에 쓰입니다.',
        'guided': '원본은 `3 2 1`, 수정본은 `4 3 2 1`이다. 첫 변경 줄 `int n = 3;`에서 `int n = 4;`로 시작 값을 바꾸면, 4·3·2·1을 출력한 뒤 0에서 멈추므로 한 칸이 앞에 더 붙는다.',
        'debug_outcome': 'infinite-loop',
        'debug': '틀린 줄은 `while (n > 0) { printf("%d ", n); n++; }`입니다. `n++`는 n을 계속 키우므로 3, 4, 5와 같이 커지기만 하고 조건 `n > 0`을 영원히 거짓으로 만들지 못합니다. 오류 분류는 무한 반복입니다. 단순히 출력이 달라진 것이 아니라, 종료를 보장하는 갱신 불변식이 깨져 멈추지 않는 것이 원인입니다. 고친 줄의 `n--`에서는 `상태를 종료 조건 쪽으로 갱신함` 동작이 지켜집니다. 정상 코드는 3→2→1 출력 뒤 0에서 멈춰 `3 2 1`을 출력합니다.',
        'qout': '먼저 n에 3이 저장되고 조건이 참입니다. 이어서 3·2·1을 출력할 때마다 n이 1씩 줄어들고, 0이 되면 조건이 거짓이 되어 멈추므로 출력 `3 2 1`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 반복문 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 세 번 호출되기 때문입니다.',
        'qmodel': '매 반복마다 n이 1씩 줄어 0에 가까워진다는 뜻은, 갱신이 종료 조건 쪽으로 향한다는 뜻이고, 0이 되면 조건이 거짓이라 멈춘다는 뜻은 반복이 끝날 보장이 있다는 뜻입니다. `n을 줄이지 않아 무한 루프가 됨`은 반대 사례인데, 갱신이 없으면 조건이 영원히 참이기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `n을 줄이지 않아 무한 루프가 됨`입니다. 잘못된 코드는 끝나지 않고 3, 4, 5를 계속 출력합니다. 무엇이 바뀌는지, 왜 끝나는지를 각각 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `상태를 종료 조건 쪽으로 갱신함`이라는 의미와 입력·출력 계약입니다. Python의 while 갱신과 Rust의 loop 탈출 조건으로 같은 동작을 구현하고, 실행 결과 `3 2 1`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`n`이 처음부터 0이면 본문은 한 번도 실행되지 않습니다. 갱신을 뺀 버전과 0 시작 버전을 구별하세요.',
    },
    18: {
        'notes': [
            '`loop`는 스스로 멈추지 않는 Rust 반복문입니다. `break`를 만날 때 끝나고, `break n * 2`처럼 값을 함께 적으면 그 값이 loop 표현식 전체의 결과가 됩니다. 조건을 확인하기 전까지 `n`이 어떻게 바뀌는지 적고, 어느 회차에서 `break`를 만나는지 확인하세요.',
            '`while`은 조건이 거짓일 때 자연스럽게 끝나고 `loop`는 멈출 위치를 직접 지정합니다. 종료 조건이 잘못되어 영원히 `break`를 만나지 않으면 프로그램도 끝나지 않습니다. `break;`처럼 값 없이 빠져나오는 경우와 결과값을 돌려주는 경우를 구별하세요.',
        ],
        'predict': '1. n은 0에서 출발합니다. 2. 1·2는 계속 반복합니다. 3. 3에서 break 6으로 종료하여 answer=6입니다. `n=0 → 1 → 2 → 3 → answer=6` 흐름으로 실제 출력 `6`이 됩니다. 핵심 `break n * 2`는 탈출과 동시에 결과값을 정하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `6`을 출력한다. 멈추는 조건은 n이 3이 되는 순간이라 시작값과 무관하게 break 값 3 곱하기 2로 6이 됩니다. 시작값만 달라지고 종료식이 그대로이므로 최종 출력은 그대로라고 명시한다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `let answer = loop { n += 1; if n == 3 { break n; } };`입니다. break 값이 `n * 2`가 아니라 `n`이므로 탈출 순간 answer에는 3이 들어가 `3`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 결과값이 달라지는 첫 순간은 break 뒤 식입니다. 고친 줄의 `break n * 2`에서는 `반복 종료 값을 사용함` 동작이 지켜집니다. 정상 코드는 n 증가→3에서 탈출→6 전달의 순서로 `6`을 출력합니다.',
        'qout': '먼저 n이 0에서 출발합니다. 이어서 1·2 회차는 조건이 맞지 않아 계속 돌고, 3에서 break를 만나 `n * 2` 값 6이 loop 전체의 결과가 되어 answer에 들어가므로 출력 `6`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'Rust의 loop는 명시적 break가 실행될 때까지 돈다는 뜻이고, break 뒤 값이 loop 표현식의 값이라는 뜻은 탈출 값이 변수에 저장된다는 뜻입니다. `break 뒤의 값을 무시하고 loop는 언제나 빈 값을 준다고 생각함`은 반대 사례인데, 값을 적으면 그 값이 결과가 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `break 뒤의 값을 무시하고 loop는 언제나 빈 값을 준다고 생각함`입니다. 잘못된 코드는 실행은 되지만 `3`이라는 잘못된 출력을 냅니다. break 뒤 식을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `반복 종료 값을 사용함`이라는 의미와 입력·출력 계약입니다. C의 break 탈출값 패턴과 Python의 루프 결과 변수로 같은 동작을 구현하고, 실행 결과 `6`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`break;`처럼 값 없이 빠져나오면 결과값이 없고, `break n * 2`처럼 적으면 결과가 있습니다. 두 경우를 구별하세요.',
    },
    19: {
        'notes': [
            '중첩 반복은 바깥쪽 값을 하나 정한 다음 안쪽 반복을 처음부터 끝까지 돌립니다. `a`가 1, 2이고 `b`가 3, 4이면 방문 순서는 `(1,3)`, `(1,4)`, `(2,3)`, `(2,4)`입니다. `a`가 한 번 바뀔 때 `b`는 다시 처음부터 시작합니다.',
            '바깥 2번, 안쪽 2번이면 본문은 4번 실행됩니다. 입력이 각각 n개, m개이면 최대 n×m번입니다. 표의 행과 열을 그려 방문 순서를 따라가 보세요. `b`가 비어 있으면 바깥 반복은 돌아도 안쪽 본문은 실행되지 않습니다.',
        ],
        'predict': '1. a=1일 때 b=3·4를 기록합니다. 2. a=2일 때 다시 b=3·4를 기록합니다. 3. 총 2×2=4쌍입니다. `a=1,b=3 → a=1,b=4 → a=2,b=3 → a=2,b=4` 흐름으로 실제 출력 `[(1, 3), (1, 4), (2, 3), (2, 4)]`가 됩니다. 핵심 `for b`는 안쪽 값을 처음부터 끝까지 방문하는 자리에 쓰입니다.',
        'guided': '원본은 `[(1, 3), (1, 4), (2, 3), (2, 4)]`, 수정본은 `[(2, 3), (2, 4), (2, 3), (2, 4)]`이다. 첫 변경 줄 `for a in [1, 2]:`에서 `for a in [2, 2]:`로 바깥 값을 바꾸면, 두 바퀴 모두 a=2에서 시작해 같은 안쪽 방문이 반복된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `for a in [3, 4]:`입니다. 안쪽 반복이 바깥 `a`와 같은 이름을 쓰므로 바깥 값이 가려지고, 본문의 `b`는 어디에도 정의되지 않아 첫 append에서 NameError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 정의되지 않은 이름 `b`를 읽는 본문입니다. 고친 줄 `for b in [3, 4]:`에서는 `두 집합의 모든 조합 생성` 동작이 지켜집니다. 정상 코드는 바깥 고정→안쪽 전체 방문→다음 바깥 값의 순서로 4쌍을 만듭니다.',
        'qout': '먼저 a=1로 고정하고 안쪽에서 b=3·4를 기록합니다. 이어서 a=2로 바꾸고 다시 b=3·4를 기록하므로 네 쌍이 순서대로 모여 출력 `[(1, 3), (1, 4), (2, 3), (2, 4)]`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 반복문 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '바깥 a를 고정하고 안쪽 b의 모든 값을 방문한 다음 a를 바꾼다는 뜻은, a가 한 번 바뀔 때 b가 처음부터 다시 돈다는 뜻입니다. `안쪽 반복이 전체에서 한 번만 실행된다고 생각함`은 반대 사례인데, 실제로는 바깥 한 바퀴마다 안쪽 전체가 실행되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `안쪽 반복이 전체에서 한 번만 실행된다고 생각함`입니다. 잘못된 코드는 실행되다가 정의되지 않은 이름에서 NameError로 멈춥니다. 안쪽 변수가 바깥과 다른 이름인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `두 집합의 모든 조합 생성`이라는 의미와 입력·출력 계약입니다. C의 중첩 for와 Rust의 중첩 iterator로 같은 동작을 구현하고, 실행 결과 네 쌍으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`b`가 비어 있으면 바깥 반복은 돌아도 안쪽 본문은 실행되지 않아 빈 리스트가 됩니다. n×m 개수를 손으로 세어 보세요.',
    },
    20: {
        'notes': [
            '함수는 입력을 받아 결과를 돌려주는 작은 계산 도구입니다. `add(10, 20)`을 부르면 `a=10`, `b=20`이 되고 `return a + b`는 30을 호출한 자리로 보냅니다. `return`은 화면 출력이 아닙니다. 호출한 쪽에서 `printf`로 결과를 보여 줘야 합니다.',
            '매개변수 `a`, `b`는 함수 안에서 쓰는 이름입니다. C에서 정수 인수를 넘기면 그 값이 복사되어 함수에 전달되므로 함수 안의 `a`를 바꿔도 호출자 변수 자체는 바뀌지 않습니다. 값을 받는 순서가 바뀌면 뺄셈 같은 함수에서는 결과도 바뀝니다.',
        ],
        'predict': '1. main에서 add(2,3)을 호출합니다. 2. 함수의 a=2,b=3으로 계산합니다. 3. 반환 5를 printf가 출력합니다. `인수 2,3 전달 → a+b=5 → 호출 지점에 5 반환` 흐름으로 실제 출력 `5`가 됩니다. 핵심 `return a + b`는 계산 결과를 호출한 자리로 보내는 자리에 쓰입니다.',
        'guided': '원본은 `5`, 수정본은 `6`이다. 첫 변경 줄 `printf("%d\\n", add(2, 3));`에서 `printf("%d\\n", add(3, 3));`로 인수를 바꾸면, 함수 안에서 3+3이 계산되어 반환된 6이 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `int add(int a, int b) { return a - b; }`입니다. 2-3을 계산하므로 실행은 되지만 결과는 -1이 됩니다. 오류 분류는 잘못된 출력입니다. 약속과 달라지는 첫 순간은 반환식의 연산자입니다. 고친 줄의 `return a + b`에서는 `입력값을 받아 반환값을 계산함` 동작이 지켜집니다. 정상 코드는 인수 전달→덧셈→반환→출력의 순서로 `5`를 출력합니다.',
        'qout': '먼저 main에서 add(2,3)이 호출되어 a=2,b=3이 됩니다. 이어서 a+b=5가 계산되어 호출 지점에 반환되고, printf가 표시하므로 출력 `5`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '매개변수 a,b는 호출 시 전달된 값이라는 뜻이고, return은 계산 결과를 호출한 위치에 돌려준다는 뜻은 반환값이 호출한 식의 값이 된다는 뜻입니다. `함수 안의 a를 main의 변수와 같은 저장공간이라고 생각함`은 반대 사례인데, 정수 인수는 값이 복사되어 전달되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `함수 안의 a를 main의 변수와 같은 저장공간이라고 생각함`입니다. 잘못된 코드는 실행은 되지만 `-1`이라는 잘못된 출력을 냅니다. 반환식의 연산자를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `입력값을 받아 반환값을 계산함`이라는 의미와 입력·출력 계약입니다. Python의 return과 Rust의 반환 표현식으로 같은 동작을 구현하고, 실행 결과 `5`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '뺄셈 함수처럼 순서가 결과에 영향을 주면 인수 순서를 바꿔 시험하세요. `return`은 화면 출력이 아니므로 호출 쪽 표시를 확인하세요.',
    },
    21: {
        'notes': [
            '함수를 호출할 때마다 함수 안의 `result`는 새로 시작합니다. 입력 `[10,20]`을 더한 뒤 다시 `[5]`를 넘기면 새 호출의 결과는 5여야 합니다. 이전 호출의 합계 30이 새 호출로 새어 나오면 지역 상태를 잘못 공유한 것입니다.',
            '`return result`는 합계를 호출자에게 넘깁니다. 반환한 값을 변수에 저장할 수도 있고 `print(total(...))`처럼 바로 출력할 수도 있습니다. 빈 입력에서는 시작값 0이 그대로 반환됩니다. 중간에 `result += value`가 몇 번 실행되는지 손으로 세어 보세요.',
        ],
        'predict': '1. 호출 시 값 [10,20]을 전달합니다. 2. result가 0→10→30으로 바뀝니다. 3. return 30을 print가 받습니다. `입력 [10,20] → result=10 → result=30 → 출력` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `result += value`는 호출별 지역 합을 갱신하는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `result = 0`에서 `result = 1`로 시작 값을 바꾸면, 이후 두 번의 덧셈이 그대로 이어져 최종 합이 1만큼 커진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `result = value`입니다. 매 반복마다 누적합이 아니라 현재 항목으로 덮어쓰므로 마지막 값 20만 반환되어 `20`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 누적이 깨지는 첫 순간은 반복 안의 대입입니다. 고친 줄 `result += value`에서는 `반복 계산을 함수로 묶음` 동작이 지켜집니다. 정상 코드는 0 시작→10→30 누적 뒤 반환의 순서로 `30`을 출력합니다.',
        'qout': '먼저 total([10, 20]) 호출로 values에 [10,20]이 전달됩니다. 이어서 result가 0→10→30으로 쌓이고 return 30을 print가 받아 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 함수 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '함수 내부 result는 호출마다 새로 시작한다는 뜻은, 이전 호출의 합이 새 호출로 새어 나오지 않는다는 뜻이고, return이 최종 합계를 호출자에게 전달한다는 뜻은 반환값이 호출한 식의 값이 된다는 뜻입니다. `반복 안에서 result를 덮어써 마지막 값만 반환함`은 반대 사례인데, 덮어쓰면 누적 불변식이 깨지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `반복 안에서 result를 덮어써 마지막 값만 반환함`입니다. 잘못된 코드는 실행은 되지만 `20`이라는 잘못된 출력을 냅니다. result가 몇 번 갱신되는지 손으로 세어 보세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `반복 계산을 함수로 묶음`이라는 의미와 입력·출력 계약입니다. C의 함수 누적과 Rust의 iterator 합으로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 입력에서는 시작값 0이 그대로 반환됩니다. `[10,20]` 뒤에 `[5]`를 넘기면 새 호출의 결과는 5여야 합니다.',
    },
    22: {
        'notes': [
            'Rust 함수 `fn twice(n: i32) -> i32`에서 화살표 뒤는 반환 타입입니다. 본문 마지막 `n * 2`는 세미콜론이 없어서 값으로 평가되어 호출자에게 돌아갑니다. `n * 2;`로 바꾸면 그 줄은 값을 버리는 문장이 되어 기대한 `i32`를 돌려주지 못합니다.',
            '`let answer = twice(15);`라면 함수 내부 `n=15`, 곱한 값 30, 호출자 `answer=30` 순서로 따라가세요. 명시적으로 `return n * 2;`라고 쓸 수도 있지만 마지막 표현식 방식에 익숙해지면 Rust의 `if`와 `match`도 값을 만든다는 점이 이해됩니다.',
        ],
        'predict': '1. doubled(7)에 n=7을 묶습니다. 2. 마지막 표현식이 14입니다. 3. main의 println!이 14를 출력합니다. `n=7 → 7*2=14 → 반환 14` 흐름으로 실제 출력 `14`가 됩니다. 핵심 `n * 2`는 값을 버리지 않고 호출자에게 돌려주는 자리에 쓰입니다.',
        'guided': '원본은 `14`, 수정본은 `21`이다. 첫 변경 줄 `n * 2`에서 `n * 3`으로 곱수를 바꾸면, 같은 n=7에 대해 21이 계산되어 반환된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `n * 3`입니다. 7에 대해 21이 계산되어 실행은 되지만 결과는 `21`이 됩니다. 오류 분류는 잘못된 출력입니다. 약속과 달라지는 첫 순간은 마지막 표현식의 곱수입니다. 고친 줄 `n * 2`에서는 `표현식 결과를 반환함` 동작이 지켜집니다. 세미콜론을 붙인 `n * 2;`는 값을 버리는 문장이 되어 반환형이 달라지므로, 이번 잘못된 코드와 구별하세요. 정상 코드는 인수 묶기→표현식 평가→반환의 순서로 `14`를 출력합니다.',
        'qout': '먼저 doubled(7) 호출로 n=7이 묶입니다. 이어서 마지막 표현식 7*2=14가 평가되어 호출자에게 반환되고, println!이 표시하므로 출력 `14`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 타입과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': '본문 마지막 n*2는 세미콜론이 없어서 i32 값을 돌려준다는 뜻은, 표현식 값이 곧 함수의 반환값이 된다는 뜻입니다. `마지막 표현식에 세미콜론을 붙여 반환형이 ()가 됨`은 반대 사례인데, 세미콜론이 있으면 값을 버리는 문장이 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `마지막 표현식에 세미콜론을 붙여 반환형이 ()가 됨`입니다. 이번 잘못된 코드는 실행은 되지만 `21`이라는 잘못된 출력을 냅니다. 마지막 줄의 세미콜론 유무를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `표현식 결과를 반환함`이라는 의미와 입력·출력 계약입니다. C의 return과 Python의 return으로 같은 동작을 구현하고, 실행 결과 `14`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`n * 2;`처럼 세미콜론을 붙이면 값을 버리는 문장이 됩니다. 표현식과 문장을 구별하세요.',
    },
    23: {
        'notes': [
            '함수 밖 `name`과 함수 안 `name`은 철자가 같아도 별개의 이름입니다. 함수 안에서 `name = "안"`을 하면 그 함수의 지역 이름에 새 값을 넣는 것이며 바깥의 `name`에는 손대지 않습니다. 함수가 끝나면 지역 이름은 더 이상 보이지 않습니다.',
            '함수 안에서 바깥 값을 바꾸고 싶다면 반환값을 받아 `name = change(name)`처럼 갱신하는 편이 흐름을 읽기 쉽습니다. 함수 안에서 먼저 `print(name)`을 한 뒤 같은 함수에서 `name = ...`을 적으면 Python의 지역 이름 규칙 때문에 오류가 날 수 있으니 위치를 확인하세요.',
        ],
        'predict': '1. 바깥 name에 밖을 둡니다. 2. show 내부에 별도의 안을 둬 출력합니다. 3. 함수 뒤 바깥의 밖을 출력합니다. `바깥=밖 → 안쪽=안 출력 → 바깥=밖 출력` 흐름으로 실제 출력 `안 / 밖`이 됩니다. 핵심 `name = "안"`은 함수 지역에만 값을 넣는 자리에 쓰입니다.',
        'guided': '원본은 `안 / 밖`, 수정본은 `안 / 밖!`이다. 첫 변경 줄 `name = "밖"`에서 `name = "밖!"`로 바깥 값을 바꾸면, 함수 안 출력은 그대로 안이고 마지막 바깥 출력만 밖!이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `name = "밖"`입니다. show 안의 지역 이름에 바깥과 같은 값을 넣었으므로 함수 안 출력이 안이 아니라 밖이 되어 `밖 / 밖`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 안쪽 값이 어긋나는 첫 순간은 지역 대입입니다. 고친 줄 `name = "안"`에서는 `지역 범위와 바깥 범위를 구분함` 동작이 지켜집니다. 정상 코드는 바깥 저장→지역 저장·출력→바깥 출력의 순서로 `안 / 밖`을 출력합니다.',
        'qout': '먼저 바깥 name에 밖이 저장됩니다. 이어서 show가 호출되어 지역 name에 안을 저장하고 출력한 뒤, 함수 뒤에서 바깥 name의 밖을 출력하므로 `안 / 밖`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 함수 정의와 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 두 번 호출되기 때문입니다.',
        'qmodel': '함수 내부의 지역 name은 바깥 name과 다른 바인딩이라는 뜻이고, 함수가 끝나면 사라진다는 뜻은 지역 이름이 바깥에 영향을 주지 않는다는 뜻입니다. `함수 안의 대입이 자동으로 바깥 변수를 바꾼다고 생각함`은 반대 사례인데, 지역 대입은 그 함수의 이름에만 값을 넣기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `함수 안의 대입이 자동으로 바깥 변수를 바꾼다고 생각함`입니다. 잘못된 코드는 실행은 되지만 `밖 / 밖`이라는 잘못된 출력을 냅니다. 안쪽 이름과 바깥 이름이 별개인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `지역 범위와 바깥 범위를 구분함`이라는 의미와 입력·출력 계약입니다. C의 지역 변수와 Rust의 섀도잉·범위로 같은 동작을 구현하고, 실행 결과 `안 / 밖`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '함수 안에서 먼저 `print(name)`을 한 뒤 같은 함수에서 대입하면 지역 이름 규칙 때문에 오류가 날 수 있습니다. 읽기와 대입의 위치를 확인하세요.',
    },
    24: {
        'notes': [
            '재귀 함수는 작업을 더 작은 같은 종류의 작업으로 바꿔 자기 자신을 호출합니다. 예를 들어 `sum_to(3)`이 `3 + sum_to(2)`를 부르고, `sum_to(2)`가 `2 + sum_to(1)`을 부릅니다. `n == 0`이면 더 이상 호출하지 않고 0을 돌려주는 바닥이 됩니다. 돌아오면서 1, 3, 6이 순서대로 만들어집니다.',
            '종료 조건을 빼거나 n을 줄이지 않으면 호출이 끝나지 않습니다. 음수가 들어오면 n을 1씩 빼는 방식으로는 0에 닿지 않으므로 입력 조건을 정해야 합니다. 호출할 때와 반환할 때의 순서를 따로 적어 보세요.',
        ],
        'predict': '1. sum_to(3)이 3+sum_to(2)를 기다립니다. 2. 2+sum_to(1), 1+sum_to(0)를 거칩니다. 3. 0에서 돌아와 1+2+3=6입니다. `sum_to(3) → sum_to(2) → sum_to(1) → sum_to(0) → 6` 흐름으로 실제 출력 `6`이 됩니다. 핵심 `n == 0`은 호출을 멈추는 바닥을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `6`, 수정본은 `5`이다. 첫 변경 줄 `if (n == 0) return 0;`에서 `if (n == 1) return 0;`으로 바닥을 바꾸면, 3+2에 1 대신 0이 더해져 5가 된다. 돌아오는 값 1, 3, 5의 순서를 손으로 적어 보세요.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `if (n == -1) return 0;`입니다. 입력 3에 대해서는 3+2+1+0+0으로 우연히 같은 `6`이 출력됩니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. 0에서 멈춰야 하는 함수 계약이 어긋나 있으므로, 음수 입력이나 일반화된 재귀에서는 종료와 정의가 달라집니다. `6이 나오지 않는다`고 쓰면 안 됩니다. 고친 줄 `if (n == 0) return 0;`에서는 `문제를 줄여 재귀적으로 합산함` 동작이 지켜집니다. 정상 코드는 내려가기→바닥 0→돌아오며 더하기의 순서로 `6`을 출력합니다.',
        'qout': '먼저 sum_to(3)이 3+sum_to(2)를 기다립니다. 이어서 2+sum_to(1), 1+sum_to(0)를 거쳐 0에서 돌아오며 1, 3, 6이 만들어지므로 출력 `6`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 재귀 호출과 종료가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '기저 조건 n=0이 멈추는 바닥이라는 뜻은, 그 조건에서 더 이상 호출하지 않는다는 뜻이고, 각 호출은 n을 1 줄여 그 바닥으로 접근한다는 뜻은 호출이 반드시 끝난다는 뜻입니다. `기저 조건이 없거나 입력을 줄이지 않아 끝없이 호출함`은 반대 사례인데, 바닥이 없으면 호출이 멈추지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `기저 조건이 없거나 입력을 줄이지 않아 끝없이 호출함`입니다. 이번 잘못된 코드는 입력 3에서는 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 0에서 멈춘다는 계약을 어긴 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `문제를 줄여 재귀적으로 합산함`이라는 의미와 입력·출력 계약입니다. Python의 재귀와 Rust의 재귀 함수로 같은 동작을 구현하고, 실행 결과 `6`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '음수가 들어오면 1씩 빼는 방식으로는 0에 닿지 않습니다. 입력 조건을 정하고 호출·반환 순서를 따로 적어 보세요.',
    },
    25: {
        'notes': [
            '`Result`는 성공하면 값, 실패하면 오류를 한 가지 타입에 담는 약속입니다. 문자열 `"30"`을 숫자로 바꾸면 `Ok(30)`이 될 수 있고 `"삼십"`이면 `Err(...)`가 됩니다. `match`는 두 경우를 나눠 처리하므로 오류를 무심코 정상 값처럼 쓸 수 없습니다.',
            '실패를 만났을 때 무조건 `unwrap()`으로 중단시키기보다, 학습 앱이라면 사용자에게 입력을 고치라고 알려 줄 수 있습니다. 성공 쪽에서만 숫자 덧셈을 하세요. 숫자로 바꾸는 작업과 결과를 쓰는 작업을 나눠 읽으면 `Result`의 흐름이 선명해집니다.',
        ],
        'predict': '1. 문자열 30을 u32로 파싱합니다. 2. Ok(30)이 생성됩니다. 3. match의 Ok arm에서 30을 출력합니다. `문자열 30 → Ok(30) → 30 출력` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `"30"`은 파싱할 입력 값을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `오류`이다. 첫 변경 줄의 `"30"`에서 `"30!"`로 입력 값을 바꾸면, 느낌표 때문에 파싱이 실패해 Err 갈래로 가므로 `오류`가 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `match parse_minutes("x") { Ok(n) => println!("{n}"), Err(_) => println!("오류") }`입니다. `"x"`는 숫자가 아니므로 파싱이 실패해 Err 갈래로 가고 `오류`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 갈래가 달라지는 첫 순간은 파싱 결과입니다. 고친 줄의 `"30"`에서는 `파싱 성공과 실패를 분리함` 동작이 지켜집니다. 정상 코드는 파싱→Ok 판정→값 출력의 순서로 `30`을 출력합니다.',
        'qout': '먼저 문자열 30을 u32로 파싱합니다. 이어서 Ok(30)이 생성되고, match가 Ok arm을 선택해 30을 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 파싱과 분기가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'Result는 성공 Ok(값) 또는 실패 Err(오류) 중 하나라는 뜻이고, match가 양쪽을 처리한다는 뜻은 어느 경우에도 정해진 동작이 있다는 뜻입니다. `unwrap으로 잘못된 입력에서 프로그램을 종료시킴`은 반대 사례인데, unwrap은 실패를 처리 대신 중단으로 바꾸기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `unwrap으로 잘못된 입력에서 프로그램을 종료시킴`입니다. 잘못된 코드는 실행은 되지만 `오류`라는 다른 갈래의 출력이 나옵니다. 입력이 파싱 가능한지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `파싱 성공과 실패를 분리함`이라는 의미와 입력·출력 계약입니다. C의 오류 코드 반환과 Python의 예외 처리로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 문자열이나 숫자가 아닌 입력은 Err가 됩니다. 성공 쪽에서만 숫자 덧셈을 하세요.',
    },
    26: {
        'notes': [
            '배열 `a = {10,20,30}`은 같은 타입의 세 값을 순서대로 붙여 저장합니다. 번호는 0부터 시작하므로 `a[0]=10`, `a[1]=20`, `a[2]=30`입니다. 두 번째라는 일상 말과 인덱스 1의 차이를 구별하세요.',
            '`a[3]`은 길이 3의 범위 밖입니다. C는 이런 읽기를 자동으로 안전하게 막아주지 않으므로 이상한 값이 보이거나 프로그램이 망가질 수 있습니다. 반복문이라면 `i < 3`을 종료 조건으로 정하고, `i <= 3`과 무엇이 다른지 네 값을 넣어 확인하세요.',
        ],
        'predict': '1. 배열에 10·20·30을 순서대로 저장합니다. 2. 인덱스 1을 읽어 20을 얻습니다. 3. printf가 출력합니다. `인덱스 0=10 → 인덱스 1=20 → 출력` 흐름으로 실제 출력 `20`이 됩니다. 핵심 `[1]`은 두 번째 칸의 값을 읽는 자리에 쓰입니다.',
        'guided': '원본은 `20`, 수정본은 `30`이다. 배열의 값 10·20·30은 그대로이고 읽는 인덱스만 1에서 2로 바뀌었으므로, 기존 출력 20 대신 마지막 원소 30을 출력한다. 인덱스 3은 범위 밖이라 사용하면 안 된다.',
        'debug_outcome': 'undefined-behavior',
        'debug': '틀린 줄은 `printf("%d\\n", minutes[3]);`입니다. `minutes[3]`은 길이 3 배열의 범위 밖 읽기이므로 C의 정의되지 않은 동작에 해당합니다. 우연히 어떤 값이 보일 수도 있고, 비정상 종료나 메모리 손상이 날 수도 있으며, 어떤 결과도 보장할 수 없으므로 특정 출력이 나온다고 쓰지 않습니다. 오류 분류는 정의되지 않은 동작입니다. 고친 줄 `printf("%d\\n", minutes[1]);`에서는 유효한 1번 칸을 읽는 `배열 위치로 값 조회` 동작이 지켜집니다. 정상 코드는 저장→1번 읽기→출력의 순서로 `20`을 출력합니다.',
        'qout': '먼저 배열에 10·20·30이 순서대로 저장됩니다. 이어서 인덱스 1을 읽어 20을 얻고, printf가 표시하므로 출력 `20`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 유효 범위 읽기라 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '길이 3 배열의 첫 원소는 0번이고 마지막은 2번이라는 뜻은, 쓸 수 있는 번호가 0·1·2뿐이라는 뜻이고, 1번은 두 번째 값이라는 뜻은 일상 말 두 번째와 인덱스 1이 같은 칸이라는 뜻입니다. `길이 3의 마지막 인덱스를 3으로 착각해 범위 밖을 읽음`은 반대 사례인데, 3번 칸은 존재하지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `길이 3의 마지막 인덱스를 3으로 착각해 범위 밖을 읽음`입니다. 범위 밖 읽기는 정의되지 않은 동작이므로, 특정 값이 나온다고 약속하지 마세요. `i < 3`과 `i <= 3`을 네 값으로 비교하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `배열 위치로 값 조회`라는 의미와 입력·출력 계약입니다. Python의 리스트 인덱스와 Rust의 범위 검사 인덱싱으로 같은 동작을 구현하고, 실행 결과 `20`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`i < 3`을 종료 조건으로 정하고 `i <= 3`과 무엇이 다른지 네 값을 넣어 확인하세요. 범위 밖은 어떤 결과도 보장할 수 없습니다.',
    },
    27: {
        'notes': [
            'Python 리스트는 여러 값을 순서대로 묶어 둡니다. `[10,20]`에서 `append(30)`을 부르면 맨 뒤에 30이 생겨 `[10,20,30]`이 됩니다. 새 리스트를 받는 것이 아니라 같은 리스트를 변경하는 메서드이므로 `items = items.append(30)`처럼 쓰면 `items`에 `None`이 들어갑니다.',
            '`for value in items:`는 원소 10, 20, 30을 차례로 읽습니다. 인덱스로 접근할 때는 0부터 세고 `len(items)`는 항목 수 3을 돌려줍니다. 빈 리스트에 append한 뒤 길이와 첫 원소가 어떻게 달라지는지도 시험해 보세요.',
        ],
        'predict': '1. [10,20]으로 시작합니다. 2. 끝에 30을 추가해 길이 3이 됩니다. 3. sum이 60을 반환합니다. `[10,20] → [10,20,30] → 60` 흐름으로 실제 출력 `60`이 됩니다. 핵심 `append(30)`은 같은 리스트의 맨 뒤를 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `60`, 수정본은 `61`이다. 첫 변경 줄 `minutes = [10, 20]`에서 `minutes = [11, 20]`으로 첫 값을 바꾸면, 뒤에 30이 붙고 합이 61이 된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `minutes.pop(30)`입니다. `pop`은 꺼낼 위치 번호를 받는데 길이 2인 리스트에 30번을 달라고 하므로, 첫 실행에서 IndexError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 존재하지 않는 위치를 꺼내는 호출입니다. 고친 줄 `minutes.append(30)`에서는 `가변 시퀀스의 끝에 추가함` 동작이 지켜집니다. `append`는 새 리스트를 돌려주지 않고 같은 리스트를 바꾸므로, 반환값을 새 리스트라고 받아 쓰면 `None`이 들어갑니다. 정상 코드는 시작→추가→합산의 순서로 `60`을 출력합니다.',
        'qout': '먼저 [10,20] 리스트가 만들어집니다. 이어서 append로 맨 뒤에 30이 생겨 [10,20,30]이 되고, sum이 합 60을 반환해 출력하므로 `60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 리스트 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'list는 원소를 순서대로 담는다는 뜻이고, append는 맨 끝에 새 원소를 넣는다는 뜻은 같은 리스트가 바뀐다는 뜻입니다. `append의 반환값을 새 리스트라고 생각함`은 반대 사례인데, append는 `None`을 돌려주므로 변수에 받으면 리스트가 사라지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `append의 반환값을 새 리스트라고 생각함`입니다. 잘못된 코드는 존재하지 않는 위치를 꺼내려 해 IndexError로 멈춥니다. 위치 번호와 값을 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `가변 시퀀스의 끝에 추가함`이라는 의미와 입력·출력 계약입니다. C의 크기 고정 배열과 Rust의 Vec push로 같은 동작을 구현하고, 실행 결과 `60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 리스트에 append한 뒤 길이와 첫 원소가 어떻게 달라지는지 시험하세요. `items = items.append(30)`이면 `None`이 들어갑니다.',
    },
    28: {
        'notes': [
            '앞에서 배운 세 표현을 같은 그림에 놓으세요. C 배열은 크기가 정해져 있고, Python 리스트는 항목을 추가할 수 있으며, Rust `Vec`도 `push`로 늘릴 수 있습니다. `vec![10,20]`에 `push(30)`을 하면 `[10,20,30]`입니다. `iter().sum()`은 원소를 순서대로 읽어 60을 만듭니다.',
            'Rust에서 `push`를 하려면 `let mut values`처럼 바꿀 수 있어야 합니다. `iter()`는 각 값을 빌려 읽으므로 합계를 구한 뒤에도 원래 Vec를 계속 쓸 수 있습니다. 세 언어 모두 항목을 방문해 더한다는 동작은 같지만 크기와 수정 규칙은 다릅니다.',
        ],
        'predict': '1. Vec [10,20]을 만듭니다. 2. push(30)으로 [10,20,30]이 됩니다. 3. 순회해 합 60을 출력합니다. `[10,20] → [10,20,30] → 60` 흐름으로 실제 출력 `60`이 됩니다. 핵심 `push(30)`은 가변 Vec의 맨 뒤를 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `60`, 수정본은 `61`이다. 첫 변경 줄 `let mut minutes = vec![10, 20];`에서 `let mut minutes = vec![11, 20];`으로 첫 값을 바꾸면, 뒤에 30이 붙고 합이 61이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `minutes.pop();`입니다. 맨 끝 20을 꺼내므로 합은 10+20이 아니라 10만 남아 `10`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 합이 어긋나는 첫 순간은 끝 원소를 제거한 호출입니다. 고친 줄 `minutes.push(30);`에서는 `가변 벡터를 순회해 합산함` 동작이 지켜집니다. `push`에는 `mut` 바인딩이 필요하므로 불변 바인딩에서는 컴파일이 막힙니다. 정상 코드는 생성→추가→순회 합산의 순서로 `60`을 출력합니다.',
        'qout': '먼저 가변 Vec [10,20]이 만들어집니다. 이어서 push로 30이 붙어 [10,20,30]이 되고, iter().sum()이 합 60을 만들어 출력하므로 `60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 타입과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'Vec는 가변 길이의 연속 원소를 담는다는 뜻이고, push는 끝에 넣는다는 뜻은 길이가 하나 늘어난다는 뜻입니다. `Vec의 원소 추가에 불변 바인딩을 사용함`은 반대 사례인데, 바꿀 수 없는 바인딩에서는 push가 컴파일 단계에서 막히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `Vec의 원소 추가에 불변 바인딩을 사용함`입니다. 잘못된 코드는 실행은 되지만 `10`이라는 잘못된 출력을 냅니다. mut 선언부터 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `가변 벡터를 순회해 합산함`이라는 의미와 입력·출력 계약입니다. C의 크기 고정 배열 순회와 Python의 리스트 합으로 같은 동작을 구현하고, 실행 결과 `60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`iter()`로 합계를 구한 뒤에도 원래 Vec를 계속 쓸 수 있습니다. 빌려 읽기와 소유 이동을 구별하세요.',
    },
    30: {
        'notes': [
            '주소는 값이 저장된 메모리의 위치이고, 포인터는 그 위치를 보관하는 변수입니다. `int values[] = {10,20}; int *p = values;`에서는 `p`가 첫 번째 `int`를 가리키고 `*p`는 그곳의 값 10입니다. `p + 1`은 주소 숫자에 1바이트를 더하라는 뜻이 아니라 다음 int 원소의 위치로 이동하라는 뜻입니다. 따라서 `*(p + 1)`은 20입니다.',
            '`values[1]`과 `*(p + 1)`은 여기서 같은 원소를 읽습니다. `p[1]`도 같은 뜻입니다. 단, 길이가 2이면 `p + 2`는 끝 바로 뒤 위치라 주소 계산 자체는 가능해도 역참조해서 읽으면 안 됩니다. 포인터 이동은 같은 배열 안에서만 의미 있게 다뤄야 합니다. 주소가 실제로 몇 번지인지는 실행할 때마다 다를 수 있으니 주소 숫자를 외우지 말고 어느 원소를 가리키는가를 추적하세요.',
        ],
        'predict': '1. p가 values[0]을 가리킵니다. 2. p+1은 두 번째 원소를 가리킵니다. 3. 역참조해 20을 출력합니다. `p=&values[0] → *p=10 → p+1=&values[1] → *(p+1)=20 → 20 출력` 흐름으로 실제 출력 `20`이 됩니다. 핵심 `p + 1`은 다음 원소의 주소로 이동하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `20`을 출력한다. 바꾼 것은 0번 원소, 읽는 것은 1번 원소라 결과가 같습니다. 값이 실제로 달라진 것은 건드리지 않은 칸뿐이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'undefined-behavior',
        'debug': '틀린 줄은 `printf("%d\\n", *(p + 3));`입니다. `p + 3`은 길이 3 배열의 범위 밖 주소를 가리키므로, 역참조하는 순간 C의 정의되지 않은 동작이 됩니다. 특정 오답 숫자를 약속하지 않습니다. 우연히 어떤 값이 보일 수도 있고 비정상 종료될 수도 있으며, 어떤 결과도 보장할 수 없습니다. 오류 분류는 정의되지 않은 동작입니다. 고친 줄 `printf("%d\\n", *(p + 1));`에서는 같은 배열 안에서 다음 원소를 참조하는 동작이 지켜집니다. 포인터 산술은 바이트 1개가 아니라 `int` 원소 단위로 이동합니다. 정상 코드는 주소 보관→다음 원소 이동→역참조 출력의 순서로 `20`을 출력합니다.',
        'qout': '먼저 p가 values[0]의 주소를 보관합니다. 이어서 p+1이 두 번째 원소의 주소가 되고, 역참조해 20을 읽어 출력하므로 `20`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 유효 범위 접근이라 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'int 포인터 p+1은 다음 int 원소의 주소라는 뜻이고, 역참조는 그 주소에 저장된 값을 읽는다는 뜻은 주소 계산과 값 읽기가 분리된다는 뜻입니다. `배열 끝을 지난 주소를 역참조함`은 반대 사례인데, 유효 범위를 벗어나면 정의되지 않은 동작이 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `배열 끝을 지난 주소를 역참조함`입니다. 범위 밖 역참조는 정의되지 않은 동작이므로, 특정 값이 나온다고 약속하지 마세요. 같은 배열 안에서만 이동·역참조하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `배열 내부에서 다음 원소를 참조함`이라는 의미와 입력·출력 계약입니다. Python의 인덱스와 Rust의 범위 검사 참조로 같은 동작을 구현하고, 실행 결과 `20`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '길이가 2이면 `p + 2`는 끝 바로 뒤라 주소 계산은 가능해도 역참조해서 읽으면 안 됩니다. 주소 숫자를 외우지 말고 어느 원소를 가리키는가를 추적하세요.',
    },
    31: {
        'notes': [
            '`alias = source`는 두 이름을 같은 리스트 객체에 붙입니다. `alias.append(30)`을 하면 `source`로 보아도 30이 추가되어 있습니다. `copy = source.copy()`는 새 바깥 리스트를 만들므로 `copy.append(40)`은 원래 리스트의 바깥 목록을 바꾸지 않습니다.',
            '하지만 `[ [10] ]`처럼 안에 또 리스트가 있다면 얕은 복사는 안쪽 리스트를 공유합니다. `copy[0].append(20)`을 하면 원본의 안쪽 리스트에도 20이 보입니다. 복사하고 싶은 것이 바깥 목록인지 그 안의 값까지인지 먼저 묻고, 필요하면 `copy.deepcopy` 같은 깊은 복사를 고려하세요.',
        ],
        'predict': '1. source에 두 원소가 있습니다. 2. copy()가 다른 리스트를 만듭니다. 3. copy[0]만 99가 되어 source[0]은 10입니다. `source=[10,20] → copy=[10,20] → copy=[99,20] → source[0]=10` 흐름으로 실제 출력 `10`이 됩니다. 핵심 `source.copy()`는 별도의 바깥 목록을 만드는 자리에 쓰입니다.',
        'guided': '원본은 `10`, 수정본은 `11`이다. 첫 변경 줄 `source = [10, 20]`에서 `source = [11, 20]`으로 첫 값을 바꾸면, 복사본의 0번만 99가 되고 원본의 0번이 그대로 출력되어 11이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `copy = source`입니다. 새 리스트가 아니라 같은 객체에 새 이름만 붙으므로, 뒤의 대입이 원본까지 바꿔 `99`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 원본과 복사가 갈라지지 않는 첫 순간은 복사 줄입니다. 고친 줄 `copy = source.copy()`에서는 `원본과 새 리스트를 구분함` 동작이 지켜집니다. 정상 코드는 원본 생성→별도 복사→복사본만 수정→원본 읽기의 순서로 `10`을 출력합니다.',
        'qout': '먼저 source에 [10,20]이 저장됩니다. 이어서 copy()가 다른 리스트 [10,20]을 만들고, copy[0]만 99가 되므로 source[0]은 그대로 10이어서 출력 `10`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 대입과 메서드 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'copy()는 새 바깥 리스트를 만든다는 뜻이고, 단순 대입은 같은 리스트에 새 이름만 붙인다는 뜻은 이름은 둘이라도 객체는 하나라는 뜻입니다. `copy = source를 독립된 리스트 복사라고 생각함`은 반대 사례인데, 대입은 객체를 공유하므로 원본까지 바뀌기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `copy = source를 독립된 리스트 복사라고 생각함`입니다. 잘못된 코드는 실행은 되지만 `99`라는 잘못된 출력을 냅니다. 복사인지 별칭인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `원본과 새 리스트를 구분함`이라는 의미와 입력·출력 계약입니다. Rust의 clone과 C의 배열 복사로 같은 동작을 구현하고, 실행 결과 `10`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '안에 또 리스트가 있으면 얕은 복사는 안쪽을 공유합니다. 바깥 목록과 안쪽 값 중 무엇을 복사할지 먼저 물으세요.',
    },
    32: {
        'notes': [
            'Rust의 `String`은 글자를 담을 메모리의 소유자를 추적합니다. `let b = a;`처럼 대입하면 보통 소유권이 `a`에서 `b`로 이동하므로 그 뒤 `a`를 읽으면 컴파일 오류가 납니다. 서로 독립된 두 문자열이 필요할 때 `let b = a.clone()`으로 글자 데이터를 복제합니다.',
            '`clone()`은 비용이 들 수 있으니 읽기만 할 때는 `&a`로 빌리는 편이 자연스럽습니다. `i32` 같은 작은 복사 타입은 대입해도 원래 변수를 쓸 수 있는데, 이는 값 자체가 복사되기 때문입니다. 이동과 복사를 타입에 따라 구별하세요.',
        ],
        'predict': '1. original이 study를 소유합니다. 2. clone으로 별도 String을 만듭니다. 3. 두 바인딩 모두 유효해 출력합니다. `original 소유 → clone 별도 소유 → 둘 다 출력` 흐름으로 실제 출력 `study study`가 됩니다. 핵심 `clone()`은 내용을 새 메모리에 복제하는 자리에 쓰입니다.',
        'guided': '원본은 `study study`, 수정본은 `study! study!`이다. 첫 변경 줄 `let original = String::from("study");`에서 `let original = String::from("study!");`로 원본 값을 바꾸면, 복제본도 새 값을 받아 두 자리가 함께 바뀐다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `let copy = original.;`입니다. 마침표 뒤에 메서드 이름이 없어 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 완성되지 않은 메서드 호출입니다. 고친 줄 `let copy = original.clone();`에서는 `새 소유 복사본 생성` 동작이 지켜집니다. 정상 코드는 소유→별도 복제→둘 다 유효의 순서로 `study study`를 출력합니다.',
        'qout': '먼저 original이 study를 소유합니다. 이어서 clone으로 별도 String이 만들어져 두 바인딩이 모두 유효해지고, println!이 두 값을 표시하므로 출력 `study study`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 소유와 복제가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'clone은 내용을 새 메모리에 복제해 두 String이 각각 소유하게 한다는 뜻이고, 단순 대입은 소유권을 이동한다는 뜻은 이전 바인딩을 쓸 수 없게 된다는 뜻입니다. `String을 이동한 뒤 이전 변수를 다시 사용함`은 반대 사례인데, 이동 뒤 읽기는 컴파일 단계에서 막히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `String을 이동한 뒤 이전 변수를 다시 사용함`입니다. 잘못된 코드는 완성되지 않은 호출이라 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `새 소유 복사본 생성`이라는 의미와 입력·출력 계약입니다. C의 문자열 복사와 Python의 깊은 복사로 같은 동작을 구현하고, 실행 결과 `study study`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '읽기만 할 때는 `&a`로 빌리세요. `i32` 같은 복사 타입과 `String` 같은 이동 타입을 구별하세요.',
    },
    33: {
        'notes': [
            '`&topic`은 문자열을 다른 함수에 읽으라고 잠시 보여 주는 것입니다. 빌린 함수는 원래 `String`의 소유자가 되지 않으므로 함수가 끝난 뒤에도 호출자에서 `topic`을 쓸 수 있습니다. `&String`을 받는 함수는 소유권 이동 없이 값을 읽습니다.',
            '공유 참조 `&T`로 읽을 수는 있어도 원래 값을 마음대로 바꿀 수 없습니다. 바꾸려면 소유자가 변경 가능해야 하고 `&mut T`로 배타적으로 빌려야 합니다. 누가 소유하는가, 누가 잠시 읽는가를 코드 옆에 적어 보세요.',
        ],
        'predict': '1. topic이 Rust를 소유합니다. 2. length가 주소를 읽기 전용으로 빌립니다. 3. 길이 4와 원본 Rust를 출력합니다. `topic 소유 → &topic 빌림 → 길이 4 → topic 여전히 유효` 흐름으로 실제 출력 `4 Rust`가 됩니다. 핵심 `&topic`은 소유권 없이 읽기를 전달하는 자리에 쓰입니다.',
        'guided': '원본은 `4 Rust`, 수정본은 `5 Rust!`이다. 첫 변경 줄 `let topic = String::from("Rust");`에서 `let topic = String::from("Rust!");`로 값을 바꾸면, 길이 계산과 뒤의 표시가 함께 바뀌어 두 자리가 달라진다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `println!("{} {topic}", length(topic.clone()));`입니다. 복제본을 만들어 빌려주므로 이번 입력에서는 화면 출력이 같습니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. 빌림으로 충분한 곳에 복제를 쓰면 비용이 들고, 참조를 값 이동과 똑같이 여기는 오해를 굳힙니다. 고친 줄의 `&topic`에서는 `소유권 이동 없이 읽기 전달` 동작이 지켜집니다. 정상 코드는 소유→공유 빌림→길이 계산→원본 표시의 순서로 `4 Rust`를 출력합니다.',
        'qout': '먼저 topic이 Rust를 소유합니다. 이어서 length가 &topic을 읽기 전용으로 빌려 길이 4를 구하고, 뒤에 원본 Rust를 표시하므로 출력 `4 Rust`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 빌림과 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': '`&String`은 소유권을 가져가지 않는 공유 참조라는 뜻이고, 호출이 끝나도 topic의 소유자는 main이라는 뜻은 빌림 뒤에도 원본을 쓸 수 있다는 뜻입니다. `참조를 값 이동과 똑같이 생각해 원본을 쓰지 못한다고 판단함`은 반대 사례인데, 공유 빌림은 원본 사용을 막지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `참조를 값 이동과 똑같이 생각해 원본을 쓰지 못한다고 판단함`입니다. 잘못된 코드는 이번 입력에서는 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 불필요한 복제라는 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `소유권 이동 없이 읽기 전달`이라는 의미와 입력·출력 계약입니다. C의 const 포인터 읽기 전달과 Python의 읽기 전용 전달로 같은 동작을 구현하고, 실행 결과 `4 Rust`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '바꾸려면 소유자가 변경 가능해야 하고 `&mut T`로 배타적으로 빌려야 합니다. 읽기와 쓰기 빌림을 구별하세요.',
    },
    34: {
        'notes': [
            '`sum(const int *a, size_t n)` 같은 함수에 배열을 넘기면 첫 원소를 가리키는 포인터가 전달됩니다. 함수 안에서 포인터만 보고 전체 배열의 길이를 자동으로 알아낼 수 없으니 호출자가 `n`도 함께 줘야 합니다. `n=3`이면 접근 가능한 원소 번호는 0, 1, 2입니다.',
            '`i < n`은 마지막 번호 2까지 방문한 뒤 멈추지만 `i <= n`은 범위 밖 `a[3]`까지 시도합니다. `const int *`라면 그 포인터를 통해 원소를 수정하지 않겠다는 뜻입니다. 길이 0인 배열도 다룰 수 있게 합계의 시작값과 반복 종료를 확인하세요.',
        ],
        'predict': '1. a의 첫 주소와 길이 3을 전달합니다. 2. i=0,1,2에서 2+4+6을 누적합니다. 3. i=3에서 멈추고 12를 반환합니다. `i=0 out=2 → i=1 out=6 → i=2 out=12` 흐름으로 실제 출력 `12`가 됩니다. 핵심 `i<n`은 유효 번호 0부터 n-1까지만 방문하게 하는 자리에 쓰입니다.',
        'guided': '원본은 `12`, 수정본은 `13`이다. 첫 변경 줄의 시작값 `out=0`에서 `out=1`로 바꾸면, 이후 세 번의 누적에 1이 더해져 최종 합이 13이 된다.',
        'debug_outcome': 'undefined-behavior',
        'debug': '틀린 줄은 `int sum(const int *a, size_t n) { int out=0; for (size_t i=0;i<=n;i++) out+=a[i]; return out; }`입니다. 마지막 반복에서 `a[3]`을 읽으므로 C의 정의되지 않은 동작이 됩니다. 합계가 특정 숫자가 된다고 단정하지 않습니다. 어떤 값이 보일 수도 있고 비정상 종료될 수도 있으며, 어떤 결과도 보장할 수 없습니다. 오류 분류는 정의되지 않은 동작입니다. 반복표를 보면 i=0,1,2는 유효하고 i=3은 범위 밖입니다. 고친 줄의 `i<n`에서는 `배열과 길이를 함께 전달함` 동작이 지켜집니다. 정상 코드는 주소·길이 전달→0,1,2 누적→12 반환의 순서로 `12`를 출력합니다.',
        'qout': '먼저 a의 첫 주소와 길이 3이 전달됩니다. 이어서 i=0,1,2에서 2+4+6을 누적해 out=12가 되고, 반환값을 출력하므로 `12`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 유효 범위 접근이라 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'a는 첫 원소를 가리킨다는 뜻이고, n은 읽을 수 있는 원소 개수라는 뜻은 쓸 수 있는 번호가 0부터 n-1까지라는 뜻입니다. `배열 끝 인덱스를 n으로 착각해 한 칸 넘겨 읽음`은 반대 사례인데, n번은 범위 밖이라 정의되지 않은 동작이 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `배열 끝 인덱스를 n으로 착각해 한 칸 넘겨 읽음`입니다. 범위 밖 읽기는 정의되지 않은 동작이므로, 합계가 특정 숫자가 된다고 약속하지 마세요. i=0,1,2 유효, i=3 범위 밖 표를 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `배열과 길이를 함께 전달함`이라는 의미와 입력·출력 계약입니다. Python의 길이 내장과 Rust의 슬라이스 길이로 같은 동작을 구현하고, 실행 결과 `12`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '길이 0인 배열도 다룰 수 있게 합계의 시작값과 반복 종료를 확인하세요. `const int *`로는 원소를 수정하지 않겠다는 뜻입니다.',
    },
    35: {
        'notes': [
            'C의 포인터, Python의 별칭, Rust의 참조를 모두 같은 데이터를 둘 이상의 경로에서 본다는 질문으로 연결해 보세요. Python에서 `alias = items` 뒤 `alias.append(30)`을 하면 `items`도 바뀝니다. 이는 이름 두 개가 한 리스트 객체를 가리키기 때문입니다.',
            '`items.copy()`를 쓰면 바깥 리스트는 따로 생기지만 안에 든 가변 객체는 여전히 공유할 수 있습니다. C에서는 포인터가 유효한 메모리인지 개발자가 살펴야 하고, Rust는 빌림 규칙으로 일부 잘못된 공유를 컴파일 때 막습니다. 세 언어가 모두 같은 값이 보인다는 것과 메모리 안전 규칙이 같다는 뜻은 아닙니다.',
        ],
        'predict': '1. items가 리스트를 가리킵니다. 2. alias가 동일 객체를 가리킵니다. 3. append가 그 객체를 변경해 items에서도 3이 보입니다. `items→리스트 → alias→같은 리스트 → append → [1,2,3]` 흐름으로 실제 출력 `[1, 2, 3]`이 됩니다. 핵심 `alias = items`는 새 객체가 아니라 같은 객체를 가리키는 자리에 쓰입니다.',
        'guided': '원본은 `[1, 2, 3]`, 수정본은 `[2, 2, 3]`이다. 첫 변경 줄 `items = [1, 2]`에서 `items = [2, 2]`로 첫 값을 바꾸면, 별칭을 통한 추가 뒤에도 첫 자리는 바뀐 값 그대로 남는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `alias = items.copy()`입니다. 별도의 바깥 리스트가 생겨 뒤의 추가가 원본에 닿지 않으므로 `items`는 `[1, 2]` 그대로 출력됩니다. 오류 분류는 잘못된 출력입니다. 공유가 끊기는 첫 순간은 복사 줄입니다. 고친 줄 `alias = items`에서는 `공유 참조와 복사를 구별함` 동작이 지켜집니다. 정상 코드는 원본 생성→별칭 공유→공유 객체 변경→원본 읽기의 순서로 `[1, 2, 3]`을 출력합니다.',
        'qout': '먼저 items가 [1,2] 리스트를 가리킵니다. 이어서 alias가 같은 객체를 가리키고, append가 그 객체를 바꿔 items에서도 3이 보이므로 출력 `[1, 2, 3]`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 별칭과 메서드 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '두 이름이 같은 가변 리스트를 가리키면 어느 이름을 통해 바꿔도 같은 객체가 수정된다는 뜻은, 이름은 둘이라도 객체는 하나라는 뜻입니다. `새 이름을 붙이면 무조건 별도의 객체가 생긴다고 생각함`은 반대 사례인데, 대입은 객체를 공유하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `새 이름을 붙이면 무조건 별도의 객체가 생긴다고 생각함`입니다. 잘못된 코드는 실행은 되지만 `[1, 2]`라는 잘못된 출력을 냅니다. 별칭인지 복사인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `공유 참조와 복사를 구별함`이라는 의미와 입력·출력 계약입니다. C의 포인터 공유와 Rust의 참조·복제로 같은 동작을 구현하고, 실행 결과 `[1, 2, 3]`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`items.copy()`는 바깥만 따로 만들고 안의 가변 객체는 공유할 수 있습니다. 무엇을 복사할지 먼저 물으세요.',
    },
    36: {
        'notes': [
            '슬라이스는 배열이나 Vec의 원소 일부를 소유권 없이 잠시 보는 창입니다. `[10,20,30,40]`에서 `[1..3]`은 1번을 포함하고 3번은 제외하므로 20과 30입니다. `[0..0]`은 아무 원소도 없는 빈 슬라이스입니다.',
            '슬라이스의 끝은 길이까지 지정할 수 있습니다. 길이 4에서 `[1..4]`는 20,30,40이지만 `[1..5]`는 범위 밖입니다. 슬라이스를 빌려 쓰는 동안 원래 Vec를 재할당할 수 있는 `push` 같은 수정은 제한됩니다. 창이 바라보는 장소가 중간에 바뀌지 않게 하려는 규칙입니다.',
        ],
        'predict': '1. 원본 배열을 저장합니다. 2. 인덱스 1과 2를 빌립니다. 3. middle[0],middle[1]이 20,30입니다. `[10,20,30,40] → [20,30] 빌림 → 20 30` 흐름으로 실제 출력 `20 30`이 됩니다. 핵심 `1..3`은 시작을 포함하고 끝을 제외하는 범위를 정하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `20 30`을 출력한다. 슬라이스 1..3은 0번 원소를 포함하지 않아 결과가 같습니다. 값이 실제로 달라진 것은 빌림 밖뿐이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `let middle = &values[1..4];`입니다. 빌림 범위가 1·2·3번으로 넓어졌지만, 출력하는 middle[0]과 middle[1]은 그대로 20과 30이므로 이번 화면 출력은 같습니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. `[1..5]`처럼 길이 밖으로 넓히면 실행 때 패닉이 나므로, 범위를 정확히 적어야 합니다. 고친 줄의 `1..3`에서는 `복사 없이 부분 범위를 빌림` 동작이 지켜집니다. 정상 코드는 저장→1·2번 빌림→두 값 출력의 순서로 `20 30`을 출력합니다.',
        'qout': '먼저 [10,20,30,40] 배열이 저장됩니다. 이어서 1·2번을 빌려 middle이 [20,30] 창이 되고, 두 값을 출력하므로 `20 30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 범위 검사가 통과해 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': '슬라이스 [1..3]은 시작 1을 포함하고 끝 3은 제외한다는 뜻은, 빌리는 원소가 1번과 2번뿐이라는 뜻입니다. `끝 인덱스 3도 포함한다고 생각함`은 반대 사례인데, 끝은 제외되므로 3번은 창 밖에 있기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `끝 인덱스 3도 포함한다고 생각함`입니다. 잘못된 코드는 이번 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 빌림 범위가 넓어진 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `복사 없이 부분 범위를 빌림`이라는 의미와 입력·출력 계약입니다. C의 포인터·길이 쌍과 Python의 슬라이스로 같은 동작을 구현하고, 실행 결과 `20 30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`[0..0]`은 빈 슬라이스이고 `[1..5]`는 길이 밖이라 실행 때 패닉이 납니다. 끝 포함 여부를 먼저 확인하세요.',
    },
    37: {
        'notes': [
            'C 문자열 `char word[] = "cat"`은 네 바이트입니다. 문자 `c`, `a`, `t`와 마지막 종료 문자 `\\0`입니다. `strlen(word)`은 처음 `\\0`을 만날 때까지 세므로 결과는 3입니다. 문자 세 개와 저장 공간 네 칸을 구분해야 합니다.',
            '직접 문자를 채울 때 종료 문자 `\\0`을 잊으면 문자열 함수가 배열 밖까지 읽을 수 있습니다. 길이 3의 배열에 `"cat"`을 담으려 하면 종료 칸이 부족합니다. 화면에 보이는 글자 수만 보고 메모리 크기를 정하지 마세요. 한글의 UTF-8 바이트 수는 영문 글자 수와 다를 수도 있습니다.',
        ],
        'predict': '1. 배열 크기는 널 포함 4입니다. 2. strlen이 널까지 전진합니다. 3. 널 이전 길이 3을 출력합니다. `c a t 종료문자 → strlen=3 → 출력` 흐름으로 실제 출력 `3`이 됩니다. 핵심 `strlen(word)`는 종료 문자를 만나기 전까지 세는 자리에 쓰입니다.',
        'guided': '원본은 `3`, 수정본은 `4`이다. 첫 변경 줄 `char word[] = "cat";`에서 `char word[] = "cat!";`로 글자를 하나 더하면, 종료 문자 앞 글자가 네 개가 되어 길이가 4가 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `printf("%zu\\n", sizeof(word));`입니다. `sizeof`는 종료 칸을 포함한 저장 공간 4를 재므로 실행은 되지만 결과는 `4`가 됩니다. 오류 분류는 잘못된 출력입니다. 글자 수와 저장 크기가 바뀌는 첫 순간은 측정 연산자 선택입니다. 고친 줄 `printf("%zu\\n", strlen(word));`에서는 `널 종료된 문자열 길이 계산` 동작이 지켜집니다. 정상 코드는 4바이트 저장→널 이전 3글자 세기→출력의 순서로 `3`을 출력합니다.',
        'qout': '먼저 word에 c, a, t와 종료 문자의 네 바이트가 저장됩니다. 이어서 strlen이 종료 문자 전까지 세어 3을 얻고, printf가 표시하므로 출력 `3`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 문자열이 널로 끝나 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'word에는 c,a,t와 종료 문자의 네 바이트가 들어 있다는 뜻이고, strlen은 널 이전의 글자 세 개만 셈다는 뜻은 저장 공간과 글자 수를 구별한다는 뜻입니다. `널 종료가 없는 버퍼를 strlen에 넘겨 범위 밖을 읽음`은 반대 사례인데, 종료 문자가 없으면 strlen이 배열 밖까지 읽기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `널 종료가 없는 버퍼를 strlen에 넘겨 범위 밖을 읽음`입니다. 잘못된 코드는 실행은 되지만 `4`라는 잘못된 출력을 냅니다. 글자 수와 저장 크기를 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `널 종료된 문자열 길이 계산`이라는 의미와 입력·출력 계약입니다. Python의 len과 Rust의 len으로 같은 동작을 구현하고, 실행 결과 `3`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '길이 3의 배열에 `"cat"`을 담으면 종료 칸이 부족합니다. 한글의 UTF-8 바이트 수는 영문 글자 수와 다를 수 있습니다.',
    },
    38: {
        'notes': [
            'Python 문자열은 유니코드 텍스트이고, `encode("utf-8")`의 결과는 파일이나 네트워크로 보낼 수 있는 바이트입니다. `len("가")`는 보통 코드 포인트 하나를 세지만 `len("가".encode("utf-8"))`은 UTF-8 바이트 세 개를 셉니다.',
            '보이는 글자 하나가 언제나 코드 포인트 하나인 것도 아닙니다. 결합 문자나 이모지 묶음은 사람이 보는 글자 수와 `len` 결과가 다를 수 있습니다. 문자열 길이를 구할 때 텍스트의 코드 포인트 수인지 저장할 바이트 수인지 목적을 먼저 정하세요.',
        ],
        'predict': '1. text에는 한·글 두 글자가 있습니다. 2. UTF-8로 각 글자를 3바이트로 인코딩합니다. 3. 2와 6을 출력합니다. `두 글자 → UTF-8 6바이트 → 2 6` 흐름으로 실제 출력 `2 6`이 됩니다. 핵심 `encode("utf-8")`은 텍스트를 바이트로 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `2 6`, 수정본은 `3 7`이다. 첫 변경 줄 `text = "한글"`에서 `text = "한글!"`로 글자를 더하면, 코드 포인트 수와 UTF-8 바이트 수가 각각 하나씩 늘어 3과 7이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(len(text), len(text.upper()))`입니다. 대문자로 바꾼 글자 수는 그대로 2이므로 실행은 되지만 결과는 `2 2`가 됩니다. 오류 분류는 잘못된 출력입니다. 바이트 수를 구해야 할 자리에 글자 수 세기를 둔 첫 순간이 원인입니다. 고친 줄 `print(len(text), len(text.encode("utf-8")))`에서는 `문자 수와 인코딩 바이트 수 구분` 동작이 지켜집니다. 정상 코드는 저장→코드 포인트 세기→바이트 세기의 순서로 `2 6`을 출력합니다.',
        'qout': '먼저 text에 한·글 두 글자가 저장됩니다. 이어서 len이 코드 포인트 2를 세고, UTF-8 인코딩의 바이트 6을 세어 출력하므로 `2 6`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 문자열 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'Python str의 len은 코드 포인트 수라는 뜻이고, UTF-8로 인코딩한 bytes 길이는 별개라는 뜻은 목적에 따라 재는 자가 다르다는 뜻입니다. `한글 한 글자를 항상 한 바이트라고 가정함`은 반대 사례인데, 한글 한 글자는 UTF-8에서 3바이트이기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `한글 한 글자를 항상 한 바이트라고 가정함`입니다. 잘못된 코드는 실행은 되지만 `2 2`라는 잘못된 출력을 냅니다. 코드 포인트 수와 바이트 수를 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `문자 수와 인코딩 바이트 수 구분`이라는 의미와 입력·출력 계약입니다. C의 바이트 길이와 Rust의 len·as_bytes로 같은 동작을 구현하고, 실행 결과 `2 6`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '결합 문자나 이모지 묶음은 보이는 글자 수와 `len` 결과가 다를 수 있습니다. 목적이 코드 포인트 수인지 바이트 수인지 먼저 정하세요.',
    },
    39: {
        'notes': [
            '`String`은 문자열 데이터를 소유하며 필요하면 `push_str`로 뒤에 글자를 붙일 수 있습니다. `&str`은 문자열 내용을 잠시 빌려 보는 구간입니다. 글자 리터럴 `"hi"`도 `&str`이고, 소유 중인 `String`에서 `as_str()`로 빌릴 수도 있습니다.',
            '`let mut s = String::from("안"); s.push_str("녕");`이면 소유 중인 데이터가 안녕으로 바뀝니다. UTF-8 문자 하나가 한 바이트라는 보장은 없으므로 바이트 인덱스 하나로 문자열의 n번째 글자를 읽으려 하면 안 됩니다. 바이트 길이와 글자 순회를 구분하세요.',
        ],
        'predict': '1. topic이 C를 소유합니다. 2. push_str이 공백과 Rust를 덧붙입니다. 3. view로 전체를 빌려 출력합니다. `topic=C → topic=C Rust → view=C Rust` 흐름으로 실제 출력 `C Rust`가 됩니다. 핵심 `push_str`은 소유 중인 데이터 뒤에 글자를 붙이는 자리에 쓰입니다.',
        'guided': '원본은 `C Rust`, 수정본은 `C! Rust`이다. 첫 변경 줄 `let mut topic = String::from("C");`에서 `let mut topic = String::from("C!");`로 시작 값을 바꾸면, 덧붙이기와 빌려 보기가 그대로 이어져 두 자리 출력이 함께 바뀐다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `topic.push(" Rust");`입니다. `push`는 글자 하나를 받는 메서드이므로 문자열을 직접 넘긴 이 줄에서 컴파일 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 메서드 선택입니다. 고친 줄 `topic.push_str(" Rust");`에서는 `소유 문자열을 수정하고 읽기 전용 뷰 사용` 동작이 지켜집니다. 정상 코드는 소유→뒤에 붙이기→빌려 보기→출력의 순서로 `C Rust`를 출력합니다.',
        'qout': '먼저 topic이 C를 소유합니다. 이어서 push_str이 공백과 Rust를 덧붙여 C Rust가 되고, view가 전체를 빌려 표시하므로 출력 `C Rust`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 소유와 메서드 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'String은 가변 힙 데이터를 소유한다는 뜻이고, &str은 그 문자열의 유효 구간을 빌린다는 뜻은 내용 복제 없이 잠시 본다는 뜻입니다. `&str 참조로 원본의 크기를 바꿀 수 있다고 생각함`은 반대 사례인데, 읽기 전용 구간으로는 크기를 바꿀 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `&str 참조로 원본의 크기를 바꿀 수 있다고 생각함`입니다. 잘못된 코드는 메서드 선택이 어긋나 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `소유 문자열을 수정하고 읽기 전용 뷰 사용`이라는 의미와 입력·출력 계약입니다. C의 버퍼 이어 붙이기와 Python의 문자열 연결로 같은 동작을 구현하고, 실행 결과 `C Rust`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'UTF-8 문자 하나가 한 바이트라는 보장은 없습니다. 바이트 인덱스로 n번째 글자를 읽으려 하지 마세요.',
    },
    40: {
        'notes': [
            '`malloc(sizeof *value)`는 값을 담을 공간을 요청하고 성공하면 그 공간의 주소, 실패하면 `NULL`을 돌려줍니다. `NULL`이면 역참조할 수 없으므로 먼저 검사해야 합니다. 성공한 뒤 `*value = 30`은 새 공간에 정수 30을 씁니다.',
            '`free(value)`를 호출한 뒤 그 공간은 더 이상 내 것이 아닙니다. 포인터 변수에 예전 주소 비트가 남아 있더라도 `*value`로 다시 읽거나 두 번 `free`하면 잘못됩니다. 할당 한 번에 해제 한 번을 대응시키고, 해제한 뒤에는 사용 경로를 닫으세요. `sizeof *value`는 포인터가 가리키는 타입 크기를 구하므로 타입 이름을 중복해서 적지 않아도 됩니다.',
        ],
        'predict': '1. int 크기만큼 할당합니다. 2. NULL 검사를 거쳐 42를 저장하고 출력합니다. 3. 사용을 마친 뒤 free합니다. `malloc → NULL 검사 → 저장·출력 → free` 흐름으로 실제 출력 `42`가 됩니다. 핵심 `free(value)`는 사용을 마친 공간을 돌려주는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `42`를 출력한다. 바꾼 return 2는 malloc 실패 갈래에 있어 성공 흐름에서는 실행되지 않습니다. 성공 흐름의 값은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `free(NULL);`입니다. `free(NULL)` 자체는 아무 일도 하지 않는 안전한 호출이지만, 실제 할당 포인터 `value`를 해제하지 않으므로 메모리 누수가 생깁니다. `printf`가 free보다 앞에 있어 화면에는 여전히 `42`가 출력될 수 있습니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. `42가 나오지 않는다`고 쓰면 안 됩니다. 고친 줄 `free(value);`에서는 `동적 메모리 수명 관리` 동작이 지켜집니다. 정상 코드는 할당→검사→저장·출력→해제의 순서로 `42`를 출력합니다.',
        'qout': '먼저 int 크기 공간이 할당됩니다. 이어서 NULL 검사를 통과해 42를 저장하고 출력한 뒤, 사용을 마치고 free하므로 `42`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 할당과 검사가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'malloc은 힙 공간의 주소 또는 NULL을 돌려준다는 뜻이고, free 후에는 해당 주소를 다시 역참조할 수 없다는 뜻은 해제 뒤 사용 경로를 닫아야 한다는 뜻입니다. `free 뒤 포인터를 다시 역참조하거나 해제를 빼먹음`은 반대 사례인데, 해제 뒤 사용은 정의되지 않은 동작이고 해제를 빼먹으면 누수가 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `free 뒤 포인터를 다시 역참조하거나 해제를 빼먹음`입니다. 이번 잘못된 코드는 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 실제 할당을 해제하지 않은 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `동적 메모리 수명 관리`라는 의미와 입력·출력 계약입니다. Rust의 소유권 자동 해제와 Python의 가비지 컬렉션으로 같은 동작을 구현하고, 실행 결과 `42`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '할당 한 번에 해제 한 번을 대응시키세요. 해제한 뒤 `*value`로 다시 읽으면 정의되지 않은 동작입니다.',
    },
    41: {
        'notes': [
            '`Box::new(30)`은 정수 30을 힙에 만들고 그 값을 소유하는 `Box<i32>`를 돌려줍니다. `*boxed`는 상자 안 값을 읽는 역참조입니다. 변수가 범위를 벗어나면 Box가 관리하는 힙 메모리가 해제됩니다.',
            '앞 Day의 C `malloc`·`free`와 비교하면 힙에 놓는다는 점은 비슷하지만 해제 책임을 표현하는 방법이 다릅니다. Box를 다른 변수로 이동시키면 기존 이름은 더 이상 소유자가 아닙니다. `Box<T>`를 아무 데나 붙이는 것이 성능 향상 공식은 아니며, 값 크기나 재귀 타입 표현 같은 필요가 있을 때 씁니다.',
        ],
        'predict': '1. Box가 40을 힙에 저장합니다. 2. 역참조로 40을 얻어 2를 더합니다. 3. 42를 출력하고 scope 끝에서 해제합니다. `Box(40) → 역참조 40 → 42 → Drop` 흐름으로 실제 출력 `42`가 됩니다. 핵심 `*boxed`는 상자 안 값을 읽는 자리에 쓰입니다.',
        'guided': '원본은 `42`, 수정본은 `43`이다. 첫 변경 줄 `let boxed = Box::new(40);`에서 `let boxed = Box::new(41);`로 힙 값을 바꾸면, 역참조 뒤 덧셈을 거쳐 43이 된다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `let answer = boxed + 2;`입니다. 상자 자체에 정수를 더하는 연산은 정의되어 있지 않으므로 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 역참조 없는 덧셈입니다. 고친 줄 `let answer = *boxed + 2;`에서는 `힙 값의 안전한 소유` 동작이 지켜집니다. 정상 코드는 힙 저장→역참조 읽기→덧셈→출력의 순서로 `42`를 출력합니다.',
        'qout': '먼저 Box가 40을 힙에 저장합니다. 이어서 역참조로 40을 얻어 2를 더한 42가 되고, println!이 표시하므로 출력 `42`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 소유와 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': '`Box<T>`는 힙 값의 유일한 소유자라는 뜻이고, `*`는 안쪽 값을 읽는다는 뜻은 상자를 열어 값을 꺼낸다는 뜻입니다. `Box를 C의 raw pointer처럼 직접 free해야 한다고 생각함`은 반대 사례인데, Box는 범위를 벗어나면 자동으로 해제되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `Box를 C의 raw pointer처럼 직접 free해야 한다고 생각함`입니다. 잘못된 코드는 역참조 없는 연산이라 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `힙 값의 안전한 소유`라는 의미와 입력·출력 계약입니다. C의 malloc·free 쌍과 Python의 객체 참조로 같은 동작을 구현하고, 실행 결과 `42`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`Box<T>`를 아무 데나 붙이는 것이 성능 향상 공식은 아닙니다. 값 크기나 재귀 타입 표현 같은 필요가 있을 때 쓰세요.',
    },
    42: {
        'notes': [
            '메모리 학습에서 먼저 물을 것은 어느 이름이 어떤 데이터를 가리키는가입니다. `b = a.copy()` 뒤 `b.append(30)`은 바깥 리스트가 달라서 `a`를 바꾸지 않습니다. `b = a`였다면 두 이름이 같은 리스트를 보므로 `a`에서도 추가한 값이 보입니다.',
            '한 줄씩 실행하며 `a → 리스트1`, `b → 리스트2`처럼 종이에 그려 보세요. C 포인터는 유효한 주소를 직접 다뤄야 하고, Rust의 소유권은 유효하지 않은 참조 사용을 막는 규칙을 제공합니다. 같은 참조라는 말을 들어도 언어마다 허용되는 동작이 다릅니다.',
        ],
        'predict': '1. a=[1]입니다. 2. copy로 b=[1]을 새로 만듭니다. 3. b만 [1,2]가 되어 함께 출력합니다. `a=[1] → b=[1] 별도 객체 → b=[1,2]` 흐름으로 실제 출력 `[1] [1, 2]`가 됩니다. 핵심 `a.copy()`는 별도의 바깥 목록을 만드는 자리에 쓰입니다.',
        'guided': '원본은 `[1] [1, 2]`, 수정본은 `[2] [2, 2]`이다. 첫 변경 줄 `a = [1]`에서 `a = [2]`로 시작 값을 바꾸면, 복사본의 추가 뒤에도 원본 첫 값은 바뀐 값 그대로 남는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `b = a`입니다. 같은 리스트를 보는 별칭이 되므로 뒤의 추가가 원본까지 바꿔 `[1, 2] [1, 2]`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 공유가 시작되는 첫 순간은 대입 줄입니다. 고친 줄 `b = a.copy()`에서는 `복사 범위와 공유 상태 확인` 동작이 지켜집니다. 정상 코드는 원본 생성→별도 복사→복사본만 추가→둘 다 읽기의 순서로 `[1] [1, 2]`를 출력합니다.',
        'qout': '먼저 a가 [1] 리스트를 가리킵니다. 이어서 copy로 별도 [1]이 생기고, b에만 2가 추가되어 a는 [1], b는 [1,2]이므로 출력 `[1] [1, 2]`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 별칭과 메서드 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않는다는 뜻은, 두 이름이 다른 객체를 본다는 뜻입니다. `얕은 복사를 중첩 리스트의 깊은 복사로 착각함`은 반대 사례인데, 얕은 복사는 안쪽 가변 객체를 공유하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `얕은 복사를 중첩 리스트의 깊은 복사로 착각함`입니다. 잘못된 코드는 실행은 되지만 `[1, 2] [1, 2]`라는 잘못된 출력을 냅니다. 중첩 리스트는 안쪽 공유까지 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `복사 범위와 공유 상태 확인`이라는 의미와 입력·출력 계약입니다. C의 메모리 복사와 Rust의 clone 범위로 같은 동작을 구현하고, 실행 결과 `[1] [1, 2]`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'C 포인터는 유효 주소를 직접 다뤄야 합니다. 같은 참조라는 말과 메모리 안전 규칙이 같다는 뜻은 다릅니다.',
    },
    43: {
        'notes': [
            'C 함수는 정수 매개변수에 값을 복사해서 받으므로 함수 안에서 값을 바꿔도 호출자의 정수는 바뀌지 않습니다. 바깥 변수를 바꾸려면 `update(&n)`처럼 주소를 넘기고, 함수에서 `*out = 30`처럼 그 주소의 값을 수정합니다. `out`은 주소이고 `*out`은 호출자의 실제 값입니다.',
            '함수에서 `out` 자체를 다른 곳으로 바꾸면 호출자의 값이 바뀌는 것과 다릅니다. 주소 인수가 `NULL`일 수도 있는 함수라면 역참조 전에 검사해야 합니다. 반환값으로 결과를 돌려주는 방법과 주소로 결과를 기록하는 방법을 구별하세요.',
        ],
        'predict': '1. main이 &n을 전달합니다. 2. 함수가 NULL을 확인합니다. 3. *out으로 n=12를 만들고 성공을 반환합니다. `n=0 → &n 전달 → *out=12 → n 출력` 흐름으로 실제 출력 `12`가 됩니다. 핵심 `*out =`은 호출자의 변수 값을 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `12`, 수정본은 `18`이다. 첫 변경 줄 `*out = input * 2; return 1;`에서 `*out = input * 3; return 1;`로 계산을 바꾸면, 호출자의 n에 18이 기록된 뒤 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `out = input * 2; return 1;`입니다. 포인터 변수에 정수를 직접 대입하려 하므로 컴파일 단계에서 타입 경고를 냅니다. 그대로 실행하면 호출자의 `n`은 바뀌지 않아 `0`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 흐름이 깨지는 첫 순간은 역참조 없는 대입입니다. 고친 줄 `*out = input * 2; return 1;`에서는 `출력 인자로 호출자 값을 수정함` 동작이 지켜집니다. 정상 코드는 주소 전달→NULL 검사→역참조 기록→성공 반환의 순서로 `12`를 출력합니다.',
        'qout': '먼저 main에서 n=0과 주소 &n이 준비됩니다. 이어서 함수가 NULL 검사를 통과해 *out에 12를 기록하고 성공을 반환하면, printf가 n을 표시하므로 출력 `12`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 주소 전달과 검사가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'out은 호출자 n의 주소라는 뜻이고, *out에 대입해야 호출자 변수의 값이 바뀐다는 뜻은 주소가 아니라 그 주소의 값을 바꿔야 한다는 뜻입니다. `out 자체에 대입하고 호출자 변수도 바뀌었다고 생각함`은 반대 사례인데, 포인터 변수 자체를 바꾸는 것과 가리키는 값을 바꾸는 것은 다르기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `out 자체에 대입하고 호출자 변수도 바뀌었다고 생각함`입니다. 잘못된 코드는 타입 경고가 나고 `0`이라는 잘못된 출력을 냅니다. 역참조 없는 대입을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `출력 인자로 호출자 값을 수정함`이라는 의미와 입력·출력 계약입니다. Python의 가변 객체 전달과 Rust의 가변 빌림으로 같은 동작을 구현하고, 실행 결과 `12`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '주소 인수가 `NULL`일 수도 있는 함수라면 역참조 전에 검사해야 합니다. 반환값 전달과 주소 기록을 구별하세요.',
    },
    44: {
        'notes': [
            'Python 함수에 리스트를 넘기면 함수 매개변수가 호출자와 같은 리스트를 참조할 수 있습니다. `items.append(30)`은 그 리스트 자체를 변경해서 호출자 `values`에도 30이 보입니다. 반면 함수 안에서 `items = [30]`은 지역 이름을 새 리스트에 붙이므로 원래 `values` 자체를 바꾸지 않습니다.',
            '이 차이를 `append` 실행 직후와 재할당 직후로 나눠 손으로 그려 보세요. 원래 입력을 보존해야 한다면 함수 안에서 `items.copy()`를 사용해 별도의 바깥 리스트를 만들고 반환할 수 있습니다. 안에 다른 리스트가 들어 있다면 얕은 복사의 한계도 기억하세요.',
        ],
        'predict': '1. values가 [10]을 참조합니다. 2. items가 같은 객체를 가리킵니다. 3. append 뒤 [10,30]이 됩니다. `values→[10] → items→같은 객체 → append → [10,30]` 흐름으로 실제 출력 `[10, 30]`이 됩니다. 핵심 `items.append(30)`은 공유 객체 자체를 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `[10, 30]`, 수정본은 `[10, 31]`이다. 첫 변경 줄 `items.append(30)`에서 `items.append(31)`로 추가 값을 바꾸면, 공유 객체에 31이 들어가 호출자 리스트가 그대로 보인다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `items = [30]`입니다. 지역 이름이 새 리스트를 가리킬 뿐 원래 공유 객체는 그대로이므로, 호출자 `values`는 `[10]` 그대로 출력됩니다. 오류 분류는 잘못된 출력입니다. 공유가 끊기는 첫 순간은 재할당 줄입니다. 고친 줄 `items.append(30)`에서는 `가변 객체의 공유 부작용 구분` 동작이 지켜집니다. 정상 코드는 공유 참조→객체 변경→호출자 읽기의 순서로 `[10, 30]`을 출력합니다.',
        'qout': '먼저 values가 [10]을 참조합니다. 이어서 items가 같은 객체를 가리키고 append가 그 객체를 바꿔 [10,30]이 되므로, print가 호출자 리스트를 읽어 출력 `[10, 30]`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 참조와 메서드 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '함수 매개변수 items와 호출자 values가 같은 가변 리스트를 참조한다는 뜻은, 이름은 둘이라도 객체는 하나라는 뜻이고, append는 객체를 바꾼다는 뜻은 호출자 쪽에서도 보인다는 뜻입니다. `매개변수 대입과 객체 내부 수정을 같은 동작으로 혼동함`은 반대 사례인데, 재할당은 지역 이름만 바꾸기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `매개변수 대입과 객체 내부 수정을 같은 동작으로 혼동함`입니다. 잘못된 코드는 실행은 되지만 `[10]`이라는 잘못된 출력을 냅니다. 대입과 내부 수정을 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `가변 객체의 공유 부작용 구분`이라는 의미와 입력·출력 계약입니다. C의 포인터 전달과 Rust의 가변 빌림으로 같은 동작을 구현하고, 실행 결과 `[10, 30]`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '원래 입력을 보존해야 한다면 `items.copy()`로 별도 리스트를 만드세요. 안에 다른 리스트가 있으면 한계를 기억하세요.',
    },
    45: {
        'notes': [
            '참조는 가리키는 원본이 살아 있는 동안에만 사용할 수 있습니다. 수명 표기는 초 단위로 세는 값이 아니라 이 입력 참조와 반환 참조가 함께 유효해야 하는 범위를 관계로 표시한 이름입니다. 두 입력 중 하나를 골라 참조로 반환한다면 결과는 두 입력이 모두 유효한 범위 안에서만 쓸 수 있습니다.',
            '함수 안에서 새로 만든 지역 문자열의 참조를 돌려주려고 하면 함수가 끝나는 순간 원본이 사라져 위험합니다. Rust는 이런 코드를 컴파일 때 막습니다. 소유한 `String`을 반환하거나 호출자가 소유한 데이터에 대한 참조를 반환하는 방식으로 고칩니다. 수명 표기는 원본을 오래 살게 만드는 마법이 아닙니다.',
        ],
        'predict': '1. study 길이는 5, log 길이는 3입니다. 2. 첫 참조를 선택합니다. 3. 유효한 문자열 study를 출력합니다. `x 길이 5 → y 길이 3 → x 반환 → study` 흐름으로 실제 출력 `study`가 됩니다. 핵심 수명 표기는 입력과 반환 참조의 유효 범위를 묶는 자리에 쓰입니다.',
        'guided': '원본은 `study`, 수정본은 `study!`이다. 첫 변경 줄의 형식 문자열 `"{}"`에서 `"{}!"`로 표시 모양을 바꾸면, 계산된 반환 참조는 그대로이고 화면에만 느낌표가 붙는다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `fn longer<수명/static>(x: 수명/a str, y: 수명/a str) -> 수명/a str {`처럼 선언과 사용의 수명 이름이 어긋난 선언입니다. 선언되지 않은 수명 이름을 쓰므로 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 함수 선언 줄입니다. 고친 줄의 일치된 수명 표기에서는 `입력 참조와 반환 참조 수명 관계 명시` 동작이 지켜집니다. 지역 문자열의 참조를 밖으로 돌려주는 코드는 원본이 사라지므로 같은 단계에서 막힙니다. 정상 코드는 길이 비교→첫 참조 선택→유효 문자열 출력의 순서로 `study`를 출력합니다.',
        'qout': '먼저 study 길이 5와 log 길이 3을 비교합니다. 이어서 첫 참조가 선택되어 유효한 문자열 study가 반환되고, println!이 표시하므로 출력 `study`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 수명 관계와 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': '수명 표기는 입력 참조와 반환 참조가 함께 유효해야 하는 범위를 관계로 표시한다는 뜻이고, 원본이 살아 있는 동안만 쓴다는 뜻은 함수 안 지역 값의 참조를 밖으로 내보낼 수 없다는 뜻입니다. `지역에서 만든 String의 참조를 함수 밖으로 반환함`은 반대 사례인데, 함수가 끝나면 원본이 사라지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `지역에서 만든 String의 참조를 함수 밖으로 반환함`입니다. 잘못된 코드는 선언이 어긋나 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `입력 참조와 반환 참조 수명 관계 명시`라는 의미와 입력·출력 계약입니다. C의 유효 수명 수동 관리와 Python의 참조 유효성으로 같은 동작을 구현하고, 실행 결과 `study`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '수명 표기는 원본을 오래 살게 만드는 마법이 아닙니다. 소유 반환과 호출자 데이터 참조 반환으로 고치세요.',
    },
    46: {
        'notes': [
            '파일 스트림은 내용뿐 아니라 현재 읽거나 쓸 위치도 기억합니다. 파일에 값을 쓴 직후 위치는 끝입니다. 같은 스트림에서 다시 읽고 싶으면 `rewind(f)`로 처음으로 옮겨야 합니다. `fopen`이 실패하면 `NULL`을 돌려주므로 읽기·쓰기 전에 확인하세요.',
            '`fclose`를 하면 스트림의 자원을 정리합니다. 열기는 성공했는데 중간 작업이 실패해도 닫는 경로를 놓치지 마세요. 실제 파일을 다룰 때는 파일 존재 여부, 접근 권한, 읽기 실패도 정상 흐름에 포함해서 처리해야 합니다.',
        ],
        'predict': '1. tmpfile이 임시 파일을 엽니다. 2. 30을 쓰고 위치를 처음으로 돌립니다. 3. fscanf 성공 후 출력하고 닫습니다. `열기 → 쓰기 → rewind → 읽기 → 닫기` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `rewind(f)`는 스트림 위치를 처음으로 돌리는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `30`을 출력한다. 바꾼 return 2는 파일 열기 실패 갈래에 있어 정상 흐름에서는 실행되지 않습니다. 성공 흐름의 값은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'undefined-behavior',
        'debug': '틀린 줄은 `fputs("30\\n", f); fclose(f);`입니다. 스트림을 닫은 뒤 같은 `FILE *`로 읽는 동작은 정상적인 파일 읽기가 아닙니다. 닫힌 스트림은 더 이상 사용할 수 없는 수명 문제이므로, 특정 출력 부재만 주장하지 않습니다. 어떤 결과가 나올지 보장할 수 없으므로 출력값을 약속하지 않습니다. 오류 분류는 정의되지 않은 동작입니다. 고친 줄의 `rewind(f)`에서는 `파일 포인터 위치와 실패를 관리함` 동작이 지켜집니다. 정상 코드는 열기→쓰기→위치 되돌리기→읽기→닫기의 순서로 `30`을 출력합니다.',
        'qout': '먼저 tmpfile이 임시 파일을 엽니다. 이어서 30을 쓰고 rewind로 처음으로 돌린 뒤, fscanf가 성공해 30을 읽어 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 열기·쓰기·읽기가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '쓰고 난 뒤 스트림 위치는 끝이라는 뜻이고, rewind로 처음으로 옮겨야 새 내용을 읽을 수 있다는 뜻은 위치를 돌리지 않으면 끝에서 읽게 된다는 뜻입니다. `쓰기 직후 위치를 되돌리지 않고 읽어 EOF를 만남`은 반대 사례인데, 끝 위치에서는 읽을 내용이 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `쓰기 직후 위치를 되돌리지 않고 읽어 EOF를 만남`입니다. 잘못된 코드는 닫힌 스트림을 읽으므로 정의되지 않은 동작입니다. 위치 되돌리기와 닫기 순서를 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `파일 포인터 위치와 실패를 관리함`이라는 의미와 입력·출력 계약입니다. Python의 파일 위치와 Rust의 커서 위치로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '실제 파일에서는 존재 여부, 접근 권한, 읽기 실패도 정상 흐름에 포함하세요. `fopen` 실패의 `NULL`을 먼저 확인하세요.',
    },
    47: {
        'notes': [
            '`StringIO`는 문자열을 파일처럼 한 줄씩 읽게 해 주는 연습 도구입니다. `for line in file:`은 줄 단위로 움직이고 `int(line)`은 그 줄이 숫자 글자일 때 정수로 바꿉니다. 줄 바꿈 포함 열 글자는 10이 되지만 숫자 아닌 줄은 `ValueError`가 납니다.',
            '실제 파일에서는 `with open`으로 열면 블록을 나갈 때 정리됩니다. 변환 실패를 `try/except ValueError`로 다루되, 어떤 줄이 실패했는지 사용자에게 알려 줄 수도 있습니다. 입력 줄이 비어 있으면 건너뛸지 오류로 볼지도 먼저 정하세요.',
        ],
        'predict': '1. 첫 줄 10을 문자열로 읽습니다. 2. 각 줄을 int로 바꾸어 [10,20]을 만듭니다. 3. sum이 30입니다. `문자열 줄 → int 변환 → [10,20] → 합 30` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `int(line)`은 숫자 글자를 정수로 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `60`이다. 첫 변경 줄의 입력에 30 줄을 더하면, 변환 리스트에 30이 추가되어 합이 60이 된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `values = [line for line in stream]`입니다. 변환 없이 글자 줄을 모아 `sum`에 넣으므로, 문자열과 정수를 더하려다 TypeError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 합계 계산입니다. 고친 줄 `values = [int(line) for line in stream]`에서는 `텍스트 스트림을 순회하고 변환함` 동작이 지켜집니다. 정상 코드는 줄 읽기→숫자 변환→합산의 순서로 `30`을 출력합니다.',
        'qout': '먼저 스트림에서 첫 줄 10을 문자열로 읽습니다. 이어서 각 줄을 int로 바꾸어 [10,20]을 만들고 sum이 30을 구하므로 출력 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 읽기·변환·합산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'StringIO는 메모리 속 파일형 객체라는 뜻이고, 실제 파일로 바꿔도 한 줄씩 읽는 규칙은 같다는 뜻은 연습과 실전이 같은 순서로 동작한다는 뜻입니다. `문자열을 숫자로 바꾸지 않고 합하려고 함`은 반대 사례인데, 글자 상태로는 덧셈이 성립하지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `문자열을 숫자로 바꾸지 않고 합하려고 함`입니다. 잘못된 코드는 TypeError로 멈춥니다. 변환 단계를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `텍스트 스트림을 순회하고 변환함`이라는 의미와 입력·출력 계약입니다. C의 줄 읽기와 Rust의 행 반복 읽기로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '숫자 아닌 줄은 `ValueError`가 납니다. 입력 줄이 비어 있으면 건너뛸지 오류로 볼지 먼저 정하세요.',
    },
    48: {
        'notes': [
            '`Cursor`는 메모리 속 바이트를 파일처럼 읽을 수 있게 합니다. `read_to_string(&mut text)`는 읽은 글자를 `text`에 덧붙이려 시도하고 성공 또는 오류를 `Result`로 돌려줍니다. 성공했다면 몇 바이트를 읽었는지도 알 수 있습니다.',
            'UTF-8이 아닌 바이트를 문자열로 읽으려 할 때는 실패할 수 있습니다. `.unwrap()`은 연습용 예제에서는 간단하지만 실제 입력에서는 오류를 사용자에게 알려 주거나 물음표 연산자로 호출자에게 전달하는 편이 낫습니다. 읽기 전 빈 `text`와 읽기 뒤 `text`를 나눠 적어 보세요.',
        ],
        'predict': '1. Cursor에 30을 넣습니다. 2. 가변 문자열 text에 내용을 읽습니다. 3. 성공을 확인하고 30을 출력합니다. `source=30 → text 비어 있음 → read 성공 text=30` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `read_to_string`은 목적 버퍼에 읽은 내용을 덧붙이는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `30!`이다. 첫 변경 줄 `Cursor::new("30")`에서 `Cursor::new("30!")`로 입력 바이트를 바꾸면, 읽기와 표시가 그대로 이어져 느낌표가 붙는다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `source.read_exact(&mut text).expect("읽기 실패");`입니다. `read_exact`는 바이트 배열을 받는 읽기인데 문자열 버퍼를 직접 넘겼으므로 타입이 맞지 않아 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 읽기 호출입니다. 고친 줄 `source.read_to_string(&mut text).expect("읽기 실패");`에서는 `읽기 결과와 목적 버퍼를 처리함` 동작이 지켜집니다. 정상 코드는 준비→읽어 덧붙이기→성공 확인→출력의 순서로 `30`을 출력합니다.',
        'qout': '먼저 Cursor에 30 바이트가 준비됩니다. 이어서 read_to_string이 text에 내용을 읽어 덧붙이고 성공을 확인한 뒤 표시하므로 출력 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 읽기와 타입이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'Cursor는 메모리 버퍼를 파일처럼 읽게 한다는 뜻이고, read_to_string은 text를 변경하고 Result를 돌려준다는 뜻은 읽기와 오류 처리가 한 호출에 들어있다는 뜻입니다. `읽기 결과의 오류를 무시해 빈 데이터를 정상으로 취급함`은 반대 사례인데, 실패를 확인하지 않으면 빈 내용을 정상으로 오인하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `읽기 결과의 오류를 무시해 빈 데이터를 정상으로 취급함`입니다. 잘못된 코드는 타입이 맞지 않아 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `읽기 결과와 목적 버퍼를 처리함`이라는 의미와 입력·출력 계약입니다. C의 버퍼 읽기와 Python의 스트림 읽기로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'UTF-8이 아닌 바이트는 문자열 읽기가 실패할 수 있습니다. 읽기 전 빈 `text`와 읽기 뒤 `text`를 나눠 적어 보세요.',
    },
    49: {
        'notes': [
            '`int minutes = 30; int *view = &minutes;`에서 `minutes`는 값 30, `&minutes`는 그 값의 주소, `view`는 저장된 주소, `*view`는 그 주소에 있는 값 30입니다. `view`와 `*view`를 같은 종류의 값으로 읽지 마세요.',
            '`*view = 31`을 실행하면 `minutes`도 31이 됩니다. `free`한 공간의 주소나 범위 밖 원소를 가리킬 때는 같은 문법이라도 안전하게 읽을 수 없습니다. 출력만 외우지 말고 포인터가 가리키는 원본의 유효성을 먼저 확인하세요.',
        ],
        'predict': '1. minutes에 30을 저장합니다. 2. view에 &minutes를 담습니다. 3. *view를 읽어 30을 출력합니다. `minutes=30 → view=&minutes → *view=30` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `*view`는 주소에 저장된 값을 읽는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `int minutes = 30;`에서 `int minutes = 31;`로 저장 값을 바꾸면, view가 가리키는 값이 함께 바뀌어 역참조 결과가 달라진다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `int view = &minutes;`입니다. 주소를 정수 변수에 넣었으므로 뒤의 `*view`는 포인터가 아닌 값을 역참조하려 해 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 타입이 어긋나는 첫 순간은 주소 대입 줄입니다. 고친 줄 `int *view = &minutes;`에서는 `주소를 통해 저장된 값 읽기` 동작이 지켜집니다. 정상 코드는 값 저장→주소 보관→역참조 읽기의 순서로 `30`을 출력합니다.',
        'qout': '먼저 minutes에 30이 저장됩니다. 이어서 view에 그 주소가 보관되고, 역참조해 30을 읽어 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 주소 보관과 역참조가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'view의 값은 minutes의 주소라는 뜻이고, *view의 값은 그 주소에 저장된 정수 30이라는 뜻은 주소와 값이 다른 종류라는 뜻입니다. `포인터 값(주소)과 역참조 결과(정수)를 같은 타입으로 혼동함`은 반대 사례인데, 주소 보관과 값 읽기는 다른 연산이기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `포인터 값(주소)과 역참조 결과(정수)를 같은 타입으로 혼동함`입니다. 잘못된 코드는 타입이 맞지 않아 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `주소를 통해 저장된 값 읽기`라는 의미와 입력·출력 계약입니다. Python의 참조 읽기와 Rust의 참조 역참조로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '해제한 공간의 주소나 범위 밖 원소를 가리킬 때는 같은 문법이라도 안전하게 읽을 수 없습니다. 원본의 유효성을 먼저 확인하세요.',
    },
    50: {
        'notes': [
            '구조체 `Session`은 관련 값들의 형식이고 한 건의 값은 따로 만듭니다. `s.minutes`는 그 기록에 있는 분 필드를 선택합니다. 학생별 이름과 분을 따로 배열에 넣으면 순서가 어긋날 수 있는데, 한 건의 데이터를 묶으면 함께 전달하기 쉽습니다.',
            '구조체 변수를 선언할 때 필드 초기화를 명확히 해 두세요. 읽기 전에 초기화하지 않은 값에는 의미 있는 기본값이 자동으로 들어 있지 않습니다. `s.minutes = 30`을 한 뒤에 읽는 순서를 지키세요.',
        ],
        'predict': '1. Session 타입을 정의합니다. 2. s에 시간 30과 집중 1을 초기화합니다. 3. s.minutes를 읽어 출력합니다. `타입 정의 → s={30,1} → s.minutes=30` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `s.minutes`는 묶인 값에서 분 필드를 선택하는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `Session s = {30, 1};`에서 `Session s = {31, 1};`로 분 값을 바꾸면, 선택하는 필드는 그대로이고 읽히는 값이 달라진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `printf("%d\\n", s.focused);`입니다. 분이 아니라 집중도 필드를 읽으므로 실행은 되지만 결과는 `1`이 됩니다. 오류 분류는 잘못된 출력입니다. 필드 선택이 어긋나는 첫 순간은 점 연산자 뒤 이름입니다. 고친 줄 `printf("%d\\n", s.minutes);`에서는 `관련 필드를 하나의 값으로 묶음` 동작이 지켜집니다. 정상 코드는 형식 정의→초기화→분 필드 선택의 순서로 `30`을 출력합니다.',
        'qout': '먼저 Session 형식에 맞춰 s에 30과 1이 초기화됩니다. 이어서 s.minutes가 분 필드 30을 선택해 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 구조체 선언과 읽기가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'Session은 두 필드의 형식이라는 뜻이고, s는 그 형식으로 만든 실제 값이라는 뜻은 형식과 값이 분리된다는 뜻입니다. `필드 선언 순서와 초기화 값 순서를 혼동함`은 반대 사례인데, 순서가 어긋나면 다른 필드의 값을 읽게 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `필드 선언 순서와 초기화 값 순서를 혼동함`입니다. 잘못된 코드는 실행은 되지만 `1`이라는 잘못된 출력을 냅니다. 점 뒤 필드 이름을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `관련 필드를 하나의 값으로 묶음`이라는 의미와 입력·출력 계약입니다. Python의 클래스와 Rust의 구조체로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '읽기 전에 초기화하지 않은 값에는 기본값이 자동으로 들어 있지 않습니다. `s.minutes = 30` 뒤에 읽는 순서를 지키세요.',
    },
    51: {
        'notes': [
            '클래스는 같은 종류의 기록을 만드는 설계이고 인스턴스는 그 설계로 만든 실제 기록 하나입니다. `__init__`의 `self`는 지금 만들거나 사용 중인 객체를 가리킵니다. `self.minutes = minutes`는 전달받은 분을 객체에 저장합니다. 두 객체를 만들면 각각 자기 `minutes`를 가집니다.',
            '함수의 지역 변수 `minutes`와 속성 `self.minutes`는 다릅니다. 메서드가 끝나도 객체의 속성은 남습니다. `a.minutes`만 바꿨을 때 `b.minutes`가 변하지 않아야 정상입니다. 값을 함께 공유하고 싶을 때와 각 객체에 따로 둘 때를 구별하세요.',
        ],
        'predict': '1. Session(30)이 새 인스턴스를 만듭니다. 2. __init__에서 그 객체의 minutes에 30을 저장합니다. 3. s.minutes로 읽습니다. `클래스 정의 → s 생성 → s.minutes=30 → 출력` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `self.minutes`는 객체에 값을 남기는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `s = Session(30)`에서 `s = Session(31)`로 인수를 바꾸면, 새 객체의 속성이 31로 저장된 뒤 읽힌다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `minutes = minutes`입니다. 지역 이름에만 값을 넣고 객체에는 남기지 않으므로, 뒤의 `print(s.minutes)`에서 속성이 없어 AttributeError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 속성 읽기 줄입니다. 고친 줄 `self.minutes = minutes`에서는 `인스턴스 속성 초기화` 동작이 지켜집니다. 정상 코드는 객체 생성→속성 저장→속성 읽기의 순서로 `30`을 출력합니다.',
        'qout': '먼저 Session(30)이 새 인스턴스를 만듭니다. 이어서 __init__이 그 객체의 minutes에 30을 저장하고, s.minutes로 읽어 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 클래스 정의와 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '클래스는 객체의 설계도라는 뜻이고, self는 지금 만들고 있는 바로 그 객체라는 뜻은 메서드가 다루는 대상이 정해져 있다는 뜻입니다. `self를 빼고 지역 변수만 저장해 객체에 값이 남지 않음`은 반대 사례인데, 지역 저장은 객체에 남지 않기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `self를 빼고 지역 변수만 저장해 객체에 값이 남지 않음`입니다. 잘못된 코드는 속성이 없어 AttributeError로 멈춥니다. self 유무를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `인스턴스 속성 초기화`라는 의미와 입력·출력 계약입니다. C의 구조체 초기화와 Rust의 구조체 생성으로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`a.minutes`만 바꿨을 때 `b.minutes`가 변하지 않아야 정상입니다. 공유와 분리를 구별하세요.',
    },
    52: {
        'notes': [
            '`struct Session { minutes: u32 }`는 값의 필드를 정의하고, `impl Session`은 그 값에 관한 메서드를 모읍니다. 메서드의 `&self`는 지금 호출한 객체를 읽기만 빌린다는 뜻입니다. `session.minutes`를 계산에 쓴 후에도 호출자에게 소유권이 남습니다.',
            '상태를 바꾸는 메서드가 필요하면 소유자가 `mut`여야 하고 수신자를 `&mut self`로 선언합니다. `self`, `&self`, `&mut self`가 각각 소유권 이동, 공유 읽기, 배타적 수정과 어떻게 연결되는지 예제 호출 전후로 표시해 보세요.',
        ],
        'predict': '1. minutes=30인 Session을 만듭니다. 2. doubled가 s를 빌려 30*2를 계산합니다. 3. 60을 출력합니다. `s.minutes=30 → &s 빌림 → 60 반환` 흐름으로 실제 출력 `60`이 됩니다. 핵심 `&self`는 소유권 없이 읽기를 빌리는 자리에 쓰입니다.',
        'guided': '원본은 `60`, 수정본은 `90`이다. 첫 변경 줄의 `self.minutes * 2`에서 `self.minutes * 3`으로 곱수를 바꾸면, 같은 30에 대해 90이 계산되어 반환된다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `impl Session { fn doubled(self) -> u32 { self.minutes * 2 } }`입니다. 읽기 전용 호출에 소유권 이동을 쓰므로 이번 한 번 호출에서는 화면 출력이 같습니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. 호출 뒤에 `s`를 다시 쓰면 이동된 소유권 때문에 컴파일이 막힙니다. 고친 줄의 `&self`에서는 `데이터에 연결된 읽기 메서드` 동작이 지켜집니다. 정상 코드는 값 생성→공유 읽기 호출→60 반환의 순서로 `60`을 출력합니다.',
        'qout': '먼저 minutes=30인 Session이 만들어집니다. 이어서 doubled가 s를 빌려 30*2를 계산해 60을 반환하고, println!이 표시하므로 출력 `60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 수신자와 계산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'impl의 `&self`는 소유권을 가져가지 않고 구조체 값을 읽는 메서드 수신자라는 뜻은, 호출 뒤에도 호출자가 값을 쓸 수 있다는 뜻입니다. `읽기 전용 메서드에 self를 써 소유권을 불필요하게 이동함`은 반대 사례인데, 이동 뒤에는 원래 이름을 쓸 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `읽기 전용 메서드에 self를 써 소유권을 불필요하게 이동함`입니다. 잘못된 코드는 이번 한 번 호출에서는 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 호출 뒤 재사용이 막히는 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `데이터에 연결된 읽기 메서드`라는 의미와 입력·출력 계약입니다. C의 읽기 함수와 Python의 메서드로 같은 동작을 구현하고, 실행 결과 `60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`self`, `&self`, `&mut self`를 호출 전후 소유권 그림에 표시하세요. 상태를 바꾸려면 `&mut self`와 `mut` 소유자가 필요합니다.',
    },
    53: {
        'notes': [
            '`enum ResultLabel { Pass, Fail }`처럼 상태를 두 가지로 정하면 뜻밖의 세 번째 문자열이 들어오는 것을 줄일 수 있습니다. `ResultLabel::Pass`는 해당 enum의 합격 상태를 만든 값입니다. `match`는 가능한 갈래별로 동작을 적게 해 줍니다.',
            '문자열 `"pass"`, `"PASS"`, `"passed"`를 조건문으로 비교하는 방식보다 상태 공간이 명확합니다. 새 갈래를 추가하면 기존 `match`에 빠진 처리를 컴파일러가 짚어 줍니다. `Option<T>`와 `Result<T,E>`도 enum을 활용하는 익숙한 예입니다.',
        ],
        'predict': '1. Pass 상태를 선택합니다. 2. match가 Pass arm을 실행합니다. 3. 통과를 출력합니다. `state=Pass → Pass arm → 통과` 흐름으로 실제 출력 `통과`가 됩니다. 핵심 `ResultLabel::Pass`는 합격 상태를 만드는 자리에 쓰입니다.',
        'guided': '원본은 `통과`, 수정본은 `통과!`이다. 첫 변경 줄의 Pass 갈래 문자열 `"통과"`에서 `"통과!"`로 표시를 바꾸면, 선택되는 갈래는 그대로이고 화면 모양만 달라진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `let state = ResultLabel::Retry;`입니다. 재시도 상태가 선택되므로 match가 Retry 갈래를 실행해 `재시도`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 상태 선택이 어긋나는 첫 순간은 state 대입입니다. 고친 줄 `let state = ResultLabel::Pass;`에서는 `한정된 상태를 타입으로 표현` 동작이 지켜집니다. 갈래를 빼먹은 match 자체는 컴파일이 막으므로, 이번 잘못된 코드와 구별하세요. 정상 코드는 상태 선택→갈래 실행→표시의 순서로 `통과`를 출력합니다.',
        'qout': '먼저 state에 Pass가 저장됩니다. 이어서 match가 Pass arm을 실행해 통과를 만들고, println!이 표시하므로 출력 `통과`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 갈래 구성이 완전해 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'enum은 정해진 경우만 값으로 만들게 한다는 뜻이고, match는 모든 경우를 처리하게 한다는 뜻은 빠진 갈래가 있으면 컴파일러가 짚어준다는 뜻입니다. `가능한 enum 경우를 match에서 하나 빠뜨림`은 반대 사례인데, 그때는 실행이 아니라 컴파일이 막히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `가능한 enum 경우를 match에서 하나 빠뜨림`입니다. 잘못된 코드는 실행은 되지만 `재시도`라는 잘못된 출력을 냅니다. 선택된 상태가 무엇인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `한정된 상태를 타입으로 표현`이라는 의미와 입력·출력 계약입니다. C의 enum·switch와 Python의 상수·분기 검토로 같은 동작을 구현하고, 실행 결과 `통과`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '새 갈래를 추가하면 기존 `match`에 빠진 처리를 컴파일러가 짚어 줍니다. 상태 추가 뒤 전체 갈래를 다시 시험하세요.',
    },
    54: {
        'notes': [
            '제네릭 `T`는 아직 구체적으로 정하지 않은 값의 타입을 뜻합니다. 하지만 어떤 `T`든 화면에 표시할 수 있는 것은 아닙니다. `T: Display`라고 제약하면 그 타입이 사용자에게 보여 줄 글자로 형식화할 수 있어야 한다는 뜻입니다.',
            '정수와 문자열을 모두 받는 출력 함수는 공통 동작을 한 번만 작성할 수 있습니다. 반대로 `Display` 구현이 없는 타입을 넘기면 컴파일 오류가 납니다. 타입 이름을 감춘다는 뜻이 아니라 필요한 능력을 선언하는 것이라고 생각하세요.',
        ],
        'predict': '1. show(30)에서 T가 정수로 정해집니다. 2. Display 구현이 있는지 검사합니다. 3. println!이 30을 표시합니다. `T=i32 추론 → Display 확인 → 30 출력` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `Display`는 표시 능력을 요구하는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `fn main() { show(30); }`에서 `fn main() { show(31); }`로 인수를 바꾸면, 같은 Display 검사를 거쳐 새 값이 표시된다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `fn show<T: std::fmt::Debug>(value: T) { println!("{value}"); }`입니다. 자리 표시자 `{value}`는 Display 능력을 요구하는데 선언된 제약은 Debug뿐이므로 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 제약 선언입니다. 고친 줄의 `Display`에서는 `필요한 동작만 제네릭 타입에 요구함` 동작이 지켜집니다. 정상 코드는 타입 확정→능력 확인→표시의 순서로 `30`을 출력합니다.',
        'qout': '먼저 show(30)에서 T가 정수로 정해집니다. 이어서 Display 구현이 확인되어 println!이 30을 표시하므로 출력 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 제약과 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'T는 타입 매개변수라는 뜻이고, Display 제약은 그 값이 사용자용 문자열로 출력될 수 있음을 보장한다는 뜻은 표시 능력이 컴파일 때 검사된다는 뜻입니다. `지원하지 않는 타입을 제약 없이 출력할 수 있다고 생각함`은 반대 사례인데, 능력이 없으면 컴파일이 막히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `지원하지 않는 타입을 제약 없이 출력할 수 있다고 생각함`입니다. 잘못된 코드는 제약이 어긋나 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `필요한 동작만 제네릭 타입에 요구함`이라는 의미와 입력·출력 계약입니다. C의 매크로 동작과 Python의 덕 타이핑으로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '타입 이름을 감춘다는 뜻이 아니라 필요한 능력을 선언하는 것이라고 생각하세요. `Display` 없는 타입을 넘기면 컴파일이 막힙니다.',
    },
    55: {
        'notes': [
            '한 파일에 입력·계산·출력을 모두 섞으면 계산을 따로 시험하기 어렵습니다. `total(values)` 같은 작은 함수를 만들고 `assert total([10,20]) == 30`으로 기대 결과를 고정하세요. `assert`는 참이면 조용히 지나가고 거짓이면 `AssertionError`로 멈춥니다.',
            '빈 입력 결과가 0인지, 한 항목이 그대로 나오는지도 별도로 시험하세요. 배포 중에도 항상 실행되어야 할 입력 검증을 `assert`에만 의존하면 안 됩니다. Python은 최적화 모드에서 `assert`를 제거할 수 있습니다. 모듈을 나눌 때는 함수가 어떤 값을 받고 무엇을 돌려주는지부터 적으세요.',
        ],
        'predict': '1. total에 두 값을 전달합니다. 2. sum 결과 30을 기대값과 비교합니다. 3. 참이므로 검증 완료를 출력합니다. `[10,20] → total=30 → 조건 참 → 출력` 흐름으로 실제 출력 `검증 완료`가 됩니다. 핵심 `assert`는 기대값을 고정하고 어긋나면 멈추는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `검증 완료`를 출력한다. 기대값을 함께 31로 바꿨으므로 assert가 통과해 출력까지 도달합니다. 검사 대상과 기대값이 함께 바뀌어 통과 여부는 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `print total([10, 20]) == 30`입니다. `print` 뒤에 괄호가 없어 Python 3 문법에 맞지 않으므로 실행 전에 SyntaxError라는 컴파일 오류가 나고 프로그램이 시작되지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 검사 줄 자체입니다. 한 걸음 더 들어가면, 괄호를 넣어 `print(...)`로 고쳐도 검사가 실패했을 때 멈추지 않고 참·거짓을 화면에 표시할 뿐이라 마지막 검증 완료가 계속 나올 수 있습니다. 실패를 탐지해 멈추는 기능이 사라진다는 점이 핵심입니다. 고친 줄 `assert total([10, 20]) == 30`에서는 `예상 결과를 작은 테스트로 고정` 동작이 지켜집니다. 정상 코드는 계산→기대값 비교→통과 후 출력의 순서로 `검증 완료`를 출력합니다.',
        'qout': '먼저 total에 [10,20]이 전달되어 sum 30이 계산됩니다. 이어서 assert가 기대값 30과 비교해 참이므로 통과하고, print가 표시하므로 출력 `검증 완료`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 함수와 검사가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'assert는 조건이 거짓일 때 예외를 낸다는 뜻이고, 조건이 참일 때는 다음 줄로 진행한다는 뜻은 통과가 조용하다는 뜻입니다. `assert가 실패해도 다음 print가 실행된다고 생각함`은 반대 사례인데, 실패하면 예외로 멈추기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `assert가 실패해도 다음 print가 실행된다고 생각함`입니다. 잘못된 코드는 괄호 없는 print라 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 자체가 막힙니다. 검사와 표시의 역할을 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `예상 결과를 작은 테스트로 고정`이라는 의미와 입력·출력 계약입니다. C의 assert와 Rust의 assert_eq!로 같은 동작을 구현하고, 실행 결과 `검증 완료`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 입력 결과가 0인지도 별도로 시험하세요. 배포 중 상시 검증을 `assert`에만 의존하면 안 됩니다.',
    },
    56: {
        'notes': [
            '`Session { minutes: 30 }`은 구조체 한 개를 만들고, `assert_eq!(s.minutes, 30)`은 실제 필드값과 기대값을 비교합니다. 구조체는 관련 데이터를 묶고 enum은 가능한 상태를 한정합니다. 둘을 함께 쓰면 어떤 기록인지와 어떤 결과 상태인지가 분명해집니다.',
            '`assert_eq!`의 두 값이 다르면 테스트가 실패하고, 같으면 이어 진행합니다. `minutes`를 31로 바꾸고 기대값을 30으로 남겨 실패 메시지를 본 다음 둘 다 31로 맞춰 보세요. 테스트는 실행 전부터 정답을 정확히 적어 두는 약속입니다.',
        ],
        'predict': '1. Session을 초기화합니다. 2. 필드 값과 기대값이 같아 단언이 통과합니다. 3. 검증 완료를 출력합니다. `s.minutes=30 → 기대값=30 → 통과` 흐름으로 실제 출력 `검증 완료`가 됩니다. 핵심 `assert_eq!`는 실제와 기대를 비교해 고정하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `검증 완료`를 출력한다. minutes와 기대값을 함께 바꿔 assert가 그대로 통과하므로 출력이 같습니다. 검사 대상과 기대값이 함께 바뀌어 통과 여부는 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `println!(s.minutes,30);`입니다. 형식 문자열 자리에 리터럴이 아니라 값이 와서 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 검사 줄 자체입니다. 고친 줄 `assert_eq!(s.minutes,30);`에서는 `구조화된 데이터에 회귀 테스트 추가` 동작이 지켜집니다. 검사하지 않은 예시 출력만 보고 맞다고 결론 내지 마세요. 정상 코드는 초기화→비교 통과→출력의 순서로 `검증 완료`를 출력합니다.',
        'qout': '먼저 Session이 minutes=30으로 초기화됩니다. 이어서 assert_eq!가 실제 30과 기대 30을 비교해 통과하고, println!이 표시하므로 출력 `검증 완료`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 초기화와 검사가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'Session의 minutes는 u32이고 assert_eq!가 실제 값 30과 기대 30을 비교한다는 뜻은, 구조화된 값에 회귀 약속을 건다는 뜻입니다. `검사하지 않은 예시 출력만 보고 구현이 맞다고 결론 냄`은 반대 사례인데, 출력만으로는 약속 위반을 잡을 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `검사하지 않은 예시 출력만 보고 구현이 맞다고 결론 냄`입니다. 잘못된 코드는 형식 문자열 자리가 어긋나 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `구조화된 데이터에 회귀 테스트 추가`라는 의미와 입력·출력 계약입니다. C의 assert와 Python의 assert로 같은 동작을 구현하고, 실행 결과 `검증 완료`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`minutes`를 31로 바꾸고 기대값을 30으로 남겨 실패 메시지를 본 다음 둘 다 31로 맞춰 보세요.',
    },
    58: {
        'notes': [
            '실행 취소를 만들 때는 가장 최근 작업부터 되돌려야 합니다. 작업 A 다음 B 다음 C를 쌓았다면 취소 순서는 C, B, A입니다. Python 리스트의 `append`는 끝에 쌓고 인자 없는 `pop()`은 끝의 값을 꺼내므로 이 순서에 맞습니다.',
            '빈 스택에서 `pop()`을 부르면 오류가 납니다. 취소할 작업 없음을 미리 검사하세요. 스택 안에 작업 이름만 넣을지, 이전 상태까지 넣을지도 중요합니다. 실제 편집기는 이전 값을 되살릴 정보를 쌓아 두어야 취소가 가능합니다.',
        ],
        'predict': '1. 입력을 저장합니다. 2. 그 위에 삭제를 쌓습니다. 3. pop이 삭제를 반환하고 입력은 남습니다. `[] → [입력] → [입력,삭제] → 삭제 pop` 흐름으로 실제 출력 `삭제`가 됩니다. 핵심 `pop()`은 맨 위의 최신 작업을 꺼내는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `삭제`를 출력한다. 꺼내는 값은 마지막에 넣은 삭제 그대로이므로 출력이 같습니다. 바뀐 것은 먼저 쌓은 값뿐이고 꺼내는 쪽은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(history.pop(0))`입니다. 맨 앞의 가장 오래된 입력을 꺼내므로 실행은 되지만 결과는 `입력`이 됩니다. 오류 분류는 잘못된 출력입니다. 순서가 뒤집히는 첫 순간은 꺼내는 위치입니다. 고친 줄 `print(history.pop())`에서는 `최근 작업부터 되돌림` 동작이 지켜집니다. 정상 코드는 쌓기→맨 위 꺼내기→입력 남기기의 순서로 `삭제`를 출력합니다.',
        'qout': '먼저 입력이 저장되고 그 위에 삭제가 쌓입니다. 이어서 pop이 맨 위 삭제를 꺼내 출력하고 입력이 남으므로 `삭제`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 리스트 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'append는 위에 쌓는다는 뜻이고, 인자 없는 pop은 맨 위의 최신 작업을 제거해 돌려준다는 뜻은 취소 순서가 쌓은 역순이라는 뜻입니다. `pop(0)으로 가장 오래된 작업을 먼저 제거함`은 반대 사례인데, 맨 앞을 꺼내면 접수 순서가 되어 취소 순서와 어긋나기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `pop(0)으로 가장 오래된 작업을 먼저 제거함`입니다. 잘못된 코드는 실행은 되지만 `입력`이라는 잘못된 출력을 냅니다. 어느 쪽 끝을 꺼내는지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `최근 작업부터 되돌림`이라는 의미와 입력·출력 계약입니다. 스택 구조의 끝 연산으로 같은 동작을 구현하고, 실행 결과 `삭제`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 스택에서 `pop()`을 부르면 오류가 납니다. 취소할 작업 없음을 미리 검사하세요.',
    },
    59: {
        'notes': [
            '은행 대기표처럼 먼저 온 사람이 먼저 처리되는 순서를 큐라고 합니다. A, B, C를 뒤에 넣으면 앞에서 A, 다음 B를 꺼냅니다. `collections.deque`에서 `append`는 뒤에 넣고 `popleft`는 앞에서 꺼냅니다.',
            '일반 리스트의 맨 앞 `pop(0)`은 남은 원소를 당겨야 하므로 항목이 많으면 비쌉니다. `deque.popleft()`는 앞에서 꺼내기 용도에 맞습니다. 빈 큐에서 꺼내지 않도록 `if queue:` 같은 검사를 먼저 하세요. 스택의 최신 우선 순서와 큐의 접수 순서를 비교해 보세요.',
        ],
        'predict': '1. A와 B가 대기열에 들어 있습니다. 2. C가 뒤에 합류해 [A,B,C]가 됩니다. 3. 앞에서 A를 꺼내 출력합니다. `[A,B] → [A,B,C] → A 처리` 흐름으로 실제 출력 `A`가 됩니다. 핵심 `popleft()`는 맨 앞의 가장 오래된 항목을 꺼내는 자리에 쓰입니다.',
        'guided': '원본은 `A`, 수정본은 `A!`이다. 첫 변경 줄 `waiting = deque(["A", "B"])`에서 `waiting = deque(["A!", "B"])`로 첫 값을 바꾸면, 뒤에 C가 붙고 앞에서 꺼내는 순서가 그대로 이어져 맨 앞의 새 값 `A!`가 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(waiting.pop())`입니다. 맨 뒤의 마지막 접수 C를 꺼내므로 실행은 되지만 결과는 `C`가 됩니다. 오류 분류는 잘못된 출력입니다. 순서가 뒤집히는 첫 순간은 꺼내는 쪽입니다. 고친 줄 `print(waiting.popleft())`에서는 `먼저 도착한 요청부터 처리` 동작이 지켜집니다. 정상 코드는 뒤에 넣기→앞에서 꺼내기→맨 앞 출력의 순서로 `A`를 출력합니다.',
        'qout': '먼저 A와 B가 대기열에 들어 있습니다. 이어서 C가 뒤에 합류해 [A,B,C]가 되고, popleft가 맨 앞 A를 꺼내 출력하므로 `A`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 덱 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '큐는 뒤에 넣고 앞에서 꺼낸다는 뜻이고, deque의 popleft는 오래된 항목을 O(1)에 꺼낸다는 뜻은 항목 수와 무관하게 앞에서 꺼낸다는 뜻입니다. `pop()으로 마지막 접수 C를 먼저 처리함`은 반대 사례인데, 맨 뒤를 꺼내면 접수 순서가 뒤집히기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `pop()으로 마지막 접수 C를 먼저 처리함`입니다. 잘못된 코드는 실행은 되지만 `C`라는 잘못된 출력을 냅니다. 어느 쪽 끝을 꺼내는지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `먼저 도착한 요청부터 처리`라는 의미와 입력·출력 계약입니다. 원형 큐의 머리 이동과 Rust의 VecDeque 꺼내기로 같은 동작을 구현하고, 실행 결과 `A`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 큐에서 꺼내지 않도록 `if queue:` 같은 검사를 먼저 하세요. `pop(0)`과 `popleft()`의 비용 차이도 비교하세요.',
    },
    60: {
        'notes': [
            '크기 3짜리 원형 큐를 바닥에 0, 1, 2번 칸이 있는 회전판으로 생각하세요. `head`가 2일 때 다음 칸은 `(2 + 1) % 3 = 0`입니다. 배열 끝에서 다시 처음으로 돌아가지만 기존 항목을 덮어써도 된다는 뜻은 아닙니다.',
            '`head == tail`만으로 비어 있는지 가득 찼는지 구별하려면 추가 규칙이 필요합니다. 항목 수 `count`를 따로 두거나 한 칸을 항상 비워 두는 방식 중 하나를 정하세요. 예제의 인덱스 이동이 데이터의 순서 보존과 어떻게 연결되는지도 그려 보세요.',
        ],
        'predict': '1. head는 마지막 인덱스 2입니다. 2. 3%3으로 위치 0을 얻습니다. 3. buffer[0]=10을 출력합니다. `head=2 → 다음 인덱스=0 → 값 10` 흐름으로 실제 출력 `10`이 됩니다. 핵심 `(head + 1) % 3`은 끝에서 처음으로 돌아오는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `10`을 출력한다. 바뀐 것은 배열 크기뿐이며 읽는 위치 (2+1)%3=0은 그대로라 결과가 같습니다. 크기만 달라지고 읽는 칸은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'undefined-behavior',
        'debug': '틀린 줄은 `printf("%d\\n", buffer[head + 1]);`입니다. head가 마지막 칸 2일 때 `head + 1`은 3이 되어 범위 밖을 읽으므로 C의 정의되지 않은 동작이 됩니다. 특정 숫자가 나온다고 약속하지 않습니다. 어떤 값이 보일 수도 있고 비정상 종료될 수도 있으며, 어떤 결과도 보장할 수 없습니다. 오류 분류는 정의되지 않은 동작입니다. head=0,1,2의 다음 값은 1,2,0이어야 합니다. 고친 줄 `printf("%d\\n", buffer[(head + 1) % 3]);`에서는 `인덱스를 용량 안에서 순환시킴` 동작이 지켜집니다. 정상 코드는 마지막 인덱스 확인→나머지 계산→0번 읽기→출력의 순서로 `10`을 출력합니다.',
        'qout': '먼저 head에 마지막 인덱스 2가 저장됩니다. 이어서 (2+1)%3=0을 계산해 buffer[0]을 읽어 출력하므로 `10`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 유효 범위 접근이라 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '인덱스는 용량으로 나눈 나머지로 순환한다는 뜻이고, head=2의 다음 위치는 (2+1)%3=0이라는 뜻은 끝에서 처음으로 돌아간다는 뜻입니다. `나머지 연산을 빼먹어 배열 경계를 넘음`은 반대 사례인데, 나머지가 없으면 마지막 칸에서 범위 밖이 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `나머지 연산을 빼먹어 배열 경계를 넘음`입니다. 범위 밖 읽기는 정의되지 않은 동작이므로, 특정 숫자가 나온다고 약속하지 마세요. 비어 있음과 가득 참을 구분하는 별도 규칙도 정하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `인덱스를 용량 안에서 순환시킴`이라는 의미와 입력·출력 계약입니다. Python의 나머지 연산과 Rust의 나머지 인덱싱으로 같은 동작을 구현하고, 실행 결과 `10`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`head == tail`만으로 비어 있는지 가득 찼는지 구별할 수 없습니다. 항목 수 `count`를 따로 두거나 한 칸을 비워 두세요.',
    },
    61: {
        'notes': [
            '연결 리스트의 노드는 값과 다음 노드의 주소를 함께 가집니다. `first.next = &last`라면 첫 노드의 `next`가 마지막 노드를 가리킵니다. `first.next->value`는 next 주소를 따라간 다음 그 노드의 value를 읽는다는 두 단계입니다.',
            '따라서 first에서 출발하면 10, 이어서 20, 다음 주소가 `NULL`이므로 끝납니다. `NULL`을 읽으려 하면 잘못됩니다. 여기 예제는 지역 변수로 만든 두 노드를 연결하므로 함수가 끝난 뒤에는 그 주소를 사용할 수 없습니다. 동적 할당한 노드라면 잃어버리기 전에 모든 노드를 순회해 각각 해제하는 책임도 생깁니다.',
        ],
        'predict': '1. last는 값 20과 비어 있음 표시를 가집니다. 2. first의 next를 마지막 노드 주소로 초기화합니다. 3. 다음 노드의 20을 출력합니다. `first(10) → next=마지막 주소 → last(20) → 출력` 흐름으로 실제 출력 `20`이 됩니다. 핵심 `first.next->value`는 주소를 따라가 값을 읽는 두 단계 자리에 쓰입니다.',
        'guided': '원본은 `20`, 수정본은 `21`이다. 첫 변경 줄의 `Node last={20,NULL};`에서 `Node last={21,NULL};`로 마지막 값을 바꾸면, 따라간 주소의 값이 달라져 출력이 달라진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `printf("%d\\n", first.value);`입니다. 다음 노드를 따라가지 않고 첫 노드 값을 읽으므로 실행은 되지만 결과는 `10`이 됩니다. 오류 분류는 잘못된 출력입니다. 링크를 따라가지 않은 첫 순간은 점 연산자 뒤 이름입니다. 고친 줄 `printf("%d\\n", first.next->value);`에서는 `주소 링크를 따라 다음 노드 방문` 동작이 지켜집니다. 정상 코드는 노드 연결→주소 이동→값 읽기의 순서로 `20`을 출력합니다.',
        'qout': '먼저 last에 20과 비어 있음 표시가 저장됩니다. 이어서 first의 next가 last를 가리키고, 따라가 읽은 20을 출력하므로 `20`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 연결과 역참조가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': 'first.next는 last의 주소라는 뜻이고, 화살표는 포인터를 통해 last.value를 읽는다는 뜻은 두 단계로 값을 찾는다는 뜻입니다. `NULL인 next를 검사하지 않고 역참조함`은 반대 사례인데, `NULL`을 따라가면 잘못되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `NULL인 next를 검사하지 않고 역참조함`입니다. 잘못된 코드는 실행은 되지만 `10`이라는 잘못된 출력을 냅니다. 따라간 주소가 유효한지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `주소 링크를 따라 다음 노드 방문`이라는 의미와 입력·출력 계약입니다. Python의 next 참조와 Rust의 Box 링크로 같은 동작을 구현하고, 실행 결과 `20`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '함수가 끝난 뒤 지역 노드 주소를 쓰면 안 됩니다. 동적 할당 노드는 순회하며 각각 해제하세요.',
    },
    62: {
        'notes': [
            '양방향 연결 리스트는 한 노드에서 다음 노드로도, 이전 노드로도 돌아갈 수 있습니다. A 다음이 B라면 `A.next == B`와 `B.prev == A`가 함께 맞아야 합니다. 한쪽만 갱신하면 앞으로 갈 때는 맞아 보여도 뒤로 돌아올 때 잘못된 노드를 만납니다.',
            '새 노드 C를 A와 B 사이에 넣는다면 연결 네 개를 점검하세요. 앞이나 뒤가 없는 머리·꼬리에서는 `None` 처리도 필요합니다. 노드를 추가·삭제할 때마다 왕복해서 같은 위치로 돌아오는지 검사하세요.',
        ],
        'predict': '1. B의 prev를 읽으면 A입니다. 2. A의 next를 읽으면 B입니다. 3. 왕복 관계가 맞는지 출력합니다. `B.prev=A → A.next=B → 일치` 흐름으로 실제 출력 `B`가 됩니다. 핵심 `["prev"]`는 뒤쪽 링크를 먼저 읽는 자리에 쓰입니다.',
        'guided': '원본은 `B`, 수정본은 `None`이다. 첫 변경 줄의 `["B"]["prev"]`에서 `["A"]["next"]`로 읽기 시작점을 바꾸면, A의 next인 B의 next(None)를 읽게 되어 `None`이 출력된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `print(nodes[nodes["B"]["next"]]["next"])`입니다. `nodes["B"]["next"]`가 `None`이므로 `nodes[None]`을 읽으려다 KeyError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 존재하지 않는 키 읽기입니다. 고친 줄 `print(nodes[nodes["B"]["prev"]]["next"])`에서는 `앞뒤 링크 일치 유지` 동작이 지켜집니다. 정상 코드는 뒤 링크 읽기→앞 링크 읽기→왕복 확인의 순서로 `B`를 출력합니다.',
        'qout': '먼저 B의 prev를 읽으면 A입니다. 이어서 A의 next를 읽으면 B가 되어 왕복이 맞으므로 출력 `B`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 딕셔너리 읽기가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'B.prev=A라면 A.next=B여야 한다는 뜻이고, 두 방향이 서로를 가리키는 것이 불변식이라는 뜻은 왕복하면 제자리라는 뜻입니다. `삽입·삭제 때 next만 바꾸고 prev를 갱신하지 않음`은 반대 사례인데, 한쪽만 고치면 돌아올 때 어긋나기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `삽입·삭제 때 next만 바꾸고 prev를 갱신하지 않음`입니다. 잘못된 코드는 존재하지 않는 키를 읽어 KeyError로 멈춥니다. 양쪽 링크를 함께 갱신했는지 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `앞뒤 링크 일치 유지`라는 의미와 입력·출력 계약입니다. C의 양방향 포인터와 Rust의 링크 구조로 같은 동작을 구현하고, 실행 결과 `B`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '머리·꼬리에서는 `None` 처리가 필요합니다. 추가·삭제 때마다 왕복 검사를 하세요.',
    },
    63: {
        'notes': [
            '같은 `[1,2,3]`이라도 어느 끝에서 꺼내느냐에 따라 자료구조의 의미가 달라집니다. 스택의 `pop()`은 가장 나중인 3을, 큐의 `popleft()`는 가장 먼저 온 1을 꺼냅니다. 남은 값도 각각 달라집니다.',
            '실행 취소에는 스택, 접수 순서 처리에는 큐가 자연스럽습니다. 자료구조 이름만 외우지 말고 다음에 처리해야 할 항목은 무엇인가를 먼저 묻고, 1·2·3을 실제로 넣고 꺼내 확인하세요. 빈 상태에서 꺼내는 동작도 정의해야 합니다.',
        ],
        'predict': '1. q=[1,2,3]입니다. 2. 앞의 1을 제거합니다. 3. 뒤의 3을 제거하고 둘을 출력합니다. `[1,2,3] → 앞 1 → 뒤 3` 흐름으로 실제 출력 `1 3`이 됩니다. 핵심 `popleft()`는 오래된 쪽 끝을 먼저 비우는 자리에 쓰입니다.',
        'guided': '원본은 `1 3`, 수정본은 `2 3`이다. 첫 변경 줄 `q = deque([1, 2])`에서 `q = deque([2, 2])`로 첫 값을 바꾸면, 앞에서 꺼내는 값만 2로 바뀌고 뒤에서 꺼내는 3은 그대로이다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(q.pop(), q.pop())`입니다. 두 번 다 맨 뒤에서 꺼내므로 3과 2가 순서대로 나와 `3 2`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 앞쪽 꺼내기가 사라진 첫 순간이 원인입니다. 고친 줄 `print(q.popleft(), q.pop())`에서는 `자료구조의 제거 위치 구분` 동작이 지켜집니다. 정상 코드는 앞 제거→뒤 제거→둘 다 출력의 순서로 `1 3`을 출력합니다.',
        'qout': '먼저 q에 [1,2,3]이 들어 있습니다. 이어서 앞에서 1을 꺼내고 뒤에서 3을 꺼내 함께 출력하므로 `1 3`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 덱 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '앞에서 popleft 하면 가장 오래된 1, 뒤에서 pop 하면 가장 최근 3을 꺼낸다는 뜻은, 꺼내는 쪽이 의미를 정한다는 뜻입니다. `큐 처리와 스택 처리를 같은 제거 연산으로 착각함`은 반대 사례인데, 같은 연산으로는 양쪽 끝을 구별할 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `큐 처리와 스택 처리를 같은 제거 연산으로 착각함`입니다. 잘못된 코드는 실행은 되지만 `3 2`라는 잘못된 출력을 냅니다. 어느 끝에서 꺼내는지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `자료구조의 제거 위치 구분`이라는 의미와 입력·출력 계약입니다. 스택 끝 연산과 큐 양쪽 끝 연산으로 같은 동작을 구현하고, 실행 결과 `1 3`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 상태에서 꺼내는 동작도 정의해야 합니다. 1·2·3을 실제로 넣고 꺼내 확인하세요.',
    },
    64: {
        'notes': [
            '트리는 하나의 부모에서 자식으로 뻗는 구조입니다. 노드 수를 세는 재귀 함수는 `None`이면 0, 실제 노드이면 `1 + 왼쪽 수 + 오른쪽 수`를 돌려줍니다. 뿌리 A에 자식 B와 C가 있으면 B=1, C=1, A=1+1+1=3으로 돌아옵니다.',
            '자식이 한쪽만 있어도 빈 쪽을 0으로 계산합니다. `None`에서 멈추지 않으면 없는 노드의 자식을 계속 읽으려 합니다. 실제 노드를 방문하기 전과 하위 노드의 수가 돌아온 후를 다른 순서로 적어 보세요.',
        ],
        'predict': '1. A가 자신 1을 셉니다. 2. B와 C가 각각 1을 셉니다. 3. 빈 자식들은 0이므로 합계 3입니다. `B=1 → C=1 → A=1+1+1=3` 흐름으로 실제 출력 `3`이 됩니다. 핵심 `node is None`은 빈 자식에서 멈추는 바닥을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `3`, 수정본은 `7`이다. 첫 변경 줄의 `return 0`에서 `return 1`로 빈 자식의 값을 바꾸면, 네 군데 빈 자식이 각각 1씩 더해져 3에서 7이 된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `return 0 if node == 0 else 1 + count(node[1]) + count(node[2])`입니다. 트리의 노드는 튜플이거나 `None`이라 `== 0`이 참이 되는 경우가 없으므로, 빈 자식에서도 멈추지 않고 `None[1]`을 읽으려다 TypeError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 빈 자식의 종료 조건입니다. 고친 줄 `return 0 if node is None else 1 + count(node[1]) + count(node[2])`에서는 `하위 트리 결과를 합산` 동작이 지켜집니다. 정상 코드는 바닥 판정→하위 합산→돌아오며 더하기의 순서로 `3`을 출력합니다.',
        'qout': '먼저 A가 자신 1을 셉니다. 이어서 B와 C가 각각 1을 세고, 빈 자식들은 0이므로 합쳐 3이 되어 출력 `3`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 재귀와 종료가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '빈 자식은 0개라는 뜻이고, 실제 노드는 자신 1개와 왼쪽·오른쪽 하위 트리 개수를 더한다는 뜻은 아래에서 올라오며 합산한다는 뜻입니다. `빈 자식의 종료 조건을 빠뜨려 None을 인덱싱함`은 반대 사례인데, 멈추지 않으면 없는 노드를 읽기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `빈 자식의 종료 조건을 빠뜨려 None을 인덱싱함`입니다. 잘못된 코드는 빈 자식에서 멈추지 못해 TypeError로 멈춥니다. `is None` 판정을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `하위 트리 결과를 합산`이라는 의미와 입력·출력 계약입니다. C의 재귀 순회와 Rust의 재귀 매칭으로 같은 동작을 구현하고, 실행 결과 `3`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '자식이 한쪽만 있어도 빈 쪽을 0으로 계산합니다. 방문 전과 합산 후를 다른 순서로 적어 보세요.',
    },
    65: {
        'notes': [
            '이진 탐색 트리는 현재 값보다 작은 값을 왼쪽, 큰 값을 오른쪽에 둔다는 규칙을 지킵니다. 뿌리가 10이고 왼쪽 5, 오른쪽 15라면 15를 찾을 때 10과 비교해 오른쪽 한 번만 따라가면 됩니다.',
            '하지만 모양이 한쪽으로 길게 치우치면 거의 모든 노드를 살펴야 할 수도 있습니다. 항상 빠르다는 뜻이 아닙니다. 찾는 값이 같을 때 멈추고, 자식이 `None`이면 없다고 답합니다. 중복 키를 왼쪽·오른쪽·별도 카운트 중 어떻게 처리할지도 트리의 규칙에 넣어야 합니다.',
        ],
        'predict': '1. 루트 8과 목표 10을 비교합니다. 2. 10>8이라 오른쪽 자식 10으로 이동합니다. 3. 목표와 같아 True입니다. `root=8 → 오른쪽=10 → 발견` 흐름으로 실제 출력 `True`가 됩니다. 핵심 `key > node`는 어느 가지로 갈지 정하는 자리에 쓰입니다.',
        'guided': '원본은 `True`, 수정본은 `False`이다. 첫 변경 줄 `key = 10`에서 `key = 9`로 목표를 바꾸면, 오른쪽으로 이동한 10과 목표 9가 달라 거짓이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `if key < node: node = tree[node][1]`입니다. 10<8이 거짓이므로 이동하지 않고 루트 8이 목표 10과 달라 `False`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 가지 선택이 어긋나는 첫 순간은 비교 방향입니다. 고친 줄 `if key > node: node = tree[node][1]`에서는 `키 비교로 가지 하나 선택` 동작이 지켜집니다. 정렬 규칙이 깨진 트리에 이분 탐색을 그대로 쓰면 답을 보장하지 못합니다. 정상 코드는 비교→오른쪽 이동→일치 확인의 순서로 `True`를 출력합니다.',
        'qout': '먼저 루트 8과 목표 10을 비교합니다. 이어서 10>8이라 오른쪽 자식 10으로 이동하고, 목표와 같아 출력 `True`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 비교와 이동이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '현재 노드보다 작은 키는 왼쪽, 큰 키는 오른쪽이라는 뜻은 비교 한 번에 한쪽 가지를 버린다는 뜻입니다. `정렬 불변식 없이 아무 트리에도 이분 탐색을 적용함`은 반대 사례인데, 규칙이 깨진 위치의 값은 경로 탐색이 보장하지 못하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `정렬 불변식 없이 아무 트리에도 이분 탐색을 적용함`입니다. 잘못된 코드는 실행은 되지만 `False`라는 잘못된 출력을 냅니다. 비교 방향을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `키 비교로 가지 하나 선택`이라는 의미와 입력·출력 계약입니다. C의 포인터 이동과 Rust의 매칭 이동으로 같은 동작을 구현하고, 실행 결과 `True`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '찾는 값이 같을 때 멈추고, 자식이 `None`이면 없다고 답합니다. 중복 키 정책을 트리 규칙에 넣으세요.',
    },
    66: {
        'notes': [
            '최소 힙은 부모가 자식보다 크지 않다는 규칙을 지킵니다. 이것은 전체 배열이 정렬됐다는 뜻은 아닙니다. 가장 작은 값이 맨 앞에 있다는 것만 우선 보장합니다. `[5,1,3]`에 `heapify`를 한 뒤 하나씩 꺼내면 1, 3, 5 순서입니다.',
            '우선순위 큐는 먼저 들어온 순서가 아니라 가장 높은 우선순위부터 꺼냅니다. Python `heapq`는 작은 수가 우선입니다. 빈 힙에서 `heappop`하면 오류가 나므로 항목이 남아 있는지 확인하세요. 동점인 작업의 순서가 중요하면 보조 순번을 함께 넣는 방법도 필요합니다.',
        ],
        'predict': '1. 세 우선순위를 힙으로 재배열합니다. 2. 루트가 최소 10입니다. 3. heappop이 10을 제거·반환합니다. `[30,10,20] → heapify 최소 루트 10 → pop 10` 흐름으로 실제 출력 `10`이 됩니다. 핵심 `heapify(waiting)`은 리스트를 힙 규칙으로 다시 엮는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `10`을 출력한다. 바꾼 31이 최솟값이 아니라 꺼내는 값은 그대로 10입니다. 최솟값이 달라지지 않았으므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `heapq.waiting.sort()`입니다. `heapq` 모듈에 `waiting`이라는 속성이 없으므로 첫 실행에서 AttributeError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 존재하지 않는 모듈 속성을 읽는 줄 자체입니다. `sort`와 `heapify`는 결과 배열이 우연히 비슷해 보여도 계약이 다릅니다. heap의 보장은 전체 정렬이 아니라 부모가 자식보다 우선되는 부분 순서이며 `heappop`과 함께 씁니다. 고친 줄 `heapq.heapify(waiting)`에서는 `최소 우선순위 추출` 동작이 지켜집니다. 정상 코드는 힙 구성→최소 루트 확정→꺼내기의 순서로 `10`을 출력합니다.',
        'qout': '먼저 [30,10,20]이 힙 규칙으로 재배열됩니다. 이어서 루트 최소값 10이 확정되고, heappop이 꺼내 출력하므로 `10`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 힙 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '최소 힙은 부모가 자식 이하라는 규칙을 유지한다는 뜻이고, 루트에서 최소값을 꺼낸다는 뜻은 전체 정렬 없이 최소만 보장한다는 뜻입니다. `힙 내부 배열 전체가 정렬돼 있다고 생각함`은 반대 사례인데, 정렬된 목록이 필요하면 하나씩 꺼내야 하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `힙 내부 배열 전체가 정렬돼 있다고 생각함`입니다. 잘못된 코드는 존재하지 않는 속성에 접근해 AttributeError로 멈춥니다. 정렬과 힙 규칙을 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `최소 우선순위 추출`이라는 의미와 입력·출력 계약입니다. 정렬 뒤 꺼내기와 힙 꺼내기로 같은 동작을 구현하고, 실행 결과 `10`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 힙에서 `heappop`하면 오류가 납니다. 동점 작업의 순서는 보조 순번으로 정하세요.',
    },
    67: {
        'notes': [
            '해시 맵인 Python `dict`는 주제 이름을 키로 써서 횟수를 저장합니다. 처음 Python을 만나면 `counts.get("Python", 0)`은 기존 값이 없어 0을 주고, 1을 더해 저장합니다. 다음 번에는 기존 값 1을 읽고 2로 갱신합니다.',
            '`get`의 두 번째 인수 0은 실제로 저장한 0이 아니라 키가 없을 때 쓸 기본값입니다. 같은 주제를 두 번 세지 않으려는 문제라면 사전의 횟수 대신 집합을 써야 합니다. 각 입력 뒤 사전의 키와 값을 한 줄씩 적어 보세요.',
        ],
        'predict': '1. 처음 C는 0+1=1입니다. 2. Rust도 1입니다. 3. 다시 C를 만나 1+1=2입니다. `C:0→1 → Rust:0→1 → C:1→2` 흐름으로 실제 출력 `2`가 됩니다. 핵심 `get(topic, 0)`은 없을 때 쓸 기본값을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `2`, 수정본은 `3`이다. 첫 변경 줄의 기본값 `0`에서 `1`로 바꾸면, 첫 등장에서 1+1=2가 저장되고 다시 만나 2+1=3이 되어 C의 횟수가 달라진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `counts[topic] = counts.get(topic, 1) + 1`입니다. 첫 등장에서 기본값 1에 또 1을 더하므로 C는 2에서 시작해 마지막에 3이 됩니다. 오류 분류는 잘못된 출력입니다. 횟수가 어긋나는 첫 순간은 첫 등장 처리입니다. 고친 줄 `counts[topic] = counts.get(topic, 0) + 1`에서는 `키별 빈도 누적` 동작이 지켜집니다. 정상 코드는 첫 등장 0 시작→재등장 누적→C 횟수 읽기의 순서로 `2`를 출력합니다.',
        'qout': '먼저 C를 만나 기본값 0에 1을 더해 1을 저장합니다. 이어서 Rust도 1이 되고, 다시 C를 만나 1+1=2가 되므로 출력 `2`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 딕셔너리 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'dict는 키를 해시해 값을 찾는다는 뜻이고, 첫 등장 때 기본값 0으로 시작해 1씩 증가시킨다는 뜻은 없을 때 쓸 값을 정해 둔다는 뜻입니다. `첫 등장 키의 기본값을 1로 주고 다시 1을 더함`은 반대 사례인데, 시작값이 어긋나면 이후 누적이 전부 어긋나기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `첫 등장 키의 기본값을 1로 주고 다시 1을 더함`입니다. 잘못된 코드는 실행은 되지만 `3`이라는 잘못된 출력을 냅니다. 기본값과 증가분을 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `키별 빈도 누적`이라는 의미와 입력·출력 계약입니다. C의 해시 테이블과 Rust의 HashMap으로 같은 동작을 구현하고, 실행 결과 `2`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '같은 주제를 두 번 세지 않으려면 사전 대신 집합을 쓰세요. 각 입력 뒤 키와 값을 적어 보세요.',
    },
    68: {
        'notes': [
            '그래프는 정점과 정점 사이의 연결을 나타냅니다. `edges["A"] = ["B","C"]`라면 A에서 바로 갈 수 있는 곳은 B와 C입니다. 이웃 목록에는 한 번 건너가서 도착할 수 있는 정점만 적고 먼 정점은 탐색으로 찾습니다.',
            '방향 있는 그래프에서 A→B라고 해서 B→A가 생기지는 않습니다. 양방향 도로라면 양쪽 목록에 모두 기록해야 합니다. 연결이 전혀 없는 정점도 키에 빈 리스트로 넣어 두면 탐색할 때 빠뜨리지 않습니다. 순서가 의미 있는지도 문제에 따라 정하세요.',
        ],
        'predict': '1. A의 이웃을 B,C로 등록합니다. 2. B의 이웃은 C입니다. 3. A 목록을 읽어 두 이웃을 출력합니다. `A→B,C → B→C → A 이웃 B,C` 흐름으로 실제 출력 `["B", "C"]`가 됩니다. 핵심 `["A"]`는 어느 정점의 목록을 읽을지 정하는 자리에 쓰입니다.',
        'guided': '원본은 `["B", "C"]`, 수정본은 `["C"]`이다. 첫 변경 줄 `print(edges["A"])`에서 `print(edges["B"])`로 읽는 정점을 바꾸면, B의 이웃 하나만 출력된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(edges["C"])`입니다. C의 이웃은 빈 목록이므로 실행은 되지만 결과는 `[]`가 됩니다. 오류 분류는 잘못된 출력입니다. 읽는 정점이 어긋나는 첫 순간이 원인입니다. 고친 줄 `print(edges["A"])`에서는 `정점별 이웃 표현` 동작이 지켜집니다. 직접 간선 A→C와 길이 2 경로 A→B→C를 구별하세요. 정상 코드는 이웃 등록→A 목록 읽기→두 이웃 출력의 순서로 `["B", "C"]`를 출력합니다.',
        'qout': '먼저 A의 이웃 B,C가 등록됩니다. 이어서 B의 이웃 C가 등록되고, A 목록을 읽어 두 이웃을 출력하므로 `["B", "C"]`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 딕셔너리 읽기가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '인접 목록은 정점마다 바로 이어진 이웃의 목록을 저장한다는 뜻이고, 방향 그래프라면 A→B가 B→A를 뜻하지 않는다는 뜻은 한쪽 기록이 반대쪽을 만들지 않는다는 뜻입니다. `방향 간선을 역방향에도 자동으로 있다고 간주함`은 반대 사례인데, 적혀 있지 않은 방향으로는 갈 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `방향 간선을 역방향에도 자동으로 있다고 간주함`입니다. 잘못된 코드는 실행은 되지만 `[]`라는 잘못된 출력을 냅니다. 읽는 정점이 맞는지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `정점별 이웃 표현`이라는 의미와 입력·출력 계약입니다. C의 배열 이웃과 Rust의 벡터 이웃으로 같은 동작을 구현하고, 이웃 목록 출력으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '연결이 없는 정점도 빈 리스트로 넣어 두세요. 적혀 있지 않은 C→A 방향으로는 갈 수 없습니다.',
    },
    69: {
        'notes': [
            '너비 우선 탐색(BFS)은 가까운 정점부터 층별로 확인합니다. A에 B와 C가 연결되어 있으면 먼저 A를 꺼내 B,C를 큐에 넣고, 그다음 B,C를 처리합니다. 큐가 먼저 발견한 정점부터 꺼내는 순서를 지켜 줍니다.',
            '`seen`은 이미 넣은 정점을 기록합니다. A→B→A처럼 다시 돌아오는 간선이 있어도 A를 두 번 넣지 않게 합니다. 큐에 넣는 순간 방문 표시를 하면 같은 정점이 여러 경로로 발견되어도 한 번만 들어갑니다. BFS가 최단 간선 수를 구하는 것은 모든 간선의 비용이 같을 때입니다.',
        ],
        'predict': '1. A를 꺼냅니다. 2. 새 이웃 B,C를 방문 표시 후 큐에 넣습니다. 3. 둘을 처리하고 seen을 정렬해 출력합니다. `q=[A] → q=[B,C] → q=[] → seen={A,B,C}` 흐름으로 실제 출력 `["A", "B", "C"]`가 됩니다. 핵심 `q.popleft()`는 먼저 발견한 정점부터 꺼내는 자리에 쓰입니다.',
        'guided': '원본은 `["A", "B", "C"]`, 수정본은 `3`이다. 첫 변경 줄 `print(sorted(seen))`에서 `print(len(seen))`로 보여 줄 값을 바꾸면, 같은 집합에서 목록 대신 개수가 출력된다.',
        'debug_outcome': 'same-output-contract',
        'debug': '틀린 줄은 `node = q.pop()`입니다. 맨 뒤에서 꺼내므로 방문 순서가 너비가 아니라 깊이 쪽으로 바뀌지만, 이 그래프에서는 최종 집합이 같아 화면 출력이 같습니다. 오류 분류는 같은 출력이지만 숨은 계약 위반입니다. 층별 순서가 깨지는 첫 순간은 꺼내는 쪽입니다. 큐를 스택으로 바꾸면 층별 순서가 사라집니다. 고친 줄 `node = q.popleft()`에서는 `가까운 정점부터 탐색` 동작이 지켜집니다. 방문 순서를 검증하려면 순서 리스트를 출력하거나 최단 거리처럼 차이가 드러나는 결과를 쓰세요. 정상 코드는 꺼내기→이웃 발견→방문 표시의 순서로 세 정점을 모읍니다.',
        'qout': '먼저 A를 꺼냅니다. 이어서 새 이웃 B,C를 방문 표시 후 큐에 넣고, 둘을 처리한 뒤 seen을 정렬해 출력하므로 `["A", "B", "C"]`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 큐 연산과 집합 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '큐는 현재 거리의 정점을 먼저 처리한다는 뜻이고, seen은 순환 간선에서 같은 정점을 다시 넣지 않게 한다는 뜻은 넣는 순간 표시한다는 뜻입니다. `방문 표시를 늦춰 같은 노드가 큐에 여러 번 들어감`은 반대 사례인데, 표시가 늦으면 중복 삽입이 생기기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `방문 표시를 늦춰 같은 노드가 큐에 여러 번 들어감`입니다. 이번 잘못된 코드는 최종 집합이 같아 화면 출력이 같습니다. 같다고 해서 맞는 것이 아니라, 층별 순서가 깨진 숨은 계약 위반입니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `가까운 정점부터 탐색`이라는 의미와 입력·출력 계약입니다. 스택 깊이 탐색과 층별 큐 탐색으로 같은 동작을 구현하고, 방문 순서로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'BFS가 최단 간선 수를 구하는 것은 모든 간선의 비용이 같을 때입니다. 큐에 넣는 순간 표시하세요.',
    },
    70: {
        'notes': [
            '자료구조를 고를 때는 무엇을 먼저 꺼내야 하는가로 시작하세요. 접수 순서라면 큐, 마지막 편집 취소라면 스택, 이름으로 빠르게 합계를 찾는다면 사전, 중복 없는 방문 기록이라면 집합입니다.',
            '예를 들어 A,B,C가 접수됐는데 C가 먼저 처리되면 접수 순서 규칙을 어긴 것입니다. A를 먼저 꺼내는 큐의 동작을 손으로 확인하세요. 답을 저장하는 모양보다 유지해야 하는 규칙이 자료구조 선택의 근거입니다.',
        ],
        'predict': '1. first가 먼저 들어왔습니다. 2. second가 뒤에 있습니다. 3. popleft가 first를 처리합니다. `first 대기 → second 대기 → first 처리` 흐름으로 실제 출력 `first`가 됩니다. 핵심 `popleft()`는 접수 순서를 지키는 자리에 쓰입니다.',
        'guided': '원본은 `first`, 수정본은 `first!`이다. 첫 변경 줄 `work = deque(["first", "second"])`에서 `work = deque(["first!", "second"])`로 첫 값을 바꾸면, 앞에서 꺼내는 값이 그대로 달라진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(work.pop())`입니다. 맨 뒤의 second를 꺼내므로 실행은 되지만 결과는 `second`가 됩니다. 오류 분류는 잘못된 출력입니다. 접수 순서가 뒤집히는 첫 순간은 꺼내는 쪽입니다. 고친 줄 `print(work.popleft())`에서는 `처리 순서에 맞는 구조 선택` 동작이 지켜집니다. 정상 코드는 접수 순서 저장→맨 앞 처리→출력의 순서로 `first`를 출력합니다.',
        'qout': '먼저 first와 second가 순서대로 들어 있습니다. 이어서 popleft가 first를 꺼내 출력하므로 `first`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 덱 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '접수 순서 보존이 불변식이라면 앞에서 꺼내는 큐가 맞다는 뜻은, 규칙이 구조를 정한다는 뜻입니다. `최근 항목 pop을 써서 접수 순서를 뒤집음`은 반대 사례인데, 맨 뒤를 꺼내면 규칙을 어기기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `최근 항목 pop을 써서 접수 순서를 뒤집음`입니다. 잘못된 코드는 실행은 되지만 `second`라는 잘못된 출력을 냅니다. 무엇을 먼저 꺼내야 하는지 먼저 물으세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `처리 순서에 맞는 구조 선택`이라는 의미와 입력·출력 계약입니다. 스택·사전·집합 후보와 비교해 같은 동작을 구현하고, 실행 결과 `first`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'A,B,C가 접수됐는데 C가 먼저 처리되면 규칙 위반입니다. 답 모양보다 유지할 규칙을 먼저 정하세요.',
    },
    71: {
        'notes': [
            '시간 복잡도는 입력이 커질 때 작업 수가 어떻게 늘어나는지를 설명합니다. 원소 3개를 한 번씩 보면 3번, 6개를 보면 6번이므로 `O(n)`입니다. 중첩 반복으로 모든 두 원소를 비교하면 대략 n×n번이라 `O(n²)`이 됩니다.',
            '`O(n)`은 정확히 n초 걸린다는 뜻이 아닙니다. 실행 환경과 각 작업의 비용을 생략하고 증가하는 방식을 보는 표기입니다. 예제에서 `steps += 1`이 어떤 반복 안에 있는지 보고 n이 0, 1, 5일 때 몇 번 실행되는지 표로 적어 보세요.',
        ],
        'predict': '1. 배열 길이는 4입니다. 2. 각 원소를 방문할 때 steps를 1 올립니다. 3. 4번 방문 후 4를 출력합니다. `0 → 1 → 2 → 3 → 4` 흐름으로 실제 출력 `4`가 됩니다. 핵심 `steps += 1`은 방문 한 번당 하나를 세는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `4`를 출력한다. 세는 것은 원소 개수라 들어 있는 값과 무관해 결과가 같습니다. 개수가 달라지지 않았으므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `steps += 2`입니다. 반복은 네 번 실행되지만 카운터에 2씩 쌓여 `8`이 출력됩니다. 오류 분류는 잘못된 출력입니다. 개수가 어긋나는 첫 순간은 증가폭입니다. 복잡도와 실제 카운터 값을 분리하세요. 반복 횟수 4회는 그대로이고 잘못된 것은 카운터뿐입니다. `O(n)은 n초`라는 오해와 억지로 연결하지 않습니다. 고친 줄 `steps += 1`에서는 `입력 크기와 작업 수의 증가율 파악` 동작이 지켜집니다. 정상 코드는 방문→1씩 누적→4 출력의 순서로 `4`를 출력합니다.',
        'qout': '먼저 길이가 4인 배열이 준비됩니다. 이어서 각 원소를 방문할 때마다 steps를 1씩 올려 4가 되고, 출력하므로 `4`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 반복문 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '원소 n개를 한 번씩 방문하면 작업 수가 n에 비례하므로 O(n)이라는 뜻이고, 실제 시간과 상수는 따로 측정한다는 뜻은 표기가 증가 방식만 본다는 뜻입니다. `O(n)을 실제 실행 시간이 정확히 n초라는 뜻으로 착각함`은 반대 사례인데, O 표기는 환경과 상수를 생략하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `O(n)을 실제 실행 시간이 정확히 n초라는 뜻으로 착각함`입니다. 잘못된 코드는 실행은 되지만 `8`이라는 잘못된 출력을 냅니다. 반복 횟수와 카운터 값을 분리하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `입력 크기와 작업 수의 증가율 파악`이라는 의미와 입력·출력 계약입니다. C의 반복 계수와 Rust의 반복 계수로 같은 동작을 구현하고, 실행 결과 `4`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'n이 0, 1, 5일 때 몇 번 실행되는지 표로 적어 보세요. `O(n)`은 n초라는 뜻이 아닙니다.',
    },
    72: {
        'notes': [
            '이진 탐색은 미리 정렬된 데이터에서만 좌우 절반을 버릴 수 있습니다. `[10,20,20,30]`에서 20의 첫 위치를 찾는다면 결과는 1입니다. 범위 `[lo,hi)`는 lo를 포함하고 hi는 제외합니다. 가운데 `mid`가 목표보다 작으면 `lo=mid+1`, 그렇지 않으면 `hi=mid`로 왼쪽 가능성을 남깁니다.',
            '같은 값이 여러 개 있을 때 첫 위치와 아무 위치는 다른 문제입니다. 반복이 끝나 `lo==hi`라도 그 칸이 실제로 목표인지 따로 확인해야 합니다. 비어 있는 배열에서는 처음부터 `lo==hi==0`입니다. 정렬이 깨진 입력에는 이 방식의 정답을 약속할 수 없습니다.',
        ],
        'predict': '1. 중간 인덱스 2의 값 6이 작아 lo=3입니다. 2. 새 중간 4의 값 10은 크므로 hi=4입니다. 3. 인덱스 3의 8에서 수렴합니다. `[0,5) → [3,5) → [3,4) → 위치 3` 흐름으로 실제 출력 `3`이 됩니다. 핵심 `lo = mid + 1`은 작은 쪽 절반을 버리는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `3`을 출력한다. 목표 8의 위치는 그대로 3이라 결과가 같습니다. 목표 위치가 달라지지 않았으므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'infinite-loop',
        'debug': '틀린 줄은 `if values[mid] < target: lo = mid`입니다. mid가 lo와 같아지는 순간 탐색 범위가 줄지 않아 같은 계산을 영원히 반복하므로 끝나지 않습니다. 오류 분류는 무한 반복입니다. 단순히 출력값만 다르다고 쓰면 안 됩니다. lo·hi·mid 표를 보면, 범위가 감소하지 않는 지점이 원인입니다. 고친 줄 `if values[mid] < target: lo = mid + 1`에서는 `정렬된 범위를 반으로 축소` 동작이 지켜집니다. 정상 코드는 절반 버리기→범위 축소→위치 수렴의 순서로 `3`을 출력합니다.',
        'qout': '먼저 [0,5) 범위에서 중간 2의 값 6이 작아 lo=3이 됩니다. 이어서 [3,5)에서 중간 4의 값 10이 크므로 hi=4가 되고, 인덱스 3의 8에서 수렴하므로 출력 `3`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 범위 축소가 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '반열린 범위 [lo,hi)에서 target 이상이 처음 나타나는 위치를 유지한다는 뜻은, 매 단계마다 답의 후보 범위를 좁힌다는 뜻입니다. `정렬되지 않은 배열에 이진 탐색을 적용함`은 반대 사례인데, 정렬이 깨지면 절반 버리기가 정답을 보장하지 못하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `정렬되지 않은 배열에 이진 탐색을 적용함`입니다. 잘못된 코드는 범위가 줄지 않아 끝나지 않습니다. lo·hi·mid 표로 구간 감소를 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `정렬된 범위를 반으로 축소`라는 의미와 입력·출력 계약입니다. C의 인덱스 이동과 Rust의 슬라이스 축소로 같은 동작을 구현하고, 실행 결과 `3`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '비어 있는 배열에서는 처음부터 `lo==hi==0`입니다. 끝난 뒤 그 칸이 실제 목표인지 따로 확인하세요.',
    },
    73: {
        'notes': [
            '삽입 정렬은 왼쪽 부분이 이미 정렬돼 있다고 믿고 새 값을 알맞은 자리에 끼워 넣습니다. `[3,1,2]`에서 1을 꺼내 3을 오른쪽으로 밀면 `[1,3,2]`, 다음 2를 꺼내 3을 밀면 `[1,2,3]`이 됩니다.',
            '`key`보다 큰 값을 오른쪽으로 옮긴다는 조건을 지키면 왼쪽의 정렬된 부분이 한 칸씩 커집니다. 이미 정렬된 입력은 거의 움직이지 않지만 역순 입력은 많이 이동합니다. 바깥 반복 시작마다 앞부분은 정렬돼 있다는 약속을 직접 확인하세요.',
        ],
        'predict': '1. 첫 key 1이 3 앞에 들어갑니다. 2. 다음 key 2가 1과 3 사이에 들어갑니다. 3. 전체가 [1,2,3]입니다. `[3,1,2] → [1,3,2] → [1,2,3]` 흐름으로 실제 출력 `[1, 2, 3]`이 됩니다. 핵심 `values[j] > key`는 큰 값을 오른쪽으로 미는 조건 자리에 쓰입니다.',
        'guided': '원본은 `[1, 2, 3]`, 수정본은 `[1, 2, 4]`이다. 첫 변경 줄 `values = [3, 1, 2]`에서 `values = [4, 1, 2]`로 첫 값을 바꾸면, 끼워 넣기 과정이 그대로 이어져 마지막 자리에 새 값이 들어간다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `while j >= 0 and values[j] < key:`입니다. 큰 값 대신 작은 값을 오른쪽으로 밀어 내림차순으로 정렬되므로 실행은 되지만 결과는 `[3, 2, 1]`이 됩니다. 오류 분류는 잘못된 출력입니다. 정렬 방향이 뒤집히는 첫 순간은 비교 방향입니다. 고친 줄 `while j >= 0 and values[j] > key:`에서는 `정렬된 접두부 불변식 유지` 동작이 지켜집니다. 정상 코드는 key 꺼내기→큰 값 밀기→빈자리 삽입의 순서로 `[1, 2, 3]`을 출력합니다.',
        'qout': '먼저 첫 key 1이 3 앞으로 들어갑니다. 이어서 다음 key 2가 1과 3 사이에 들어가 전체가 [1,2,3]이 되므로 출력 `[1, 2, 3]`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 반복과 삽입이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '반복 시작 때 [0,i)는 정렬돼 있다는 뜻이고, key보다 큰 값만 한 칸씩 오른쪽으로 민다는 뜻은 왼쪽 약속을 깨지 않는다는 뜻입니다. `비교 방향을 반대로 써 내림차순으로 정렬함`은 반대 사례인데, 방향이 뒤집히면 약속이 깨지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `비교 방향을 반대로 써 내림차순으로 정렬함`입니다. 잘못된 코드는 실행은 되지만 `[3, 2, 1]`이라는 잘못된 출력을 냅니다. 비교 방향을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `정렬된 접두부 불변식 유지`라는 의미와 입력·출력 계약입니다. C의 배열 삽입과 Rust의 벡터 삽입으로 같은 동작을 구현하고, 실행 결과 `[1, 2, 3]`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '이미 정렬된 입력은 거의 움직이지 않지만 역순 입력은 많이 이동합니다. 바깥 반복 시작마다 약속을 확인하세요.',
    },
    74: {
        'notes': [
            '병합 정렬은 큰 목록을 반으로 나눠 작은 목록을 먼저 정렬하고, 정렬된 두 목록을 합칩니다. `[4,1,3,2]`는 `[4,1]`과 `[3,2]`로 나뉘고 `[1,4]`, `[2,3]`으로 정렬된 뒤 `[1,2,3,4]`로 합쳐집니다.',
            '합칠 때는 각 부분의 맨 앞 값만 비교하면 됩니다. 한쪽이 먼저 비면 남은 쪽을 그대로 붙입니다. 원소가 하나 이하일 때 멈추는 것이 재귀의 바닥입니다. 시간은 보통 `O(n log n)`이고 병합을 위한 추가 공간이 필요합니다. 같은 값의 순서를 유지하려면 같을 때 왼쪽 값을 먼저 뽑으세요.',
        ],
        'predict': '1. 길이 2 미만을 기저로 둡니다. 2. [4,1]과 [3,2]를 각각 [1,4], [2,3]으로 정렬합니다. 3. 두 부분의 앞 원소를 비교해 작은 값을 옮깁니다. 4. 남은 부분을 붙여 최종 결과를 만듭니다. `[4,1,3,2] → [1,4]+[2,3] → [1,2,3,4]` 흐름으로 실제 출력 `[1, 2, 3, 4]`가 됩니다. 핵심 `a[i] <= b[j]`는 앞 원소 중 작은 쪽을 고르는 자리에 쓰입니다.',
        'guided': '원본은 `[1, 2, 3, 4]`, 수정본은 `[3, 2, 4, 1]`이다. 첫 변경 줄의 기저 조건 `< 2`에서 `< 3`으로 바꾸면, 길이 2 부분도 정렬 없이 그대로 돌아 병합 순서가 어긋난다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `if a[i] >= b[j]: out.append(a[i]); i += 1`입니다. 크거나 같은 조건에서 왼쪽을 뽑으므로 작은 값부터 쌓이지 않고 순서가 어긋나 실행은 되지만 `[4, 3, 2, 1]`이라는 정렬되지 않은 결과가 됩니다. 오류 분류는 잘못된 출력입니다. 병합 순서가 어긋나는 첫 순간은 앞 원소 비교입니다. 고친 줄 `if a[i] <= b[j]: out.append(a[i]); i += 1`에서는 `분할한 부분을 정렬해 병합` 동작이 지켜집니다. 정상 코드는 분할→부분 정렬→앞 비교 병합→꼬리 연결의 순서로 `[1, 2, 3, 4]`를 출력합니다.',
        'qout': '먼저 [4,1]과 [3,2]가 각각 [1,4], [2,3]으로 정렬됩니다. 이어서 앞 원소를 비교해 작은 값을 옮기고 남은 꼬리를 붙여 [1,2,3,4]가 되므로 출력 `[1, 2, 3, 4]`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 분할과 병합이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '길이가 1 이하인 부분은 이미 정렬됐다는 뜻이고, 나머지는 반으로 나눈 뒤 두 정렬된 부분의 앞 원소를 비교하며 병합한다는 뜻은 작은 쪽부터 쌓는다는 뜻입니다. `부분 배열의 정렬이 끝나기 전에 병합하거나 남은 꼬리를 빠뜨림`은 반대 사례인데, 순서가 어긋나거나 값이 빠지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `부분 배열의 정렬이 끝나기 전에 병합하거나 남은 꼬리를 빠뜨림`입니다. 잘못된 코드는 실행은 되지만 정렬되지 않은 잘못된 출력을 냅니다. 비교 방향과 꼬리 연결을 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `분할한 부분을 정렬해 병합`이라는 의미와 입력·출력 계약입니다. C의 배열 병합과 Rust의 벡터 병합으로 같은 동작을 구현하고, 실행 결과 `[1, 2, 3, 4]`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '원소가 하나 이하일 때 멈추는 것이 재귀의 바닥입니다. 같을 때 왼쪽 값을 먼저 뽑아야 순서가 유지됩니다.',
    },
    75: {
        'notes': [
            '퀵 정렬은 피벗 하나를 정하고 나머지를 피벗보다 작은 쪽과 크거나 같은 쪽으로 분리합니다. `[3,1,4,2]`에서 피벗 3이면 작은 쪽 `[1,2]`, 큰 쪽 `[4]`입니다. 양쪽을 다시 정렬한 뒤 작은 쪽 + 피벗 + 큰 쪽으로 합칩니다.',
            '이해용 예제처럼 새 리스트를 만들면 제자리 정렬 구현과 공간 사용이 다릅니다. 피벗이 매번 최솟값이 되는 입력에서는 분할이 한쪽으로 치우쳐 최악 `O(n²)`이 될 수 있습니다. 빈 부분과 한 원소 부분에서 재귀가 끝나는지도 확인하세요.',
        ],
        'predict': '1. 원소가 하나면 그대로 반환합니다. 2. 피벗 4 아래에 1,3,2,3이 모입니다. 3. 다음 피벗 1·3이 각각 부분을 나눕니다. 4. 재귀 결과를 합쳐 중복 3을 모두 유지합니다. `[4,1,3,2,3] → 피벗4 → 피벗3 → [1,2,3,3,4]` 흐름으로 실제 출력 `[1, 2, 3, 3, 4]`가 됩니다. 핵심 `v >= pivot`은 같은 값을 큰 쪽에 남기는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `[1, 2, 3, 3, 4]`를 출력한다. 길이 3인 부분도 그대로 분할되므로 정렬 결과가 같습니다. 기저 조건만 달라지고 분할은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `more = [v for v in values[1:] if v > pivot]`입니다. 피벗과 같은 값이 어느 쪽에도 들어가지 못해 중복 3 하나가 사라지고 `[1, 2, 3, 4]`가 됩니다. 오류 분류는 잘못된 출력입니다. 값이 사라지는 첫 순간은 같은 값의 분류입니다. 고친 줄 `more = [v for v in values[1:] if v >= pivot]`에서는 `피벗 기준 분할 후 재귀적으로 정렬` 동작이 지켜집니다. 정상 코드는 피벗 선택→양쪽 분할→재귀 정렬→합치기의 순서로 `[1, 2, 3, 3, 4]`를 출력합니다.',
        'qout': '먼저 피벗 4 아래에 1,3,2,3이 모입니다. 이어서 다음 피벗들이 부분을 나누고, 재귀 결과를 합쳐 중복 3을 모두 유지하므로 출력 `[1, 2, 3, 3, 4]`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 분할과 재귀가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '첫 값을 피벗으로 뽑고 나머지를 작은 값과 이상인 값으로 나눈다는 뜻은, 같은 값을 큰 쪽에 남긴다는 뜻입니다. `피벗과 같은 값을 두 그룹 모두에서 제외해 중복 원소를 잃음`은 반대 사례인데, 어느 쪽에도 못 들어가면 값이 사라지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `피벗과 같은 값을 두 그룹 모두에서 제외해 중복 원소를 잃음`입니다. 잘못된 코드는 실행은 되지만 `[1, 2, 3, 4]`라는 잘못된 출력을 냅니다. 같은 값의 행방을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `피벗 기준 분할 후 재귀적으로 정렬`이라는 의미와 입력·출력 계약입니다. C의 제자리 분할과 Rust의 분할 수집으로 같은 동작을 구현하고, 실행 결과 `[1, 2, 3, 3, 4]`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 부분과 한 원소 부분에서 재귀가 끝나는지 확인하세요. 피벗이 매번 최솟값이면 최악 `O(n²)`이 됩니다.',
    },
    76: {
        'notes': [
            '분할 정복의 순서는 나눈다 → 작은 문제를 해결한다 → 답을 합친다입니다. 최댓값 문제에서 `[4,9,2,7]`을 `[4,9]`, `[2,7]`로 나누고 각 답 9,7을 구한 다음 큰 값 9를 택합니다.',
            '원소 하나면 그 값이 답이므로 재귀가 멈춥니다. 빈 입력에는 최댓값이 없으니 함수가 어떤 오류나 별도 결과를 돌릴지 정해야 합니다. 한 번 전체를 훑는 방식과 비교하면 이 분할 방식이 무조건 빠른 것은 아닙니다. 여기서는 재귀 문제를 나누고 합치는 방법을 배우는 것이 목적입니다.',
        ],
        'predict': '1. [3,9]의 최대는 9입니다. 2. [2,7]의 최대는 7입니다. 3. 둘을 비교해 9를 반환합니다. `[3,9,2,7] → max(3,9)=9 + max(2,7)=7 → 9` 흐름으로 실제 출력 `9`가 됩니다. 핵심 `len(values) == 1`은 더 나눌 수 없는 바닥을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `9`, 수정본은 `3`이다. 첫 변경 줄의 기저 조건 `== 1`에서 `== 2`로 바꾸면, 길이 2 부분도 그대로 첫 원소를 돌려주므로 각 절반의 첫 값 3과 2 중 큰 3이 된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `if len(values) == 0: return values[0]`입니다. 길이가 0인 호출에서 빈 목록의 0번을 읽으려 하므로 IndexError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 빈 입력의 기저 조건입니다. 고친 줄 `if len(values) == 1: return values[0]`에서는 `부분 답을 결합해 전체 답 생성` 동작이 지켜집니다. 빈 입력 처리 정책 없이 길이 0의 배열도 호출하면 안 됩니다. 정상 코드는 절반 분할→바닥 값→큰 값 선택의 순서로 `9`를 출력합니다.',
        'qout': '먼저 [3,9]와 [2,7]로 나뉩니다. 이어서 각 답 9와 7을 구한 뒤 큰 값 9를 택하므로 출력 `9`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 분할과 기저가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '원소 하나인 부분은 답이 바로 그 값이라는 뜻이고, 두 절반의 최대 중 큰 것이 전체 최대라는 뜻은 합치기가 비교라는 뜻입니다. `빈 입력 처리 정책 없이 길이 0의 배열도 호출함`은 반대 사례인데, 빈 입력에는 최댓값이 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `빈 입력 처리 정책 없이 길이 0의 배열도 호출함`입니다. 잘못된 코드는 빈 목록을 읽어 IndexError로 멈춥니다. 바닥 조건을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `부분 답을 결합해 전체 답 생성`이라는 의미와 입력·출력 계약입니다. C의 재귀 분할과 Rust의 재귀 결합으로 같은 동작을 구현하고, 실행 결과 `9`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 입력에는 최댓값이 없으니 오류나 별도 결과를 정하세요. 분할이 무조건 빠른 것은 아닙니다.',
    },
    77: {
        'notes': [
            '찾으려는 값이 있는지 한 번만 확인한다면 원본을 순회하는 편이 간단할 수 있습니다. 여러 번 탐색한다면 먼저 정렬하고 이진 탐색을 쓰는 선택도 가능합니다. `sorted(values)`는 새 리스트를 만들고 `values.sort()`는 원래 리스트를 바꿉니다.',
            '`sorted_values.index(target)`은 같은 값이 여러 개 있으면 첫 위치를 돌려주지만, 없으면 예외가 납니다. 값이 원래 배열의 몇 번째에 있었는지가 중요하면 정렬 뒤의 인덱스를 원본 인덱스와 혼동하면 안 됩니다. 데이터 크기와 탐색 횟수를 함께 생각하세요.',
        ],
        'predict': '1. 입력 [8,2,6,4]가 정렬되어 [2,4,6,8]이 됩니다. 2. 6을 찾으면 인덱스 2입니다. 마지막에는 `원본 → [2,4,6,8] → 6은 2번` 흐름과 실제 출력 `2`를 연결합니다. 핵심 `sorted(`는 새 정렬 리스트를 만드는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `2`를 출력한다. 6의 위치는 그대로 2라 결과가 같습니다. 목표값의 위치가 달라지지 않았으므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(values.index(8))`입니다. 바로 앞 줄 `sorted(values)`가 새 리스트를 반환하지만 반환값을 저장하지 않아 `values`는 `[8,2,6,4]` 그대로이므로, 8의 위치는 0이 출력됩니다. 오류 분류는 잘못된 출력입니다. 정렬 결과가 버려지는 첫 순간이 원인입니다. 고친 줄 `values = sorted([8, 2, 6, 4])`와 `print(values.index(8))`에서는 정렬 뒤 위치를 읽는 동작이 지켜져 `3`이 됩니다. `list([8, 2, 6, 4]).index(6)`과 `sorted([8, 2, 6, 4]).index(6)`은 둘 다 2이므로, 기존의 정렬하지 않으면 2가 나오지 않는다는 설명은 틀렸다. 정상 코드는 정렬 저장→위치 읽기→출력의 순서로 동작합니다.',
        'qout': '먼저 입력 [8,2,6,4]가 정렬되어 [2,4,6,8]이 됩니다. 이어서 6을 찾으면 인덱스 2이므로 출력 `2`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 정렬과 탐색이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'sorted는 새 정렬 리스트를 만들고 index는 그 리스트에서 목표의 위치를 찾는다는 뜻입니다. `원본도 sorted 호출만으로 바뀐다고 생각함`은 반대 사례인데, 반환값을 저장하지 않으면 원본이 그대로이기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `원본도 sorted 호출만으로 바뀐다고 생각함`입니다. 잘못된 코드는 실행은 되지만 `0`이라는 잘못된 출력을 냅니다. 반환값 저장 여부를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `정렬 전후 탐색 위치 구분`이라는 의미와 입력·출력 계약입니다. 정렬 뒤 인덱스와 원본 인덱스를 구별하고, 실행 결과로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '같은 값이 여러 개 있으면 첫 위치를 돌려주지만, 없으면 예외가 납니다. 정렬 뒤 인덱스와 원본 인덱스를 혼동하지 마세요.',
    },
    78: {
        'notes': [
            '그리디는 매 순간 지금 가장 좋아 보이는 선택을 확정합니다. 동전 5와 1로 11을 만들면 5+5+1의 세 개가 좋은 선택입니다. 하지만 동전 4,3,1로 6을 만들 때 큰 것부터 고르면 4+1+1로 세 개이고, 3+3이면 두 개입니다.',
            '따라서 그리디가 정답인지 동전 체계에 대한 근거나 반례 확인이 필요합니다. 예제의 선택 과정을 남은 금액에서 선택한 동전으로 적어 보고 6의 반례를 직접 계산하세요. 지역에서 좋아 보이는 선택이 전체에서도 최적인지는 별개의 질문입니다.',
        ],
        'predict': '1. 11에서 5를 두 번 빼 1이 남습니다. 2. 1을 한 번 뺍니다. 3. 세 동전 [5,5,1]을 출력합니다. `11 → 6 → 1 → 0` 흐름으로 실제 출력 `[5, 5, 1]`이 됩니다. 핵심 `remaining >= coin`은 남은 금액이 동전 이상일 때 고르는 자리에 쓰입니다.',
        'guided': '원본은 `[5, 5, 1]`, 수정본은 `[6, 1, 1, 1, 1, 1]`이다. 첫 변경 줄 `coins = [5, 1]`에서 `coins = [6, 1]`로 큰 동전을 바꾸면, 6 하나와 1 다섯 개로 채워진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `while remaining > coin: remaining -= coin; used.append(coin)`입니다. 남은 금액이 동전과 정확히 같을 때 고르지 못하므로, 마지막 1이 남고 `used`는 `[5, 5]`가 됩니다. 오류 분류는 잘못된 출력입니다. 같은 값 선택이 빠지는 첫 순간이 원인입니다. 고친 줄 `while remaining >= coin: remaining -= coin; used.append(coin)`에서는 `국소 선택의 최적성 조건 확인` 동작이 지켜집니다. 4,3,1 체계에서 6을 만들 때 큰 것부터 고르면 3개이지만 3+3 두 개가 정답이므로, 탐욕이 맞는지 작은 입력으로 검증하세요. 정상 코드는 고르기→빼기→기록의 순서로 `[5, 5, 1]`을 출력합니다.',
        'qout': '먼저 11에서 5를 두 번 빼 1이 남습니다. 이어서 1을 한 번 빼 used에 [5,5,1]이 모이므로 출력 `[5, 5, 1]`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 반복과 기록이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '큰 동전을 먼저 고르는 규칙은 5·1 체계에서 11에 3개라는 뜻이지만, 임의 동전 체계에서 최적을 보장하지 않는다는 뜻은 체계마다 검증해야 한다는 뜻입니다. `모든 동전 체계에서 큰 동전부터 고르면 최적이라고 일반화함`은 반대 사례인데, 4,3,1 체계의 6에서는 3+3이 더 적기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `모든 동전 체계에서 큰 동전부터 고르면 최적이라고 일반화함`입니다. 잘못된 코드는 실행은 되지만 `[5, 5]`라는 잘못된 출력을 냅니다. 같은 값일 때도 고르는지 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `국소 선택의 최적성 조건 확인`이라는 의미와 입력·출력 계약입니다. 탐욕 선택과 완전 탐색으로 같은 동작을 구현하고, 실행 결과 `[5, 5, 1]`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '동전 4,3,1로 6을 만들 때 3+3 두 개가 정답입니다. 탐욕이 맞는지 작은 입력으로 검증하세요.',
    },
    79: {
        'notes': [
            '계단 한 번에 1칸 또는 2칸을 간다면 n칸에 도착하는 마지막 발걸음은 n−1에서 1칸 오거나 n−2에서 2칸 오는 두 경우뿐입니다. 그래서 `ways[n] = ways[n-1] + ways[n-2]`입니다. 예를 들어 3칸은 1+1+1, 1+2, 2+1의 세 방법입니다.',
            '`ways[0]=1`은 아무 걸음도 하지 않는 한 가지 방법으로 두면 점화식이 자연스럽습니다. `ways[1]=1`입니다. 같은 작은 n의 답을 여러 번 재귀 계산하지 말고 배열에 한 번 저장하면 작업을 줄일 수 있습니다. n이 음수일 때 처리할 규칙도 정하세요.',
        ],
        'predict': '1. ways[0]=1입니다. 2. ways[1..4]가 1,2,3,5가 됩니다. 3. 4칸 경우 수 5를 출력합니다. `ways[0]=1 → 1 → 2 → 3 → 5` 흐름으로 실제 출력 `5`가 됩니다. 핵심 `ways[step-1]`은 바로 앞 칸의 경우 수를 읽는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `5`를 출력한다. ways의 0번을 1로 고정한 뒤에는 같은 점화식으로 쌓으므로 결과가 같습니다. 시작값 고정 뒤 쌓는 과정은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `ways[step] = ways[step-2] + (ways[step-2] if step >= 2 else 0)`입니다. 바로 앞 칸 대신 두 칸 앞만 더하므로, step=1에서는 ways[-1]인 마지막 칸을 읽어 어긋나고 최종 `4`가 됩니다. 오류 분류는 잘못된 출력입니다. 앞 칸 읽기가 빠진 첫 순간이 원인입니다. 고친 줄 `ways[step] = ways[step-1] + (ways[step-2] if step >= 2 else 0)`에서는 `작은 문제의 답을 저장해 결합` 동작이 지켜집니다. 정상 코드는 기저 고정→앞 두 칸 합산→5 출력의 순서로 `5`를 출력합니다.',
        'qout': '먼저 ways[0]=1로 고정됩니다. 이어서 앞 두 칸을 더해 1,2,3,5가 쌓이고, 4칸 경우 수 5를 출력하므로 `5`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 점화식 실행이 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'ways[n]은 n칸까지 오는 경우 수이며 마지막 이동은 n-1 또는 n-2에서 온다는 뜻은, 두 앞 칸의 합이 답이라는 뜻입니다. `기저 경우 수 0을 0으로 두어 모든 값이 0이 됨`은 반대 사례인데, 시작값이 어긋나면 뒤가 전부 어긋나기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `기저 경우 수 0을 0으로 두어 모든 값이 0이 됨`입니다. 잘못된 코드는 실행은 되지만 `4`라는 잘못된 출력을 냅니다. 앞 칸 읽기를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `작은 문제의 답을 저장해 결합`이라는 의미와 입력·출력 계약입니다. C의 배열 점화와 Rust의 벡터 점화로 같은 동작을 구현하고, 실행 결과 `5`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '`ways[0]=1`로 두는 이유를 설명하세요. n이 음수일 때 규칙도 정하세요.',
    },
    80: {
        'notes': [
            '백트래킹은 선택해 보고, 끝까지 탐색한 뒤 그 선택을 되돌려 다음 선택을 시험합니다. `path.append(x)`로 경로에 x를 추가했다면 재귀 호출이 끝난 뒤 반드시 `path.pop()`으로 x를 빼야 같은 시작 상태에서 다른 가지를 탐색할 수 있습니다.',
            '예를 들어 `[1,2]`의 순서를 만들 때 1을 선택해 `[1,2]`를 완성한 뒤 1 선택을 되돌려야 `[2,1]`을 만들 수 있습니다. 완성된 답을 저장할 때는 변하는 `path` 자체가 아니라 복사본을 저장하세요. 그렇지 않으면 나중에 되돌린 내용이 저장된 답에도 반영될 수 있습니다.',
        ],
        'predict': '1. 1을 먼저 넣어 (1,2)를 저장합니다. 2. pop으로 비운 뒤 2를 선택합니다. 3. (2,1)을 저장합니다. `[] → [1,2] 기록 → []로 복귀 → [2,1] 기록` 흐름으로 실제 출력 두 순서쌍이 됩니다. 핵심 `path.pop()`은 다음 가지를 위해 경로를 비우는 자리에 쓰입니다.',
        'guided': '원본은 두 순서쌍, 수정본은 한 원소 순서쌍 둘이다. 첫 변경 줄 `visit([], [1, 2])`에서 `visit([], [2, 2])`로 남은 값을 바꾸면, 같은 값만 골라 길이가 짧은 순서쌍이 기록된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 재귀 호출 뒤의 `path.append(x)`입니다. 되돌리기는커녕 선택을 한 번 더 쌓으므로, 다른 가지로 넘어갈 때 경로에 이전 선택이 섞여 기록되는 순서쌍이 어긋납니다. 입력 [1,2]에서는 두 번째 기록이 섞인 채 남습니다. 오류 분류는 잘못된 출력입니다. 경로가 섞이는 첫 순간은 호출 뒤의 추가입니다. 고친 줄 `path.pop()`에서는 `선택·탐색·취소의 대칭 유지` 동작이 지켜집니다. 정상 코드는 선택→탐색→되돌리기의 순서로 두 순서쌍을 기록합니다.',
        'qout': '먼저 1을 넣어 (1,2)를 저장합니다. 이어서 pop으로 비운 뒤 2를 선택해 (2,1)을 저장하므로 두 순서쌍이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 재귀 호출과 되돌리기가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'path에 넣은 값을 재귀 호출 뒤 반드시 빼야 다음 가지가 같은 시작 상태에서 출발한다는 뜻은, 선택·탐색·취소가 대칭이어야 한다는 뜻입니다. `되돌리기 pop을 빼먹어 다른 가지의 경로가 섞임`은 반대 사례인데, 빼지 않으면 이전 선택이 다음 가지에 남기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `되돌리기 pop을 빼먹어 다른 가지의 경로가 섞임`입니다. 잘못된 코드는 실행은 되지만 순서쌍이 어긋난 잘못된 출력을 냅니다. 호출 뒤 줄이 되돌리기인지 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `선택·탐색·취소의 대칭 유지`라는 의미와 입력·출력 계약입니다. C의 재귀 되돌리기와 Rust의 상태 복원으로 같은 동작을 구현하고, 실행 결과 두 순서쌍으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '완성된 답을 저장할 때는 변하는 `path` 자체가 아니라 복사본을 저장하세요. 되돌린 내용이 답에 반영되는지 확인하세요.',
    },
    81: {
        'notes': [
            '깊이 우선 탐색(DFS)은 한 길을 가능한 끝까지 따라간 뒤 돌아와 다른 길을 살핍니다. A→B→C를 먼저 따라갔다면 C의 이웃을 다 처리하고 B로 돌아옵니다. 방문한 정점은 `seen`에 넣어 A→B→A 같은 순환에서 같은 작업을 반복하지 않게 합니다.',
            '`seen.add(node)`를 호출 초기에 하면 다른 길에서 동일 노드를 만났을 때 바로 돌아올 수 있습니다. 재귀가 매우 깊은 그래프에서는 Python의 재귀 깊이 제한을 고려해 명시적 스택을 쓸 수 있습니다. 방문 순서는 이웃 목록의 순서에 따라서도 달라집니다.',
        ],
        'predict': '1. A를 표시하고 B로 갑니다. 2. B를 표시하고 C로 갑니다. 3. C의 다음 A는 이미 방문해 돌아옵니다. `A 방문 → B 방문 → C 방문 → A 재방문 차단` 흐름으로 실제 출력 `3`이 됩니다. 핵심 `seen.add(node)`는 방문 사실을 기록하는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `3`을 출력한다. 순환 구조라 시작점이 달라도 방문 집합은 같은 3개라 결과가 같습니다. 시작점만 달라지고 모이는 집합은 그대로이므로 중간 상태가 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `seen.remove(node)`입니다. A는 집합에 없으므로 첫 호출에서 KeyError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 방문 기록 줄입니다. 고친 줄 `seen.add(node)`에서는 `사이클을 차단하며 깊이 탐색` 동작이 지켜집니다. 표시를 빼면 A→B→C→A 사이클에서 끝나지 않으므로, 기록은 필수입니다. 정상 코드는 표시→이웃 따라가기→재방문 차단의 순서로 `3`을 출력합니다.',
        'qout': '먼저 A를 표시하고 B로 갑니다. 이어서 B를 표시하고 C로 가며, C의 다음 A는 이미 방문해 돌아오므로 세 정점이 모여 출력 `3`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 재귀와 집합 연산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'DFS는 이웃을 재귀적으로 끝까지 따라간다는 뜻이고, 이미 방문한 정점은 즉시 반환한다는 뜻은 순환에서 같은 작업을 반복하지 않는다는 뜻입니다. `방문 표시를 빼고 A→B→C→A 사이클에서 무한 재귀함`은 반대 사례인데, 표시가 없으면 끝없이 돌기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `방문 표시를 빼고 A→B→C→A 사이클에서 무한 재귀함`입니다. 잘못된 코드는 없는 원소를 빼려 해 KeyError로 멈춥니다. 기록 줄을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `사이클을 차단하며 깊이 탐색`이라는 의미와 입력·출력 계약입니다. C의 방문 배열과 Rust의 집합 기록으로 같은 동작을 구현하고, 실행 결과 `3`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '재귀가 매우 깊은 그래프에서는 재귀 깊이 제한을 고려하세요. 이웃 순서에 따라 방문 순서가 달라집니다.',
    },
    82: {
        'notes': [
            '가중치가 있는 그래프의 최단 경로에서는 방문 횟수보다 비용의 합을 봅니다. 현재까지 A→B 비용이 5인데 A→C→B가 2+1=3이면 B의 거리 후보를 5에서 3으로 바꿉니다. 이 동작을 완화라고 부릅니다.',
            '우선순위 큐에서 가장 작은 후보를 먼저 꺼내는 다익스트라 방식은 간선 비용이 음수가 없을 때 적용합니다. 큐에는 이전의 더 큰 후보가 남아 있을 수 있으므로 꺼낸 비용이 현재 거리보다 크면 건너뜁니다. 도달할 수 없는 정점은 거리가 무한대로 남습니다. 음수 간선이 있으면 다른 알고리즘을 골라야 합니다.',
        ],
        'predict': '1. 모든 거리를 무한대로 시작하고 A만 0으로 둡니다. 2. 큐에서 A를 꺼내 B=2,C=5로 기록합니다. 3. B에서 C의 새 후보 3을 발견해 갱신합니다. 4. 오래된 C=5 후보는 건너뛰고 3을 출력합니다. `A=0 → B=2,C=5 → B 경유 C=3 → C=3` 흐름으로 실제 출력 `3`이 됩니다. 핵심 `cost + weight < dist[next_node]`는 더 짧을 때만 갱신하는 자리에 쓰입니다.',
        'guided': '원본은 `3`, 수정본은 `4`이다. 첫 변경 줄의 B 간선 비용 2에서 3으로 바꾸면, B 경유 C 후보가 3+1=4가 되어 직접 간선 5보다 여전히 짧지만 최종 거리가 4가 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `if cost + weight > dist[next_node]:`입니다. 더 길 때만 갱신하므로 어떤 거리도 짧아지지 않아 C는 무한대 그대로 남아 `inf`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 완화가 일어나지 않는 첫 순간이 원인입니다. 고친 줄 `if cost + weight < dist[next_node]:`에서는 `더 짧은 후보로 거리 완화` 동작이 지켜집니다. 음수 간선에는 다익스트라의 확정 규칙을 그대로 적용하면 안 됩니다. 정상 코드는 0 시작→이웃 기록→B 경유 갱신→3 출력의 순서로 `3`을 출력합니다.',
        'qout': '먼저 모든 거리가 무한대이고 A만 0입니다. 이어서 A를 꺼내 B=2,C=5를 기록하고, B에서 C의 새 후보 3을 발견해 갱신한 뒤, 오래된 후보를 건너뛰고 3을 출력하므로 `3`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 힙 연산과 갱신이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '음수가 없는 간선에서는 현재까지 가장 짧은 후보부터 꺼낸다는 뜻이고, 이웃 경로가 기존 거리보다 짧으면 거리와 우선순위 큐를 갱신한다는 뜻은 더 짧을 때만 바꾼다는 뜻입니다. `음수 간선에도 다익스트라의 확정 규칙을 그대로 적용함`은 반대 사례인데, 음수가 있으면 확정 순서가 깨지기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `음수 간선에도 다익스트라의 확정 규칙을 그대로 적용함`입니다. 잘못된 코드는 실행은 되지만 `inf`라는 잘못된 출력을 냅니다. 비교 방향을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `더 짧은 후보로 거리 완화`라는 의미와 입력·출력 계약입니다. C의 거리 배열과 Rust의 거리 맵으로 같은 동작을 구현하고, 실행 결과 `3`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '도달할 수 없는 정점은 거리가 무한대로 남습니다. 음수 간선이 있으면 다른 알고리즘을 고르세요.',
    },
    83: {
        'notes': [
            '프로젝트의 CSV 한 행은 날짜·언어·주제·분·결과의 순서 기록입니다. 단순 예제의 `split(",")`는 쉼표로 나눠 다섯 조각을 얻고 네 번째인 인덱스 3을 분으로 읽습니다. 분 문자열은 합계에 넣기 전에 정수로 바꿔야 합니다.',
            '실제 CSV에서는 따옴표 안 쉼표 같은 규칙이 있어서 단순 나누기로 항상 안전하게 파싱할 수 없습니다. 완성 프로젝트의 각 언어 구현과 CSV 규칙을 다루는 코드를 확인하세요. 누락된 열·빈 분·음수 분을 어떻게 처리할지도 공통 계약에 적어야 합니다.',
        ],
        'predict': '1. 헤더 문자열을 쉼표로 나눕니다. 2. 다섯 항목을 얻습니다. 3. 인덱스 3의 minutes를 출력합니다. `헤더 → 다섯 열 → 네 번째 minutes` 흐름으로 실제 출력 `5 minutes`가 됩니다. 핵심 나누기 연산은 열을 가르는 자리에 쓰입니다.',
        'guided': '원본은 `5 minutes`, 수정본은 `5 topic`이다. 첫 변경 줄 `print(len(fields), fields[3])`에서 `print(len(fields), fields[2])`로 읽는 열을 바꾸면, 네 번째 대신 세 번째 값이 출력된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `fields = header.split(";")`입니다. 세미콜론으로 나누면 조각이 하나뿐이므로 뒤의 `fields[3]`을 읽으려다 IndexError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 구분자 선택입니다. 고친 줄 `fields = header.split(",")`에서는 `공통 입력 스키마 정의` 동작이 지켜집니다. 따옴표 안 쉼표가 있는 실제 CSV는 단순 나누기로 처리하면 안 됩니다. 정상 코드는 나누기→다섯 열 확정→네 번째 읽기→출력의 순서로 `5 minutes`를 출력합니다.',
        'qout': '먼저 헤더 문자열이 쉼표로 나뉩니다. 이어서 다섯 항목을 얻고 인덱스 3의 minutes를 읽어 출력하므로 `5 minutes`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 나누기와 인덱싱이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'CSV 한 행은 날짜·언어·주제·분·결과의 다섯 열이라는 뜻이고, minutes는 네 번째 열이라는 뜻은 인덱스 3을 읽는다는 뜻입니다. `쉼표가 포함된 인용 필드를 단순 나누기로 실제 CSV처럼 처리함`은 반대 사례인데, 인용 규칙을 무시하면 열이 어긋나기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `쉼표가 포함된 인용 필드를 단순 나누기로 실제 CSV처럼 처리함`입니다. 잘못된 코드는 조각이 하나뿐이라 IndexError로 멈춥니다. 구분자를 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `공통 입력 스키마 정의`라는 의미와 입력·출력 계약입니다. 세 언어의 같은 스키마 파서로 동작을 구현하고, 실행 결과 `5 minutes`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '누락된 열·빈 분·음수 분을 어떻게 처리할지 공통 계약에 적으세요. 따옴표 안 쉼표는 단순 나누기로 처리하면 안 됩니다.',
    },
    84: {
        'notes': [
            'fixture는 세 언어가 똑같은 입력을 받아 같은 결과를 내는지 비교하기 위한 작은 고정 데이터입니다. C 기록이 두 건이고 분이 10과 30이면 합계는 40입니다. Rust 기록은 C만 고르는 필터에서 제외돼야 합니다.',
            '정상 입력 하나만으로는 검증이 약합니다. 빠진 열, 숫자가 아닌 분, 빈 파일, 중복된 주제도 각각 입력으로 만들어 어떤 결과나 오류가 나와야 하는지 미리 정하세요. 프로그램을 작성한 후에 정답을 맞추는 대신, 먼저 기대 출력을 적으면 오류를 찾기 쉽습니다.',
        ],
        'predict': '1. 첫 C에서 30을 얻습니다. 2. Rust는 건너뜁니다. 3. 두 번째 C의 10을 더해 40입니다. `C 30 → Rust 제외 → C 10 → 40` 흐름으로 실제 출력 `40`이 됩니다. 핵심 `language == "C"`는 고를 행을 정하는 자리에 쓰입니다.',
        'guided': '원본은 `40`, 수정본은 `41`이다. 첫 변경 줄의 첫 C분 30에서 31로 바꾸면, 필터를 통과하는 합이 그대로 1만큼 커진다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(sum(minutes for language, minutes in rows if language != "C"))`입니다. C가 아니라 Rust 행만 골라 20이 되므로 실행은 되지만 결과는 `20`이 됩니다. 오류 분류는 잘못된 출력입니다. 고를 행이 뒤집히는 첫 순간이 원인입니다. 고친 줄 `print(sum(minutes for language, minutes in rows if language == "C"))`에서는 `공통 fixture의 필터 기대값 계산` 동작이 지켜집니다. 정상 코드는 C 선택→30+10 합산→Rust 제외의 순서로 `40`을 출력합니다.',
        'qout': '먼저 첫 C에서 30을 얻습니다. 이어서 Rust는 건너뛰고 두 번째 C의 10을 더해 40이 되므로 출력 `40`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 필터와 합산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'C 행 두 개의 분을 더하면 40이고 Rust 행은 필터에서 제외된다는 뜻은, 고르는 조건이 결과를 정한다는 뜻입니다. `필터를 적용한 결과와 전체 합계를 같은 값으로 비교함`은 반대 사례인데, 필터를 씌운 값과 전체 합은 다르기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `필터를 적용한 결과와 전체 합계를 같은 값으로 비교함`입니다. 잘못된 코드는 실행은 되지만 `20`이라는 잘못된 출력을 냅니다. 고르는 조건을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `공통 fixture의 필터 기대값 계산`이라는 의미와 입력·출력 계약입니다. 세 언어의 같은 필터 조건으로 동작을 구현하고, 실행 결과 `40`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빠진 열, 숫자가 아닌 분, 빈 파일, 중복 주제도 입력으로 만들어 기대 결과를 미리 정하세요. 정답을 나중에 맞추지 마세요.',
    },
    85: {
        'notes': [
            '`csv.DictReader`는 헤더 이름을 키로 각 행을 읽습니다. `row["minutes"]`가 문자열 30이면 `int(...)`를 거쳐 정수 30이 되고 합계에 쓸 수 있습니다. 숫자가 아닌 값은 `ValueError`가 납니다. `int()`는 숫자 형식 변환만 할 뿐 음수, 누락 필드, 허용 언어를 모두 검증하지는 않습니다.',
            '키 자체가 빠졌다면 별도 오류이고, 빈 값이나 음수 값은 따로 검증해야 합니다. CSV를 읽는 동작과 유효한 학습 기록인가를 판단하는 동작을 나누세요. 오류 메시지에는 어떤 행의 어느 필드인지 알려 주면 사용자가 파일을 고칠 수 있습니다.',
        ],
        'predict': '1. 헤더로 열 이름을 인식합니다. 2. 첫 데이터 행을 사전으로 읽습니다. 3. minutes 문자열 30을 정수 30으로 바꿉니다. `CSV 문자열 → minutes 문자열 30 → int 30` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `int(row["minutes"])`는 문자열을 합산 가능한 정수로 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄의 입력 행 분 30에서 31로 바꾸면, 같은 변환을 거쳐 31이 출력된다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `minutes = row["minutes"]`와 `print(minutes + 5)`입니다. 변환 없이 문자열에 5를 더하려 하므로 TypeError라는 런타임 오류가 나고 프로그램이 멈춥니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 문자열과 정수의 덧셈입니다. 고친 줄 `minutes = int(row["minutes"])`와 `print(minutes + 5)`에서는 30+5를 계산해 `35`를 출력합니다. `int()`는 숫자 형식 변환만 할 뿐 음수·누락·언어 검증을 대신하지 않으므로, 제목의 검증을 살리려면 누락 확인·허용 집합·0 이상 검사를 실제 코드로 더해야 합니다. 고친 코드는 읽기→정수 변환→35 계산의 순서로 `35`를 출력합니다. 이날 원본 예제의 `30` 출력과 구별하세요.',
        'qout': '먼저 헤더로 열 이름을 인식합니다. 이어서 첫 행을 사전으로 읽고 minutes 문자열 30을 정수 30으로 바꿔 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 읽기와 변환이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': 'DictReader는 헤더 이름으로 필드를 읽는다는 뜻이고, int는 minutes를 정수로 변환하며 잘못된 값이면 실패한다는 뜻은 변환과 검증이 분리된다는 뜻입니다. `검증 없이 음수·누락·알 수 없는 언어를 정상 행으로 취급함`은 반대 사례인데, `int()`는 형식 변환만 하므로 별도 검사가 필요하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `검증 없이 음수·누락·알 수 없는 언어를 정상 행으로 취급함`입니다. 잘못된 코드는 문자열 덧셈에서 TypeError로 멈춥니다. 변환 뒤 별도 검사를 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `CSV 레코드 필드별 검증`이라는 의미와 입력·출력 계약입니다. C의 필드 검사와 Rust의 파싱 검증으로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '키 자체가 빠지면 별도 오류입니다. 오류 메시지에 행과 필드를 적으세요.',
    },
    86: {
        'notes': [
            '언어별 누적 분은 사전에 저장합니다. C의 기존 합계가 20이고 다음 C 기록이 30이면 새 합계는 50이 됩니다. 새 언어 Rust는 기존 키가 없으므로 0에서 시작합니다.',
            'Top N 정렬은 합계가 큰 순서, 합계가 같으면 이름이 앞서는 순서처럼 동점 규칙까지 정해야 구현끼리 같은 결과가 납니다. N=0, N이 항목 수보다 클 때도 정해진 결과가 있어야 합니다. 사전의 순회 순서에 우연히 의존하지 마세요.',
        ],
        'predict': '1. C 30을 기록합니다. 2. Python 45와 C의 추가 15로 둘 다 45입니다. 3. 동률에서 C가 앞서 첫 항목입니다. `C=30 → Python=45 → C=45 → 동률은 이름순 C` 흐름으로 실제 출력 첫 항목이 됩니다. 핵심 `totals.get(name, 0)`은 없을 때 0에서 시작하는 자리에 쓰입니다.',
        'guided': '원본 첫 항목과 수정본 첫 항목은 합계가 달라진다. 첫 변경 줄의 첫 C분 30에서 31로 바꾸면, C 합계가 46이 되어 동률이 깨지고 첫 항목이 바뀐다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `for name, minutes in rows: totals[name] = totals.get(name, 1) + minutes`입니다. 첫 등장부터 1을 더 얹어 C는 47, Python은 46이 되므로 실행은 되지만 첫 항목이 어긋나 `("C", 46)`이 됩니다. 오류 분류는 잘못된 출력입니다. 합계가 어긋나는 첫 순간은 기본값입니다. 고친 줄 `for name, minutes in rows: totals[name] = totals.get(name, 0) + minutes`에서는 `그룹별 합계와 결정적 정렬` 동작이 지켜집니다. 동률 순서를 명시하지 않으면 구현마다 결과가 달라집니다. 정상 코드는 누적→동점 규칙 정렬→첫 항목의 순서로 첫 항목을 출력합니다.',
        'qout': '먼저 C 30이 기록됩니다. 이어서 Python 45와 C의 추가 15로 둘 다 45가 되고, 동률에서 이름순으로 C가 앞서 첫 항목이 되므로 해당 항목이 출력됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 누적과 정렬이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '사전은 언어별 누적값을 저장한다는 뜻이고, 정렬 키는 합계 내림차순, 언어 이름 오름차순이라는 뜻은 동률까지 규칙으로 정한다는 뜻입니다. `동률 순서를 명시하지 않아 구현 언어마다 Top N이 달라짐`은 반대 사례인데, 규칙이 없으면 결과가 우연에 좌우되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `동률 순서를 명시하지 않아 구현 언어마다 Top N이 달라짐`입니다. 잘못된 코드는 실행은 되지만 `("C", 46)`이라는 잘못된 출력을 냅니다. 기본값과 동점 규칙을 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `그룹별 합계와 결정적 정렬`이라는 의미와 입력·출력 계약입니다. 세 언어의 같은 정렬 키로 동작을 구현하고, 첫 항목 출력으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'N=0, N이 항목 수보다 클 때도 정해진 결과가 있어야 합니다. 순회 순서에 우연히 의존하지 마세요.',
    },
    87: {
        'notes': [
            'C의 숫자 변환은 숫자 글자를 읽고 어디까지 읽었는지 끝 위치로 알려 줍니다. 문자열 30 뒤에 다른 글자가 남으면 끝 검사를 거부할 수 있습니다. 아무 숫자도 읽지 못한 경우도 따로 거부해야 합니다.',
            '정수 범위를 넘은 입력은 범위 오류 확인이 필요하고, 음수를 허용하지 않는 분이라면 결과가 0 이상인지 검사합니다. 문자열 일부만 숫자인 것을 정상 입력으로 착각하면 집계가 조용히 틀어집니다. 네 가지 입력을 차례로 시험하세요.',
        ],
        'predict': '1. 30을 10진수 30으로 바꿉니다. 2. 끝 위치는 문자열 끝을 가리킵니다. 3. 양수와 종료 문자를 확인해 출력합니다. `입력 30 → minutes=30 → 끝 확인 → 성공` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `*end != \\0`는 입력 전체가 숫자인지 확정하는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `33`이다. 첫 변경 줄의 진법 10에서 11로 바꾸면, 같은 글자 30이 11진수로 읽혀 33이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `if (!end || *end == \\0 || minutes <= 0) return 1;`입니다. 끝 검사가 뒤집혀 정상 입력 30도 거부되므로, 실행은 되지만 아무것도 출력하지 않고 종료 코드 1로 끝납니다. 오류 분류는 잘못된 출력입니다. 정상 흐름이 끊기는 첫 순간은 끝 검사 조건입니다. 고친 줄의 `*end != \\0` 다름 비교에서는 `숫자 필드의 완전한 파싱 검사` 동작이 지켜집니다. `atoi`로는 오류·남은 문자·범위를 구분하지 못합니다. 정상 코드는 변환→끝 확인→양수 확인→출력의 순서로 `30`을 출력합니다.',
        'qout': '먼저 30을 10진수 30으로 바꿉니다. 이어서 끝 위치가 문자열 끝을 가리키는지 확인하고, 양수임이 확인되어 출력하므로 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 변환과 검사가 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '숫자를 읽은 마지막 위치를 끝 위치에 넣는다는 뜻이고, 끝이 널일 때만 입력 전체가 숫자라는 뜻은 일부만 숫자면 거부한다는 뜻입니다. `atoi로 오류·남은 문자·범위를 구분하지 못함`은 반대 사례인데, 구분 없이는 조용한 오집을 막을 수 없기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `atoi로 오류·남은 문자·범위를 구분하지 못함`입니다. 잘못된 코드는 정상 입력까지 거부해 출력 없이 끝납니다. 끝 비교 방향을 먼저 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `숫자 필드의 완전한 파싱 검사`라는 의미와 입력·출력 계약입니다. Python의 엄격 변환과 Rust의 파싱 검사로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '빈 입력, 남은 글자, 아주 큰 숫자, 음수를 차례로 시험하세요. 일부만 숫자면 정상으로 착각하지 마세요.',
    },
    88: {
        'notes': [
            'C에서 길이 3의 레코드 배열을 더한다면 유효한 인덱스는 0,1,2입니다. `total=0`에서 시작해 각 항목의 minutes를 한 번씩 더하면 10→30→60처럼 중간 상태를 적을 수 있습니다. `i < 3`을 `i <= 3`으로 바꾸면 범위 밖 네 번째 원소를 읽는 잘못된 접근입니다.',
            '입력 레코드 수를 실제 길이와 함께 함수에 전달하고 유효한 값인지 확인하세요. 잘못된 분을 건너뛸지 오류로 종료할지는 프로젝트의 공통 계약에 맞춰야 합니다. 포인터와 길이 두 정보를 항상 함께 보세요.',
        ],
        'predict': '1. total=0입니다. 2. 30,20,10을 순서대로 누적합니다. 3. 합계 60을 출력합니다. `0 → 30 → 50 → 60` 흐름으로 실제 출력 `60`이 됩니다. 핵심 `i < 3`은 유효 번호 0,1,2만 방문하게 하는 자리에 쓰입니다.',
        'guided': '원본은 `60`, 수정본은 `61`이다. 첫 변경 줄의 첫 값 30에서 31로 바꾸면, 이후 누적에 그대로 더해져 최종 합이 1만큼 커진다.',
        'debug_outcome': 'undefined-behavior',
        'debug': '틀린 줄은 `for (int i = 0; i <= 3; i++) total += minutes[i];`입니다. 마지막 반복에서 네 번째 원소를 읽으므로 C의 정의되지 않은 동작이 됩니다. 합계가 특정 숫자가 된다고 단정하지 않습니다. 어떤 값이 보일 수도 있고 비정상 종료될 수도 있으며, 어떤 결과도 보장할 수 없습니다. 오류 분류는 정의되지 않은 동작입니다. 유효 인덱스는 0,1,2뿐입니다. 고친 줄 `for (int i = 0; i < 3; i++) total += minutes[i];`에서는 `레코드 시간 합계 누적` 동작이 지켜집니다. 정상 코드는 0 시작→30→50→60 누적→출력의 순서로 `60`을 출력합니다.',
        'qout': '먼저 total이 0에서 시작됩니다. 이어서 30,20,10을 순서대로 누적해 60이 되고, 출력하므로 `60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 유효 범위 접근이라 컴파일과 실행이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 printf가 실제로 호출되기 때문입니다.',
        'qmodel': '현재 합계 total에 각 유효 인덱스의 값을 한 번씩 더한다는 뜻이고, 마지막 유효 인덱스는 2라는 뜻은 쓸 수 있는 번호가 0,1,2뿐이라는 뜻입니다. `배열 길이와 인덱스의 경계를 혼동해 원소를 하나 더 읽음`은 반대 사례인데, 하나 더 읽으면 범위 밖이라 정의되지 않은 동작이 되기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `배열 길이와 인덱스의 경계를 혼동해 원소를 하나 더 읽음`입니다. 범위 밖 읽기는 정의되지 않은 동작이므로, 합계가 특정 숫자가 된다고 약속하지 마세요. 길이와 인덱스를 함께 전달하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `레코드 시간 합계 누적`이라는 의미와 입력·출력 계약입니다. Python의 합산과 Rust의 iterator 합으로 같은 동작을 구현하고, 실행 결과 `60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '잘못된 분을 건너뛸지 오류로 종료할지는 공통 계약에 맞추세요. 포인터와 길이를 항상 함께 보세요.',
    },
    89: {
        'notes': [
            '글자를 숫자로 바꾸는 시도는 성공하면 값을, 실패하면 오류를 돌려줍니다. 저장할 필드 타입을 정하면 잘못된 형태의 기록이 조기에 드러납니다. 분 필드는 음수 없는 정수로 정하세요.',
            '숫자가 아닌 값, 빈 값, 범위를 넘은 큰 숫자는 유효한 분 값이 아닙니다. 오류를 갑자기 끝내기보다 행 번호와 함께 알려 주고 계속 읽을지 중단할지 정책을 정하세요. 타입을 정하는 것만으로 현실적인 범위 같은 업무 규칙까지 해결되지는 않습니다.',
        ],
        'predict': '1. raw는 문자열 30입니다. 2. parse로 u32 30을 얻습니다. 3. Session의 필드로 저장해 출력합니다. `raw 문자열 → parse Ok(30) → Session.minutes=30` 흐름으로 실제 출력 `30`이 됩니다. 핵심 `raw.parse()`는 글자를 필드 타입으로 바꾸는 자리에 쓰입니다.',
        'guided': '원본은 `30`, 수정본은 `31`이다. 첫 변경 줄 `let raw = "30";`에서 `let raw = "31";`로 입력 글자를 바꾸면, 같은 파싱을 거쳐 새 값이 저장되어 출력된다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `let session = Session { minutes: raw.len().expect("분 숫자 오류") };`입니다. 길이 숫자에 기대한다는 메서드가 없어 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 변환 호출입니다. 고친 줄 `let session = Session { minutes: raw.parse().expect("분 숫자 오류") };`에서는 `텍스트 행을 타입 있는 레코드로 변환` 동작이 지켜집니다. 실제 파일 파서에서는 어느 행인지 숨기고 전체를 끝내지 마세요. 정상 코드는 글자 읽기→u32 변환→필드 저장→출력의 순서로 `30`을 출력합니다.',
        'qout': '먼저 raw에 문자열 30이 저장됩니다. 이어서 parse로 u32 30을 얻어 Session 필드에 저장하고 표시하므로 출력 `30`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 파싱과 타입이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'minutes 필드에서 parse가 숫자 변환을 한다는 뜻이고, 실패하면 오류가 된다는 뜻은 잘못된 기록이 조기에 드러난다는 뜻입니다. `실제 파일 파서에서 expect로 전체 프로그램을 종료하고 어느 행인지 숨김`은 반대 사례인데, 행 번호 없는 종료는 디버깅을 어렵게 하기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `실제 파일 파서에서 expect로 전체 프로그램을 종료하고 어느 행인지 숨김`입니다. 잘못된 코드는 있는 메서드가 아니라 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `텍스트 행을 타입 있는 레코드로 변환`이라는 의미와 입력·출력 계약입니다. C의 strtol 검사와 Python의 int 변환으로 같은 동작을 구현하고, 실행 결과 `30`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '타입을 정하는 것만으로 업무 규칙까지 해결되지는 않습니다. 행 번호와 함께 알리고 계속 읽을지 정하세요.',
    },
    90: {
        'notes': [
            'Rust의 `iter()`는 원소를 차례로 빌려 보고 `sum()`은 그 값들을 더합니다. `[10,20,30]`이면 중간 합계는 10,30,60입니다. 원래 모음을 이동하지 않고 읽기 때문에 뒤에 다시 사용할 수 있습니다.',
            '언어별 Top N을 만들려면 합계 계산 뒤에도 합계 내림차순·이름 오름차순 같은 동점 규칙을 명시적으로 정렬해야 합니다. 큰 값의 덧셈이 정수 타입 범위를 넘는지도 입력 검증에 포함하세요. iterator는 반복문을 없앤 마법이 아니라 읽기·변환·누적 단계를 이어 표현하는 방식입니다.',
        ],
        'predict': '1. 30,20,10을 배열에 둡니다. 2. iter가 세 값을 읽습니다. 3. sum이 60을 계산합니다. `30 → 50 → 60` 흐름으로 실제 출력 `60`이 됩니다. 핵심 `iter().sum()`은 빌려 읽고 누적하는 자리에 쓰입니다.',
        'guided': '원본은 `60`, 수정본은 `61`이다. 첫 변경 줄의 두 번째 값 20에서 21로 바꾸면, 빌려 읽기 뒤 합산에 그대로 더해져 최종 합이 1만큼 커진다.',
        'debug_outcome': 'compile-error',
        'debug': '틀린 줄은 `let total: u32 = minutes.iter().count();`입니다. 개수는 개수 타입으로 돌아오므로 `u32` 합계 변수에 담을 수 없어 컴파일 단계에서 오류가 나고 실행 파일 자체가 만들어지지 않아 아무것도 출력되지 않습니다. 오류 분류는 컴파일 오류입니다. 흐름이 깨지는 첫 순간은 합계가 아니라 개수를 구한 대입입니다. 고친 줄 `let total: u32 = minutes.iter().sum();`에서는 `iterator로 합계 계산` 동작이 지켜집니다. 정상 코드는 배열 준비→빌려 읽기→누적 합산의 순서로 `60`을 출력합니다.',
        'qout': '먼저 [30,20,10] 배열이 준비됩니다. 이어서 iter가 세 값을 빌려 읽고 sum이 60을 계산해 출력하므로 `60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 빌림과 합산이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 println!이 실제로 호출되기 때문입니다.',
        'qmodel': 'iter는 값들을 차례로 빌려준다는 뜻이고, sum은 누적합을 돌려준다는 뜻은 읽기·변환·누적 단계를 잇는다는 뜻입니다. `배열 길이를 합계와 혼동하거나 release overflow 정책을 가정함`은 반대 사례인데, 개수와 합계는 다른 값이기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `배열 길이를 합계와 혼동하거나 release overflow 정책을 가정함`입니다. 잘못된 코드는 개수와 합계의 타입이 맞지 않아 컴파일 오류가 나므로 화면 출력 비교 이전에 실행 파일 생성 자체가 막힙니다.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `iterator로 합계 계산`이라는 의미와 입력·출력 계약입니다. C의 반복 합산과 Python의 sum으로 같은 동작을 구현하고, 실행 결과 `60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '큰 값의 덧셈이 정수 타입 범위를 넘는지도 검증하세요. iterator는 마법이 아니라 단계를 잇는 표현입니다.',
    },
    91: {
        'notes': [
            '세 구현이 같다고 주장하려면 같은 fixture와 같은 규칙으로 결과를 비교해야 합니다. Python 사전의 `==`는 키의 순서가 달라도 모든 키와 값이 같으면 참입니다. 따라서 언어별 합계처럼 순서가 중요하지 않은 자료는 사전 비교가 편합니다.',
            '반면 Top N 목록에는 순서가 의미 있으므로 리스트 순서까지 비교해야 합니다. 오류 사례에서는 단순히 실패했다뿐 아니라 어떤 입력이 거부되어야 하는지도 확인하세요. 결과가 다르면 첫 번째로 다른 기록 또는 집계 단계부터 추적하세요.',
        ],
        'predict': '1. expected와 actual에 같은 두 키·값이 들어 있습니다. 2. 순서는 달라도 값 비교가 성공합니다. 3. True를 출력합니다. `두 결과의 키·값 일치 → True` 흐름으로 실제 출력 `True`가 됩니다. 핵심 `actual == expected`는 값 일치를 묻는 자리에 쓰입니다.',
        'guided': '원본은 `True`, 수정본은 `False`이다. 첫 변경 줄 `expected = {"total": 60, "count": 3}`에서 `expected = {"total": 61, "count": 3}`으로 기대값을 바꾸면, actual과 달라져 거짓이 된다.',
        'debug_outcome': 'wrong-output',
        'debug': '틀린 줄은 `print(actual is expected)`입니다. 서로 다른 두 객체이므로 동일성 검사는 거짓이 되어 실행은 되지만 결과는 `False`가 됩니다. 오류 분류는 잘못된 출력입니다. 값 일치가 동일성으로 바뀐 첫 순간이 원인입니다. 고친 줄 `print(actual == expected)`에서는 `공통 fixture 기대값 비교` 동작이 지켜집니다. 정상 코드는 두 결과 준비→값 비교→일치 판정의 순서로 `True`를 출력합니다.',
        'qout': '먼저 expected와 actual에 같은 두 키·값이 들어 있습니다. 이어서 순서는 달라도 값 비교가 성공해 True가 되므로 출력 `True`가 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 딕셔너리 비교가 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '사전의 ==는 항목의 순서가 달라도 키와 값이 모두 같으면 참이라는 뜻은, 순서가 의미 없는 자료에 편하다는 뜻입니다. `객체 동일성 is로 독립된 결과의 값 일치를 검사함`은 반대 사례인데, 다른 객체는 is가 거짓이기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `객체 동일성 is로 독립된 결과의 값 일치를 검사함`입니다. 잘못된 코드는 실행은 되지만 `False`라는 잘못된 출력을 냅니다. 값 일치와 객체 동일성을 구별하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `공통 fixture 기대값 비교`라는 의미와 입력·출력 계약입니다. 세 언어의 같은 비교 규칙으로 동작을 구현하고, 실행 결과 `True`로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': 'Top N 목록에는 순서가 의미 있으므로 리스트 순서까지 비교하세요. 결과가 다르면 첫 번째로 다른 기록부터 추적하세요.',
    },
    92: {
        'notes': [
            '완성 확인은 작은 예제의 출력 하나로 끝나지 않습니다. 세 언어 구현이 같은 CSV에서 총 학습 시간과 언어별 합계를 같게 계산하는지 검증하세요. `assert total == 60`은 총합 60이라는 계약을 검사합니다. 입력 건수, 필터 결과, 잘못된 행 처리도 별도 사례로 확인해야 합니다.',
            '표시 기능을 추가하더라도 계산된 데이터와 화면에 그리는 동작을 분리하세요. 예를 들어 10분당 막대 하나라면 30분은 막대 3개입니다. 0분, 큰 값, 빈 입력에서는 어떻게 표시할지도 정하고, 원래 합계가 바뀌지 않았는지 다시 검증하세요.',
        ],
        'predict': '1. 세 기록의 시간을 합해 60을 구합니다. 2. assert로 공통 기대값을 확인합니다. 3. 완료 메시지를 출력합니다. `C30 + Python20 + Rust10 → 60 → 테스트 통과` 흐름으로 실제 출력 `Study Log Analyzer 완료: 60`이 됩니다. 핵심 `assert total == 60`은 기대값을 검증하고 통과해야 다음으로 가는 자리에 쓰입니다.',
        'guided': '원본과 수정본 모두 `Study Log Analyzer 완료: 60`을 출력한다. 추가된 assert도 통과하므로 print까지 도달합니다. assert는 기대값을 검증할 뿐 출력값 계산과 표시에는 관여하지 않습니다. 계산값 total은 계속 60이고, 새 assert는 길이 3을 검사하여 통과 여부만 결정하므로 출력이 같다. 값이 실제로 바뀌지 않았으므로 중간 값이 달라졌다고 쓰지 않는다.',
        'debug_outcome': 'runtime-error',
        'debug': '틀린 줄은 `assert total == 0`입니다. 실제 합 60과 기대 0이 달라 단언이 실패하므로 AssertionError라는 런타임 오류가 나고 프로그램이 멈춰 완료 메시지가 출력되지 않습니다. 오류 분류는 런타임 오류입니다. 흐름이 깨지는 첫 순간은 기대값 비교입니다. 고친 줄 `assert total == 60`에서는 `동일 입력·출력 명세와 테스트 완료` 동작이 지켜집니다. 정상 코드는 합산→기대값 검증→완료 표시의 순서로 `Study Log Analyzer 완료: 60`을 출력합니다.',
        'qout': '먼저 세 기록의 시간을 합해 60을 구합니다. 이어서 assert가 공통 기대값을 확인하고 통과하므로 완료 메시지가 출력되어 `Study Log Analyzer 완료: 60`이 됩니다. `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 합산과 검증이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다.',
        'qmodel': '공통 명세가 참인지 테스트로 고정한 후 선호 언어에서 ASCII 시각화 한 가지를 추가한다는 뜻은, 약속을 먼저 코드로 묶는다는 뜻입니다. `한 언어의 결과만 보고 나머지 구현도 같다고 가정함`은 반대 사례인데, 한쪽만 보면 다른 쪽 위반을 놓치기 때문입니다.',
        'qpit': '먼저 확인할 실수는 `한 언어의 결과만 보고 나머지 구현도 같다고 가정함`입니다. 잘못된 코드는 기대값이 어긋나 AssertionError로 멈춥니다. 입력 건수와 필터 결과도 별도로 확인하세요.',
        'qtrans': '세 언어로 옮길 때 보존해야 하는 것은 `동일 입력·출력 명세와 테스트 완료`라는 의미와 입력·출력 계약입니다. 세 언어의 같은 명세 테스트로 동작을 구현하고, 실행 결과 `Study Log Analyzer 완료: 60`으로 대조하면 옮김이 맞는지 확인할 수 있습니다.',
        'boundary': '0분, 큰 값, 빈 입력에서는 어떻게 표시할지 정하고 원래 합계가 바뀌지 않았는지 다시 검증하세요. 계산과 그리기를 분리하세요.',
    },
}
