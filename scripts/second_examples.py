"""A second, solved example for each generated Day; authored in Korean."""

MORE = {
    2: """이번에는 이름이 비어 있을 때를 보세요. `name = ""`이고 `print("안녕,", name)`이면 빈 이름 자리까지 공백이 붙습니다. 입력을 그대로 쓰는 앱이라면 빈 문자열 검사를 먼저 해 `name = name or "익명"`으로 바꿀 수 있습니다. 그러면 출력은 `안녕, 익명`입니다. **입력을 정한다 → 필요한 경우 고친다 → 출력한다**라는 순서를 익혀 보세요.""",
    3: """`printf("%d + %d = %d\\n", 10, 20, 10 + 20);`을 읽어 봅시다. `%d`가 셋이라 뒤의 정수도 셋이며, 자리를 왼쪽부터 순서대로 채워 `10 + 20 = 30`이 출력됩니다. `%d` 셋에 숫자 둘만 준다면 세 번째 칸이 어떻게 채워질지 C가 안전하게 보장하지 않습니다. 형식과 인수의 **개수·타입**을 같이 확인하세요.""",
    4: """두 코드를 비교하세요: `let mut n = 1; n += 1;`은 같은 바인딩의 값을 2로 고치지만 `let n = 1; let n = n + 1;`은 새 바인딩을 만듭니다. 두 코드에서 `println!("{n}")`은 모두 2입니다. 앞의 `let`을 다시 쓴 것인지 `mut`로 기존 값을 갱신한 것인지 설명할 수 있어야 결과가 같아도 다른 동작임을 압니다.""",
    5: """`(8 + 4) / 2`는 괄호 안을 먼저 계산하므로 `12 / 2 = 6.0`입니다. `8 + 4 / 2`는 `4 / 2 = 2.0`부터 계산해 `10.0`입니다(Python의 `/`는 실수 결과). 괄호를 지우고 계산하면 단순히 쓰는 모양만 달라지는 것이 아니라 수식의 의미가 달라집니다. 손으로 계산 순서를 번호로 매겨 보세요.""",
    6: """`int a = 7, b = 2;`라면 `a / b`는 3, `(double)a / b`는 3.5, `(double)(a / b)`는 3.0입니다. 세 번째 식에서는 괄호 **안에서 이미 7/2를 정수로 계산**한 뒤 3을 실수로 바꿨습니다. 평균을 낼 때 어느 시점에 실수 타입이 되었는지 화살표로 표시하세요.""",
    7: """`minutes = 15; print(f"{minutes + 5}분")`은 `20분`을 보여 줍니다. 이 출력문은 `minutes`를 바꾸지 않으므로 곧바로 `print(minutes)`를 하면 15입니다. 중괄호의 계산값, 문자열로 합친 결과, 변수에 저장된 현재 값은 서로 다른 대상입니다. `f`를 빼면 `{minutes + 5}분`이 그대로 나타납니다.""",
    8: """`focused = False; minutes = 40`인 경우 `minutes >= 30 && focused`는 앞이 참이어도 뒤가 거짓이므로 전체가 거짓입니다. `minutes >= 30 || focused`로 바꾸면 둘 중 앞이 참이므로 참입니다. `&&`와 `||`는 상황에 따라 요구 조건 자체가 달라지므로, 참/거짓을 두 칸씩 적은 표로 결정하세요.""",
    9: """`score = 2.75`에서 `f"{score:.1f}"`를 출력하면 `2.8`입니다. 그러나 `score`를 다시 출력하면 2.75로 남아 있습니다. 숫자를 화면에 **어떻게 보일지** 정하는 표현과 계산에 사용할 원래 값은 분리됩니다. 표시한 글자를 다시 수에 더하려면 `float(...)` 변환도 필요합니다.""",
    10: """`if (value > INT_MAX - extra)`는 `value + extra`를 실제로 수행하기 **전에** 범위를 확인합니다(두 수가 0 이상인 경우). 예를 들어 최대 허용값이 100이라고 가정하면 98에 3을 더하려 할 때 `98 > 100-3`이 참이라 거부합니다. 이 작은 수 모형을 이해한 다음 실제 `INT_MAX`에도 같은 판단을 적용하세요.""",
    11: """`let minutes: u32 = 2; let seconds = minutes * 60;`이면 `seconds`는 120이며 문맥상 `u32`입니다. 음수 시간을 표현해야 한다면 `u32`가 맞는 타입인지 다시 고르세요. 0분과 매우 큰 분을 넣어 보고 덧셈·곱셈 결과가 타입 범위 안에 있어야 한다는 이유도 생각해 보세요.""",
    12: """`minutes = 60`이면 `30 <= minutes <= 60`의 두 비교는 모두 참입니다. 61로 바꾸면 앞쪽은 참, 뒤쪽은 거짓이라 전체는 거짓입니다. 경계 30과 60은 포함되고 29와 61은 빠진다는 사실을 **각각의 비교 결과**로 설명해 보세요. C/Rust로 옮길 때 비교식을 그대로 이어 쓰지 않습니다.""",
    13: """`minutes = 29`와 `minutes = 30`을 따로 넣어 보세요. 조건이 `minutes >= 30`이라면 29는 거짓 블록, 30은 참 블록으로 갑니다. 두 갈래를 모두 실행하는 상황은 없습니다. 블록 밖으로 `print("검사 완료")`를 옮기면 이 줄만 두 경우 모두 실행된다는 점을 직접 확인하세요.""",
    14: """합격선을 30에서 31로 바꿨다면 입력값이 30일 때의 결과가 먼저 달라집니다. 예전 조건 `minutes >= 30`에서는 참, 새 조건 `minutes >= 31`에서는 거짓입니다. 29는 양쪽 모두 거짓이고 31은 양쪽 모두 참입니다. **새 경계 바로 아래·경계·바로 위**를 다시 골라 검사하세요.""",
    15: """`match 3 { 2 => "둘", 3 => "셋", _ => "기타" }`의 결과는 `"셋"`입니다. `3 =>`를 지우면 3은 `_` 갈래에서 `"기타"`가 됩니다. 갈래를 확인할 때는 입력 값과 패턴을 하나씩 비교하고, `_`가 앞 갈래보다 먼저 놓였다면 뒤의 경우가 도달할 수 없다는 점도 기억하세요.""",
    16: """`total = 0`에서 `[5, 10, 5]`를 차례로 더하면 표는 `0 → 5 → 15 → 20`입니다. 두 번째 항목에서 합계가 15가 되는 이유는 새 값 10만 남기지 않고 기존 5에 10을 더했기 때문입니다. 시작값을 1로 바꾸면 최종값은 21이니, 시작값이 결과에 어떻게 남는지도 확인하세요.""",
    17: """`n=1`에서 `while(n>0)` 본문은 한 번 실행되고 `n--` 후 0이 됩니다. 다음 조건 검사에서 거짓이라 끝납니다. 처음부터 `n=0`이면 본문은 0번 실행됩니다. n을 1 줄이는 줄을 지우면 1이 영원히 그대로여서 멈추지 않는다는 점이 종료 조건의 핵심입니다.""",
    18: """`let answer = loop { break 7; };`이라고 적으면 `answer`는 7입니다. `break`가 반복을 끝내는 동시에 값도 되돌립니다. `let answer = loop { break; };`에서는 7 같은 정수 대신 값이 없는 단위 타입 `()`이 결과이므로, 정수가 필요한 곳에 넣으면 타입이 맞지 않습니다.""",
    19: """`for a in [1,2]` 안에 `for b in [3,4,5]`가 있다면 `(1,3), (1,4), (1,5), (2,3), (2,4), (2,5)` 순으로 여섯 번 실행됩니다. 두 번째 바깥 회차에서는 안쪽이 처음인 3부터 다시 시작합니다. 안쪽 모음이 비어 있으면 바깥이 돌아도 짝은 만들어지지 않습니다.""",
    20: """`int twice(int x) { return x * 2; }`를 `printf("%d", twice(4));`에서 호출하면 `x=4`가 된 뒤 8이 호출한 자리로 돌아와 출력됩니다. 함수 안에 `printf`가 없는 데도 화면에 값이 나타난 이유는 **호출자가 출력했기 때문**입니다. `return`과 `printf`를 한 동작으로 착각하지 마세요.""",
    21: """`total([2,3])`이 5를 돌려준 뒤 `total([10])`을 다시 부르면 새 함수 호출의 `result`는 0에서 시작해 10을 돌려줍니다. 5+10=15가 되면 두 번의 호출이 상태를 잘못 공유한 셈입니다. 입력이 빈 리스트라면 반복이 한 번도 실행되지 않아 0을 반환합니다.""",
    22: """`fn triple(n: i32) -> i32 { n * 3 }`에서 `triple(4)`는 12입니다. 마지막 줄을 `{ n * 3; }`으로 바꾸면 계산을 하고 버리므로 `i32`를 반환하겠다는 선언과 맞지 않습니다. `return n * 3;`을 쓰면 다시 정수 12를 돌려줄 수 있습니다.""",
    23: """바깥에 `name = "밖"`이 있고 `def greet(): name = "안"; return name`이라면 `greet()`는 `"안"`을 돌려주어도 바깥 `name`은 `"밖"`입니다. 지역 이름은 함수가 끝나면 사라지고, 바깥 이름을 고치려면 호출 결과를 `name = greet()`처럼 받아야 합니다.""",
    24: """`sum_to(2)`의 호출을 펼치면 `2 + sum_to(1)`, 다시 `2 + 1 + sum_to(0)`이 됩니다. 맨 아래에서 `sum_to(0)=0`을 반환한 다음 1, 3 순으로 위로 올라옵니다. 아래로 내려가는 호출과 위로 올라오는 계산을 한 줄로 뭉개지 말고 두 번에 나눠 따라가세요.""",
    25: """`"oops".parse::<u32>()`는 숫자 0이 되지 않고 오류 `Err(...)`가 됩니다. `match`의 성공 갈래에서만 더하기를 수행하고 실패 갈래에서는 수정할 입력을 알려 주세요. 잘못된 값을 0으로 **몰래 바꾸면** 진짜 0분 기록과 오류 기록을 구별할 수 없습니다.""",
    26: """**세 칸을 실제로 그려 보세요.** `int minutes[3] = {10,20,30};`을 실행하면 다음과 같이 채워집니다. 번호는 0부터 시작하지만, 칸의 개수는 3입니다.

| 인덱스 | 0 | 1 | 2 |
| --- | ---: | ---: | ---: |
| 저장된 분 | 10 | 20 | 30 |

`minutes[1]`을 읽으면 가운데 칸 20입니다. 괄호 속 숫자 1을 2로 바꿔 `minutes[2]`을 읽으면 마지막 칸 30이 나옵니다. 출력이 달라진 까닭은 배열을 바꿔서가 아니라 **읽는 칸을 바꿨기 때문**입니다. `minutes[3]`은 네 번째 칸인데 실제 칸이 세 개라서 읽으면 안 됩니다.

이번에는 배열 값을 모두 더해 보세요. `total`은 처음에 0이고, 반복할 때마다 값 하나가 더해집니다.

```c
#include <stdio.h>
int main(void) {
    int minutes[3] = {10, 20, 30};
    int total = 0;
    for (int i = 0; i < 3; i++) {
        total += minutes[i];
    }
    printf("%d\\n", total);  // 60
    return 0;
}
```

반복 시작 때 `i=0, total=0`, 첫 번 뒤 10, 둘째 번 뒤 30, 셋째 번 뒤 60입니다. 그다음 `i=3`이면 `i < 3`이 거짓이라 본문에 들어가지 않습니다. `i <= 3`으로 쓰면 마지막에 없는 `minutes[3]`을 읽는 위험이 생깁니다. C의 배열은 이런 실수를 실행 전에 자동으로 해결해 주지 않으므로 조건을 직접 맞춰야 합니다.

**같은 작업을 다른 언어로:** Python에서는 `minutes = [10,20,30]; print(minutes[2])`가 30입니다. Rust에서는 `let minutes = [10,20,30]; println!("{}", minutes[2]);`가 30입니다. 두 언어 모두 범위를 넘는 접근을 오류로 드러내지만 C에서는 그 결과를 믿을 수 없습니다. 지금 단계에서는 세 언어의 문법보다 **세 칸의 번호가 0·1·2라는 공통점**을 먼저 잡으세요.""",
    27: """`items = [1,2]; items.append(3); print(len(items))`의 결과는 3입니다. 추가 전에는 두 항목, 추가 뒤에는 세 항목입니다. `print(items.append(4))`를 실행하면 리스트에 4가 추가되지만 화면에는 `None`이 표시됩니다. `append`의 반환값을 새 리스트로 오해하지 마세요.""",
    28: """Python에서 `[10,20]`에 30을 붙이면 `[10,20,30]`, Rust에서 `let mut v = vec![10,20]; v.push(30);`을 해도 같은 세 값을 얻습니다. Rust의 `push`는 Vec를 직접 바꾸므로 `mut`가 빠지면 컴파일되지 않습니다. C 고정 배열은 길이 2로 선언한 후 세 번째 칸에 바로 쓰면 안 됩니다.""",
    30: """배열 `{3,6,9}`에서 `int *p = values;`를 만들면 `*p=3`, `*(p+1)=6`, `*(p+2)=9`입니다. 괄호가 없으면 `*p + 1`은 **현재 값 3에 1을 더한 4**라 결과가 다릅니다. '다음 원소로 이동한 뒤 읽기'는 `*(p+1)`, '읽고 나서 숫자에 1 더하기'는 `*p+1`로 구별해 보세요. 끝 뒤 `p+3`을 읽으면 안 됩니다.""",
    31: """`a = [[10]]; b = a.copy(); b[0].append(20)`을 따라가면 바깥 리스트는 두 개지만 **안쪽 리스트는 하나**입니다. 따라서 `print(a)`는 `[[10, 20]]`입니다. 반대로 `b.append([30])`은 새 바깥 리스트에만 원소를 붙이므로 `a`에는 안 보입니다. 무엇이 새로 복사됐는지 두 겹으로 그리세요.""",
    32: """`let first = String::from("hi"); let second = first.clone();` 뒤에는 두 이름을 모두 출력할 수 있습니다. `clone()` 대신 `let second = first;`로 바꾸면 첫 이름을 다시 읽을 수 없습니다. 작은 정수 `let b = a;`에서는 `Copy`로 값 자체가 복사될 수 있으므로, 어떤 타입이 쓰였는지도 확인하세요.""",
    33: """`let topic = String::from("Rust"); let view = &topic;`이면 `println!("{view}")` 뒤에도 `println!("{topic}")`을 쓸 수 있습니다. `view`는 글자의 주인이 아니라 잠시 읽는 길입니다. 그 참조를 쓰는 동안 원래 소유자의 데이터를 마음대로 변경할 수 없다는 제한이 함께 붙습니다.""",
    34: """함수 `sum(const int *a, size_t n)`에 `{4,5,6}`과 길이 3을 넘기면 `a[0]+a[1]+a[2] = 15`입니다. 같은 포인터에 길이 4를 잘못 넘기면 함수가 없는 네 번째 원소까지 읽습니다. 포인터 값만큼이나 **호출자가 알려 주는 길이**가 정확해야 하므로 두 값을 한 세트로 기록하세요.""",
    35: """`a=[1]; b=a; b.append(2)`이면 둘 다 `[1,2]`를 봅니다. `b=a.copy(); b.append(3)`을 그다음 실행하면 a는 `[1,2]`, b는 `[1,2,3]`입니다. 중간에 바뀐 것은 이름 b가 가리키는 대상이지 a의 내용이 새 리스트로 복제됐다는 뜻이 아닙니다.""",
    36: """네 값 `[5,10,15,20]`에서 `[1..3]`은 인덱스 1과 2를 포함해 `[10,15]`입니다. 인덱스 3은 제외됩니다. `[1..4]`는 마지막 20까지 포함하고 `[4..4]`는 비어 있습니다. 끝 번호가 원소 개수와 같아도 **끝을 포함해 읽는 것은 아니므로** 유효할 수 있습니다.""",
    37: """`char word[] = "hi";`는 보이는 h·i 두 글자에 마지막 `\\0`까지 세 칸을 사용합니다. `strlen(word)`는 2이고 `sizeof word`는 3입니다. `char word[2] = {'h','i'};`는 종료 문자가 없으니 그대로 문자열 함수에 넘기지 마세요. **배열 크기와 문자열 길이**를 숫자 하나로 뭉뚱그리면 위험합니다.""",
    38: """`text = "가a"`라면 `len(text)`는 두 코드 포인트를 세어 2입니다. 그러나 UTF-8 바이트는 '가'에 3바이트, a에 1바이트라 4바이트입니다. 따라서 파일의 바이트 길이를 제한할 때 단순 `len(text) <= 3`만 검사하면 요구를 어길 수 있습니다. 표시 글자와 저장 바이트의 기준을 분리하세요.""",
    39: """`let mut s = String::from("C"); s.push_str("와 Rust");`이면 s가 소유한 문자열이 `C와 Rust`가 됩니다. `let view: &str = &s;`로 빌려 읽을 수 있습니다. `String`을 함수에 넘겨 소유권을 옮기는 방식과 `&str`로 읽기만 빌려 주는 방식 중 어느 것이 필요한지 먼저 정하세요.""",
    40: """`int *p = malloc(sizeof *p);` 다음에는 `if (p == NULL)`로 실패를 확인합니다. 성공이라면 `*p=7`로 기록하고 읽은 뒤 `free(p)`로 사용을 마칩니다. `free(p); printf("%d", *p);`는 이전 주소가 변수에 남아 보여도 **해제한 공간을 다시 읽는 잘못된 코드**입니다. 두 번째 `free(p)`도 하면 안 됩니다.""",
    41: """`let boxed = Box::new(7); println!("{}", *boxed);`는 7을 출력합니다. Box가 변수의 범위를 벗어나면 관리하던 메모리를 함께 정리합니다. `let other = boxed;`로 이동시킨 뒤에는 `boxed`가 아닌 `other`가 소유하므로 옛 이름으로 다시 출력하면 컴파일 단계에서 막힙니다.""",
    42: """`a=[[1]]; b=a.copy(); b[0].append(2)`이면 a까지 `[[1,2]]`가 됩니다. 이번엔 `b.append([3])`을 더하면 a는 그대로 `[[1,2]]`, b만 `[[1,2],[3]]`입니다. 같은 얕은 복사여도 **안쪽 객체 수정**과 **바깥 리스트 추가**는 다른 결과를 냅니다.""",
    43: """`void set(int *out) { *out = 9; }`와 호출 `int n=1; set(&n);` 뒤 n은 9입니다. 함수에 주소 `&n`을 줬으므로 `*out`이 실제 n의 저장 칸에 접근합니다. 만약 함수 안에서 `out = NULL;`만 하면 함수의 지역 포인터가 바뀔 뿐, 호출자 n에는 9가 적히지 않습니다.""",
    44: """`values=[1]; add(values)`에서 함수 `add(items)`가 `items.append(2)`를 호출하면 values는 `[1,2]`입니다. 하지만 함수에서 `items = [2]`만 하면 호출자 values는 `[1]`입니다. **같은 리스트를 바꾸는가, 지역 이름을 다시 연결하는가**를 구분해서 호출 전후의 그림을 그리세요.""",
    45: """`fn first<'a>(x: &'a str, y: &'a str) -> &'a str`가 x 또는 y를 돌려준다면 결과 참조는 두 입력이 모두 살아 있는 범위에서만 사용할 수 있습니다. 호출자가 결과를 저장해도 함수 안에서 만든 임시 문자열의 수명이 늘어나지는 않습니다. 원본을 누가 소유하고 언제 사라지는지 함께 적어 보세요.""",
    46: """`fputs("AB", f)` 뒤 파일 위치는 두 글자를 쓴 끝에 있습니다. `rewind(f); fgetc(f)`를 호출하면 첫 글자 A를 읽습니다. 되감기를 생략하면 끝에서 읽으려 해서 첫 글자가 나오지 않습니다. 파일을 열지 못했다면 `f`가 `NULL`이므로 읽기·쓰기보다 먼저 실패를 처리해야 합니다.""",
    47: """`StringIO("10\\n20\\n")`에서 각 줄에 `int(line)`을 적용하면 10과 20이 되어 합계는 30입니다. 입력을 `"10\\nxyz\\n"`로 바꾸면 두 번째 변환에서 `ValueError`가 납니다. 오류를 조용히 0으로 세지 말고 어떤 줄을 고쳐야 하는지 사용자에게 알려 주세요.""",
    48: """`Cursor::new(b"abc")`를 `read_to_string(&mut text)`로 읽어 성공했다면 text에는 `abc`가 들어갑니다. 성공 결과의 숫자는 **읽은 바이트 수 3**이고 텍스트 자체가 아닙니다. UTF-8로 해석할 수 없는 바이트가 들어오면 `Result::Err`가 될 수 있으니 읽기 실패도 따로 처리하세요.""",
    49: """`int n=30; int *view=&n; *view=31;`의 마지막에서 `n`을 출력하면 31입니다. 주소 저장 `view=&n`이 n의 값을 복사한 것은 아닙니다. 같은 저장 칸을 다른 경로로 읽고 쓰는 것이므로 `view`와 `*view`의 역할을 각각 적으세요.""",
    50: """`struct Session a={.minutes=10}; struct Session b={.minutes=20};`이라면 `a.minutes`는 10, `b.minutes`는 20입니다. 형식은 같아도 값은 두 개입니다. `a.minutes=15`로 바꿔도 b가 20인 이유는 각 구조체 변수가 자기 필드를 저장하기 때문입니다.""",
    51: """`a=Session(10); b=Session(20)`이라고 두 객체를 만든 뒤 `a.minutes += 5`를 하면 a는 15, b는 20입니다. `self.minutes`가 각 인스턴스에 속하기 때문입니다. 메서드 안에서 지역 이름 `minutes`를 다시 대입하는 것과 `self.minutes`를 바꾸는 것을 구별하세요.""",
    52: """`impl Session { fn minutes(&self) -> u32 { self.minutes } }`라면 `session.minutes()`는 구조체의 필드를 읽어 반환합니다. 수신자가 `&self`여서 호출 뒤에도 session을 사용할 수 있습니다. 필드를 바꾸는 메서드라면 `&mut self`가 필요하고 호출자 역시 변경 가능한 값이어야 합니다.""",
    53: """`enum Status { Waiting, Done }`에서 Done 기록을 `match`로 확인하면 완료 동작을 택합니다. Waiting을 아직 완료로 처리하면 상태 이름과 행동이 어긋납니다. 문자열로 상태를 임의 입력받으면 철자를 틀릴 수 있지만 enum은 가능한 상태를 코드에서 한정합니다.""",
    54: """`fn show<T: std::fmt::Display>(value: T)`는 정수 3도, 출력 가능한 문자열도 받을 수 있습니다. 다만 어떤 `T`든 가능하다는 뜻은 아니며 `Display` 구현이 있어야 합니다. `T`는 **타입을 바꿔 쓸 자리**, `Display`는 **그 자리에 들어올 자격**이라고 나누어 생각하세요.""",
    55: """`total([10,20]) == 30`이 참일 때 `assert`는 화면에 아무것도 표시하지 않습니다. 기대값을 31로 잘못 적으면 `AssertionError`가 납니다. 함수의 결과가 바뀐 것이 아니라 **검사 기준과 실제 결과가 달라졌다**는 뜻입니다. 빈 목록에 대한 `assert total([]) == 0`도 추가해 보세요.""",
    56: """`let s=Session {minutes: 31}; assert_eq!(s.minutes,31);`은 통과합니다. 오른쪽 기대값만 30으로 되돌리면 실패하므로 어떤 값이 실제이고 어떤 값이 기대인지 메시지에서 확인하세요. 테스트를 통과시키려고 실제값과 기대값을 모두 아무 숫자로 바꾸는 것은 문제의 계약을 검증하지 못합니다.""",
    58: """작업 A를 하고 B를 한 뒤 `undo.pop()`을 부르면 B가 나와야 합니다. `['A','B'] → ['A']`로 남고, 한 번 더 취소하면 A가 나와 빈 목록이 됩니다. 이미 빈 스택에서 또 취소한다면 오류를 그대로 보여 주기보다 '취소할 작업 없음'이라는 동작을 정해야 합니다.""",
    59: """접수 순서가 A→B→C라면 `append(A)`, `append(B)`, `append(C)`를 수행한 큐에서 첫 `popleft()`는 A, 두 번째는 B를 돌려줍니다. `pop()`을 사용하면 마지막 C가 나와 규칙이 바뀝니다. 접수 순서를 실제로 줄로 적고 어느 끝에서 꺼내는지 표시해 보세요.""",
    60: """원형 큐 용량이 3이고 머리 위치가 2라면 다음 위치는 `(2+1)%3=0`입니다. 숫자 0으로 돌아왔더라도 아직 0번 칸에 **처리되지 않은 값이 있다면 덮어쓰면 안 됩니다.** 빈 상태·가득 찬 상태를 항목 수 `count`로 구별한다고 정하면 `count=3`에서 새 삽입을 거부합니다.""",
    61: """A(값 7)의 `next`가 B(값 9)의 주소라면 `A.next->value`는 9입니다. 먼저 `A.next`로 B를 찾고 그다음 `->value`로 9를 읽습니다. B.next가 `NULL`이면 다음 방문은 여기서 멈춰야 하며 `B.next->value`를 읽으려 하면 유효한 노드가 없습니다.""",
    62: """A와 B 사이에 C를 넣는다고 해 봅시다. 기존 연결 `A.next=B, B.prev=A`를 `A.next=C, C.prev=A, C.next=B, B.prev=C`로 모두 바꿉니다. A→C→B로 앞으로 갔다가 B→C→A로 돌아올 수 있으면 일치합니다. 한쪽 포인터만 고치면 되돌아갈 때 연결이 어긋납니다.""",
    63: """동일한 `1,2,3`을 순서대로 넣었을 때 스택 첫 `pop()`은 3이고 큐 첫 `popleft()`는 1입니다. 어느 자료구조가 더 '좋은지'는 문제가 정하는 순서에 달렸습니다. 가장 최근 편집을 취소해야 한다면 3, 가장 먼저 접수된 일을 처리한다면 1이 맞습니다.""",
    64: """나무 모양 `A(B, C(D, None))`의 노드를 세면 B는 1, D는 1, C는 자기 자신을 포함해 2, A는 `1+1+2=4`입니다. `None`의 개수를 노드에 더하면 답이 부풀어 오릅니다. 자식의 계산이 끝나 **부모로 돌아오는 순서**를 적어 보세요.""",
    65: """뿌리 10에 왼쪽 5·오른쪽 15가 있다면 키 5는 `5<10`이라 왼쪽에서 발견합니다. 키 12는 오른쪽 15를 만나 `12<15`라 그 왼쪽이 비었으면 없다고 답합니다. 정렬 규칙이 깨져 12가 엉뚱한 곳에 놓여 있다면 이 경로 탐색은 답을 보장하지 못합니다.""",
    66: """우선순위를 수 `4,1,3`으로 나타내 최소 힙에 넣으면 첫 `heappop()`은 1입니다. 나머지 내부 배열이 언제나 `[3,4]`처럼 완전히 정렬된 것은 아닙니다. 힙 규칙은 **부모가 자식보다 크지 않다**는 약속이고, 정렬된 전체 목록이 필요하면 원소를 하나씩 꺼내야 합니다.""",
    67: """주제 순서가 `C, Python, C`라면 집계표는 첫 C 후 `{C:1}`, Python 후 `{C:1, Python:1}`, 마지막 C 후 `{C:2, Python:1}`입니다. 첫 등장에만 기본값 0을 쓰고 이후에는 저장된 1을 읽습니다. 주제 이름이 대소문자까지 같은지에 따라 별도 키가 될 수 있습니다.""",
    68: """`A:[B,C]`, `B:[C]`, `C:[]`라면 A에서 B와 C로 바로 갈 수 있고 B에서도 C로 갈 수 있습니다. 그러나 C에서 A로 가는 간선은 적혀 있지 않습니다. **길이 2인 경로 A→B→C**와 **직접 간선 A→C**를 구별해 보세요.""",
    69: """A가 B,C를 이웃으로 갖고 B가 D를 이웃으로 갖는다면 큐는 `[A] → [B,C] → [C,D] → [D]`로 변합니다(방문 표시를 넣을 때 한다고 가정). 그래서 A에서 한 단계 거리인 B,C를 먼저, 두 단계인 D를 다음에 처리합니다. 큐를 스택으로 바꾸면 이 층별 순서가 사라집니다.""",
    70: """방문할 고객이 `[A,B,C]`라면 큐에서 첫 고객 A를 처리해야 순서를 지킵니다. 같은 데이터를 작업 취소 기록이라고 가정하면 스택에서는 최근 C가 먼저 나와야 합니다. 사전은 고객 이름으로 기록을 찾을 때, 집합은 이미 처리한 고객의 중복을 확인할 때 사용합니다.""",
    71: """원소가 4개인 목록을 한 번씩 검사하면 핵심 방문은 4번입니다. 8개면 8번이라 두 배가 됩니다. 모든 원소 쌍을 중첩 반복하면 4개에서 16번, 8개에서 64번으로 네 배입니다. `O(n)`과 `O(n²)`를 비교할 때는 컴퓨터 속도보다 **입력이 늘어난 비율**을 보세요.""",
    72: """정렬된 `[2,4,4,8]`에서 4가 **처음 나오는 위치**는 1입니다. 가운데가 4여도 거기서 바로 끝내면 2를 반환할 수 있습니다. 조건이 맞을 때도 `hi=mid`로 왼쪽 절반을 남겨 탐색이 끝나면 1을 얻습니다. 목표가 5라면 끝난 위치의 값이 5인지 확인하고 없다고 답해야 합니다.""",
    73: """`[4,2,3]`에서 두 번째 값 2를 빼 들고 큰 4를 한 칸 오른쪽으로 밀어 `[2,4,3]`을 만듭니다. 다음에는 3을 빼 들고 4를 밀어 `[2,3,4]`가 됩니다. 매 회차 시작 전에 **왼쪽 부분이 이미 정렬돼 있음**을 믿고 범위를 한 칸 넓힙니다.""",
    74: """정렬된 두 목록 `[1,4]`와 `[2,3]`을 합칠 때 맨 앞끼리 비교해 1, 다음 2, 다음 3을 고르고 왼쪽에 남은 4를 붙입니다. `[1,2,3,4]`입니다. 두 목록이 원래 정렬되어 있지 않다면 앞 원소만 비교하는 병합 규칙으로는 정렬 결과를 보장하지 못합니다.""",
    75: """`[3,1,3,2]`에서 피벗을 첫 3으로 고르면 작은 쪽 `[1,2]`, 크거나 같은 쪽 `[3]`입니다. 합치면 `[1,2,3,3]`입니다. 오른쪽을 `v > pivot`으로만 만들어 같음 3을 누락하면 원소가 하나 사라집니다. **정렬 전후 항목 개수가 같은지** 먼저 확인하세요.""",
    76: """`[9,4,7,2]`를 `[9,4]`와 `[7,2]`로 나누면 각각의 최댓값은 9와 7, 전체의 최댓값은 9입니다. 원소 하나인 부분에서 더 나누지 않기 때문에 재귀가 끝납니다. 빈 목록의 최댓값은 정해지지 않으니 호출 전에 막거나 별도 실패 값을 정의하세요.""",
    77: """원본 `[9,2,9]`를 `sorted(...)` 하면 `[2,9,9]`이고 `index(9)`는 정렬된 목록에서 첫 번째 9의 인덱스 1입니다. 원본의 첫 9는 인덱스 0이었습니다. **값을 찾는 문제인지 원본 위치를 찾는 문제인지** 먼저 구별해야 정렬로 위치 정보를 잃지 않습니다.""",
    78: """동전 `[4,3,1]`로 6을 만들 때 큰 것부터 고르면 4+1+1로 세 개입니다. 그러나 3+3이면 두 개입니다. 단순히 '큰 동전을 우선'이라는 규칙이 모든 동전 체계에 맞지 않는다는 반례입니다. 어떤 문제에서 그리디가 맞는지 증명하거나 작은 입력을 완전히 탐색해 검증하세요.""",
    79: """두 칸씩 또는 한 칸씩 오를 수 있다면 4칸의 경우 수는 `ways[3] + ways[2]`입니다. 3칸은 3가지, 2칸은 2가지라 5가지입니다. `ways[0]=1`이라는 시작값을 2로 잘못 바꾸면 뒤의 경우 수들도 틀어집니다. **작은 답을 맞게 저장한 뒤** 큰 답을 쌓아 보세요.""",
    80: """`path=[1]`에서 2를 추가하면 `[1,2]`가 한 가지 답입니다. 재귀가 끝나 `pop()`으로 2를 지우면 다시 `[1]`이 되어 다른 선택을 시험할 수 있습니다. 답을 모을 때 바뀌는 같은 `path` 객체를 그대로 저장하면 나중에 비운 경로를 가리킬 수 있으니 완성 순간 복사하세요.""",
    81: """A→B, B→C, C→A인 그래프에서 방문 기록이 없다면 `dfs(A)`가 결국 다시 `dfs(A)`를 불러 끝나지 않습니다. `seen.add(A)`를 한 뒤 B,C를 따라가면 C의 이웃 A는 이미 방문했으므로 돌아옵니다. 순환 간선을 허용하는 그래프일수록 방문 표시 시점을 확인하세요.""",
    82: """A→B 직접 비용이 8이고 A→C 비용이 2, C→B 비용이 3이면 B의 거리 후보는 처음 8에서 `2+3=5`로 줄어듭니다. 큐에 예전 8이 남아 있더라도 나중에 꺼냈을 때 현재 거리 5보다 크면 건너뜁니다. **음수 비용이 없다**는 전제가 없으면 이 알고리즘을 그대로 쓰지 마세요.""",
    83: """CSV 행 `2026-10-01,Python,변수,30,pass`를 다섯 칸으로 분리하면 번호 0=날짜, 1=언어, 2=주제, 3=분, 4=결과입니다. `fields[3]`은 글자 `"30"`이지 정수 30이 아닙니다. 연습용 `split(",")`로는 따옴표 속 쉼표를 안전하게 처리하지 못하니 실제 파일은 CSV 파서를 사용하세요.""",
    84: """두 C 행이 10분과 30분, Rust 행이 20분이라면 C 필터 결과는 건수 2·합계 40분입니다. 세 언어 중 하나에서 60분이 나오면 필터가 Rust까지 포함했는지 살펴보세요. 같은 fixture로 같은 결과를 기대한다는 약속이 있어야 구현끼리 비교할 수 있습니다.""",
    85: """헤더 `minutes` 아래 문자열 `"30"`은 `int`를 거쳐 숫자 30이 됩니다. `"30x"`는 `ValueError`, minutes 열이 없으면 `KeyError`로 실패할 수 있습니다. 둘 다 '30분'으로 대충 해석하면 잘못된 입력을 은폐합니다. 몇 번째 행의 어떤 필드가 잘못됐는지 알려 주세요.""",
    86: """언어별 기록이 `C:20`, `Rust:20`, `Python:30`이라면 합계 내림차순 뒤 동점 이름 오름차순에서 Python이 앞이고 C와 Rust는 이름순이 됩니다. 두 구현의 결과를 비교할 때 동점 규칙이 없다면 같은 합계라도 순서가 달라질 수 있습니다.""",
    87: """입력 `"10xyz"`를 `strtol`로 읽으면 숫자 10까지 읽었어도 `end`는 x를 가리키므로 **전체가 숫자는 아닙니다**. `""`는 아무 숫자도 읽지 않아 `end==raw`입니다. 두 경우를 모두 거부해야 엉뚱한 기록을 정상 분으로 집계하지 않습니다.""",
    88: """길이 3 배열 `[10,20,30]`을 더할 때 `i=0` 뒤 합계 10, `i=1` 뒤 30, `i=2` 뒤 60입니다. `i=3`에서는 `i<3`이 거짓이므로 끝납니다. `i<=3`으로 바꾼 코드는 합계 60 뒤에 없는 원소를 더하려 하므로 검사할 값을 예측하기보다 **범위 밖 접근을 바로 고쳐야** 합니다.""",
    89: """`"31".parse::<u32>()`는 `Ok(31)`이고 `"-1".parse::<u32>()`는 음수를 담지 못해 오류입니다. 성공에서만 Session의 분 필드를 채우세요. 정수 변환에 성공해도 999999분이 실제 한 학습 세션으로 적절한지는 별도 범위 검사가 필요합니다.""",
    90: """20분과 30분 기록을 `iter().sum::<u32>()`으로 합하면 50분입니다. 사전의 항목이 C:50, Rust:50처럼 동점이면 원하는 순서를 이름 기준으로 정해 두어야 두 구현 결과가 같습니다. **합산**, **정렬**, **상위 N개 선택**은 별도 단계로 추적하세요.""",
    91: """`{"C":40,"Rust":20}`과 `{"Rust":20,"C":40}`은 사전 항목의 순서가 다르더라도 같은 키·값이라 동등합니다. 그러나 순위 목록 `[C,Rust]`와 `[Rust,C]`는 순서가 달라 다른 결과입니다. 비교하려는 출력이 집합·사전인지 순서 있는 목록인지 구별하세요.""",
    92: """세 기록이 각각 10·20·30분이면 총합은 60분, 기록 수는 3입니다. 총합만 검사하면 한 건이 빠져도 다른 잘못된 값으로 우연히 60이 될 수 있으므로 건수도 검사합니다. 입력이 없을 때 합계 0, 잘못된 분을 만났을 때 오류 처리도 함께 시험해야 완성 확인이 됩니다.""",
}
