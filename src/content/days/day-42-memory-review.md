---
schemaVersion: 1
contentVersion: 2026.10-f
id: day-42-memory-review
courseId: crp-92
phaseId: phase-04
dayNumber: 42
date: "2026-11-11"
title: 메모리 모델 종합 회상
summary: 주소 공유와 독립 복사를 손으로 구분하고 수명에 따라 안전한 방법을 선택합니다. Python 예시를 추적하고 다른 두 언어로 옮겨 봅니다.
anchorLanguage: python
transferLanguages:
  - c
  - rust
difficulty: intermediate
estimatedMinutes: 50
prerequisites:
  - day-41-box
learningObjectives:
  - "'메모리 모델 종합 회상' 개념이 필요한 상황을 예로 든다."
  - Python 코드의 핵심 줄과 실행 결과를 추적한다.
  - "다음 오류를 발견하고 고친다: 얕은 복사를 중첩 리스트의 깊은 복사로 착각함."
  - 같은 개념을 나머지 두 언어에 적용한다.
concepts:
  - memory review
  - execution trace
  - language transfer
runnerMode: python
reviewOffsets:
  - 1
  - 3
  - 7
  - 14
  - 30
sample: false
exercises:
  - id: ex-day-42-memory-review-predict
    title: 결과를 먼저 예측하기
    kind: predict
    objective: 코드를 실행하지 않고 상태와 출력을 순서대로 추적한다.
    prompt: 아래 코드를 실행하면 어떤 문장이 출력될까요? a=[1]에서 시작해 계산하세요.
    starter: "a = [1]

      b = a.copy()

      b.append(2)

      print(a, b)"
    answer: "[1] [1, 2]"
    hint: a=[1]에서 시작해 b=[1,2]까지 순서대로 적어 보세요. b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않습니다.
    explanation:
      1. a=[1]입니다. 2. copy로 b=[1]을 새로 만듭니다. 3. b만 [1,2]가 되어 함께 출력합니다. `a=[1] → b=[1] 별도 객체 → b=[1,2]` 흐름으로
      실제 출력 `[1] [1, 2]`가 됩니다. 핵심 `a.copy()`는 별도의 바깥 목록을 만드는 자리에 쓰입니다.
    commonMistakes:
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함
      - 실행 전에 출력과 중간 상태를 손으로 확인하지 않음
    language: python
    verification: none
  - id: ex-day-42-memory-review-fill
    title: 핵심 표현 빈칸 채우기
    kind: fill
    objective: 메모리 모델 종합 회상의 핵심 표현을 스스로 적는다.
    prompt: "빈칸을 채워 예시와 같은 결과를 만드세요. 필요한 표현: a.copy()"
    starter: "a = [1]

      b = _____

      b.append(2)

      print(a, b)"
    answer: "a = [1]

      b = a.copy()

      b.append(2)

      print(a, b)"
    hint: 필요한 표현은 복사 범위와 공유 상태 확인 동작을 잇는 a.copy()입니다.
    explanation:
      빈칸에 들어갈 표현은 'a.copy()'입니다. 이 표현이 없으면 실행 결과는 달라집니다. 1. a=[1]입니다. 2. copy로 b=[1]을 새로 만듭니다. 3. b만 [1,2]가
      되어 함께 출력합니다. `a=[1] → b=[1] 별도 객체 → b=[1,2]` 흐름으로 실제 출력 `[1] [1, 2]`가 됩니다. 핵심 `a.copy()`는 별도의 바깥 목록을 만드는 자리에
      쓰입니다.
    commonMistakes:
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함
      - 실행 전에 출력과 중간 상태를 손으로 확인하지 않음
    language: python
    verification: run
  - id: ex-day-42-memory-review-guided
    title: 값을 바꿔 다시 추적하기
    kind: modify
    objective: 입력이 바뀌었을 때 코드의 상태와 출력이 어떻게 변하는지 설명한다.
    prompt: 예시를 직접 타이핑한 뒤 처음 등장하는 숫자를 1에서 2로 바꿔 보세요. 출력이 바뀌는지 먼저 예측하고, 그대로라면 그 이유도 설명하세요.
    starter: "a = [1]

      b = a.copy()

      b.append(2)

      print(a, b)"
    answer: "a = [2]

      b = a.copy()

      b.append(2)

      print(a, b)"
    hint: 처음 등장하는 숫자를 1에서 2로 바꾼 뒤 실행 결과를 먼저 적어 보세요. 원본 출력 '[1] [1, 2]'와 비교해 달라지는 첫 중간 값을 찾으면 됩니다.
    explanation:
      바꾼 뒤 출력은 '[2] [2, 2]'입니다. 원본은 `[1] [1, 2]`, 수정본은 `[2] [2, 2]`이다. 첫 변경 줄 `a = [1]`에서 `a = [2]`로 시작
      값을 바꾸면, 복사본의 추가 뒤에도 원본 첫 값은 바뀐 값 그대로 남는다.
    commonMistakes:
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함
      - 실행 전에 출력과 중간 상태를 손으로 확인하지 않음
    language: python
    verification: run
  - id: ex-day-42-memory-review-debug
    title: 오류 설명하고 고치기
    kind: debug
    objective: 문법 오류와 의미 오류를 구별하고 고친 이유를 설명한다.
    prompt: 아래 코드와 본문의 정상 예제를 비교하여 잘못된 부분을 찾고 고치세요. 특히 얕은 복사를 중첩 리스트의 깊은 복사로 착각함 상황을 확인하세요.
    starter: "a = [1]

      b = a

      b.append(2)

      print(a, b)"
    answer: "a = [1]

      b = a.copy()

      b.append(2)

      print(a, b)"
    hint: 얕은 복사를 중첩 리스트의 깊은 복사로 착각함 상황에서 어긋나는 줄을 b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않습니다. 설명과 대조해 보세요.
    explanation:
      틀린 줄은 `b = a`입니다. 같은 리스트를 보는 별칭이 되므로 뒤의 추가가 원본까지 바꿔 `[1, 2] [1, 2]`가 출력됩니다. 오류 분류는 잘못된 출력입니다. 공유가
      시작되는 첫 순간은 대입 줄입니다. 고친 줄 `b = a.copy()`에서는 `복사 범위와 공유 상태 확인` 동작이 지켜집니다. 정상 코드는 원본 생성→별도 복사→복사본만 추가→둘 다 읽기의 순서로
      `[1] [1, 2]`를 출력합니다.
    commonMistakes:
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함
      - 실행 전에 출력과 중간 상태를 손으로 확인하지 않음
    language: python
    verification: run
  - id: ex-day-42-memory-review-independent
    title: 예시를 가리고 다시 구현하기
    kind: independent
    objective: 설명 없이 같은 개념을 작은 프로그램으로 재현한다.
    prompt: 예시를 가리고 메모리 모델 종합 회상 개념을 적용한 프로그램을 처음부터 작성하세요. 예상 출력과 경계 상황도 말로 설명하세요.
    starter: "# 메모리 모델 종합 회상: 직접 구현"
    answer: "a = [1]

      b = a.copy()

      b.append(2)

      print(a, b)"
    hint: C 포인터는 유효 주소를 직접 다뤄야 합니다. 같은 참조라는 말과 메모리 안전 규칙이 같다는 뜻은 다릅니다. 예상 출력과 경계 조건도 함께 적어 보세요.
    explanation:
      입력·처리·출력·경계 조건을 스스로 설계하세요. C 포인터는 유효 주소를 직접 다뤄야 합니다. 같은 참조라는 말과 메모리 안전 규칙이 같다는 뜻은 다릅니다. 같은 복사 범위와
      공유 상태 확인 동작을 구현하고 실행 결과 '[1] [1, 2]'와 대조할 수 있으면, 예시 답안과 달라도 정답입니다.
    commonMistakes:
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함
      - 실행 전에 출력과 중간 상태를 손으로 확인하지 않음
    language: python
    verification: run
quiz:
  - id: quiz-day-42-memory-review-output
    question: 예시 코드의 출력은 무엇인가요?
    choices:
      - "[1] [1, 2]"
      - 실행 전에 반드시 오류가 난다
      - 아무것도 출력하지 않는다
    answerIndex: 0
    explanation:
      먼저 a가 [1] 리스트를 가리킵니다. 이어서 copy로 별도 [1]이 생기고, b에만 2가 추가되어 a는 [1], b는 [1,2]이므로 출력 `[1] [1, 2]`가 됩니다.
      `실행 전에 반드시 오류가 난다`는 틀린 선택지인데 별칭과 메서드 호출이 모두 정상이기 때문입니다. `아무것도 출력하지 않는다`도 틀린 선택지인데 print가 실제로 호출되기 때문입니다. 다른
      선택지는 이 추적과 맞지 않습니다.
  - id: quiz-day-42-memory-review-model
    question: "'메모리 모델 종합 회상' 개념을 이해하는 데 맞는 설명은?"
    choices:
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함(이것이 정상적인 사용법이다)
      - b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않습니다.
      - 코드가 짧다면 상태 추적은 필요 없다
    answerIndex: 1
    explanation:
      b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않는다는 뜻은, 두 이름이 다른 객체를 본다는 뜻입니다. `얕은 복사를 중첩 리스트의 깊은 복사로 착각함`은
      반대 사례인데, 얕은 복사는 안쪽 가변 객체를 공유하기 때문입니다.
  - id: quiz-day-42-memory-review-pitfall
    question: 다음 중 실습에서 먼저 확인할 오류는?
    choices:
      - 실제 출력과 예측한 출력이 일치함
      - 변경된 입력을 다시 추적하여 결과를 확인함
      - 얕은 복사를 중첩 리스트의 깊은 복사로 착각함
    answerIndex: 2
    explanation:
      먼저 확인할 실수는 `얕은 복사를 중첩 리스트의 깊은 복사로 착각함`입니다. 잘못된 코드는 실행은 되지만 `[1, 2] [1, 2]`라는 잘못된 출력을 냅니다. 중첩 리스트는
      안쪽 공유까지 확인하세요.
  - id: quiz-day-42-memory-review-transfer
    question: 세 언어로 옮길 때 무엇을 보존해야 하나요?
    choices:
      - 복사 범위와 공유 상태 확인라는 동작과 경계 조건
      - 세 언어의 표면 문법을 한 글자도 바꾸지 않는다
      - 타입과 오류 처리를 모두 생략한다
    answerIndex: 0
    explanation:
      세 언어로 옮길 때 보존해야 하는 것은 `복사 범위와 공유 상태 확인`이라는 의미와 입력·출력 계약입니다. C의 메모리 복사와 Rust의 clone 범위로 같은 동작을 구현하고,
      실행 결과 `[1] [1, 2]`로 대조하면 옮김이 맞는지 확인할 수 있습니다.
playgroundSource: "a = [1]

  b = a.copy()

  b.append(2)

  print(a, b)"
---

## 오늘 배울 이유

주소 공유와 독립 복사를 손으로 구분하고 수명에 따라 안전한 방법을 선택합니다. Day 42 "`메모리 모델 종합 회상`"에서는 Python 코드의 실행 결과(`[1] [1, 2]`)를 따라가며, `복사 범위와 공유 상태 확인` 동작이 왜 필요한지 확인합니다. 이 동작이 빠지면 "`얕은 복사를 중첩 리스트의 깊은 복사로 착각함`" 같은 문제가 생깁니다.

## 시작 전에 확인할 것

바로 앞선 [Day 41](/learn/day-41-box)에서 배운 "Rust Box와 힙 소유" 내용을 한 문장으로 말해 보세요. 이번 "`메모리 모델 종합 회상`"에서 새로 달라지는 조건을 찾아보세요. 핵심 표현은 `a.copy()`입니다.

## 머릿속 그림

b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않습니다. Day 42에서는 아래 흐름 순서대로 상태가 바뀌고, 결과를 가르는 부분은 `a.copy()`입니다.

```text
a=[1]  →  b=[1] 별도 객체  →  b=[1,2]
```

위 흐름에서 `a.copy()` 부분이 실행되는 지점을 찾으면 읽는 값과 바뀌는 값이 나뉘어 보입니다. 마지막 출력은 `[1] [1, 2]`입니다.

## 천천히 풀어보기

메모리 학습에서 먼저 물을 것은 '어느 이름이 어떤 데이터를 가리키는가'입니다. `b = a.copy()` 뒤 `b.append(30)`은 바깥 리스트가 달라서 `a`를 바꾸지 않습니다. `b = a`였다면 두 이름이 같은 리스트를 보므로 `a`에서도 추가한 값이 보입니다.

한 줄씩 실행하며 `a → 리스트1`, `b → 리스트2`처럼 종이에 그려 보세요. C 포인터는 유효한 주소를 직접 다뤄야 하고, Rust의 소유권은 유효하지 않은 참조 사용을 막는 규칙을 제공합니다. 같은 '참조'라는 말을 들어도 언어마다 허용되는 동작이 다릅니다.

메모리 학습에서 먼저 물을 것은 어느 이름이 어떤 데이터를 가리키는가입니다. `b = a.copy()` 뒤 `b.append(30)`은 바깥 리스트가 달라서 `a`를 바꾸지 않습니다. `b = a`였다면 두 이름이 같은 리스트를 보므로 `a`에서도 추가한 값이 보입니다.

한 줄씩 실행하며 `a → 리스트1`, `b → 리스트2`처럼 종이에 그려 보세요. C 포인터는 유효한 주소를 직접 다뤄야 하고, Rust의 소유권은 유효하지 않은 참조 사용을 막는 규칙을 제공합니다. 같은 참조라는 말을 들어도 언어마다 허용되는 동작이 다릅니다.

## 문법을 예제로 보기

아래 Python 코드는 Day 42 "`메모리 모델 종합 회상`"의 독립 예제입니다. 전체 4줄 가운데 핵심 부분은 `a.copy()`이며, 실행 결과는 `[1] [1, 2]`입니다.

```python
a = [1]
b = a.copy()
b.append(2)
print(a, b)
```

예상 출력:

```text
[1] [1, 2]
```

하단 Python 실행 영역에 같은 코드가 들어 있습니다. 먼저 실행 결과(`[1] [1, 2]`)를 가리고 예측한 뒤 실행해 비교하세요.

## 핵심 줄 따라 읽기

예제의 핵심 표현은 `a.copy()`입니다. 아래 줄 번호가 붙은 코드에서 이 표현이 있는 줄 번호를 말하고, 그 줄이 읽는 값과 바꾸는 값을 적어 보세요.

```python
 1 | a = [1]
 2 | b = a.copy()
 3 | b.append(2)
 4 | print(a, b)
```

1. a=[1]입니다
2. copy로 b=[1]을 새로 만듭니다
3. b만 [1,2]가 되어 함께 출력합니다

`a.copy()` 부분이 실행될 때 읽는 값과 바뀌는 값을 한 줄씩 적어 보세요. 다음 `결과 예측과 작은 변경`에서는 이 부분이 달라집니다.

## 실행 추적

| 순서 | 상태 또는 동작  |
| ---: | --------------- |
|    1 | a=[1]           |
|    2 | b=[1] 별도 객체 |
|    3 | b=[1,2]         |

위 순서대로 실행한 최종 출력은 `[1] [1, 2]`입니다. `a=[1]` 단계와 마지막 단계를 비교하면 입력과 출력의 관계가 보입니다.

## 다른 예제로 다시 이해하기

`a=[[1]]; b=a.copy(); b[0].append(2)`이면 a까지 `[[1,2]]`가 됩니다. 이번엔 `b.append([3])`을 더하면 a는 그대로 `[[1,2]]`, b만 `[[1,2],[3]]`입니다. 같은 얕은 복사여도 **안쪽 객체 수정**과 **바깥 리스트 추가**는 다른 결과를 냅니다.

## 결과 예측과 작은 변경

원본 실행 결과는 `[1] [1, 2]`입니다. 아래 코드에서 원본과 달라지는 첫 줄을 표시하고, 그 줄의 변경 전후 값을 적어 보세요.

```python
a = [2]
b = a.copy()
b.append(2)
print(a, b)
```

찾은 줄을 적용했을 때의 실행 결과를 먼저 예측해 보세요.

해설: 바뀐 코드의 실행 결과는 아래와 같습니다.

```text
[2] [2, 2]
```

원본은 `[1] [1, 2]`, 수정본은 `[2] [2, 2]`이다. 첫 변경 줄 `a = [1]`에서 `a = [2]`로 시작 값을 바꾸면, 복사본의 추가 뒤에도 원본 첫 값은 바뀐 값 그대로 남는다.

## 자주 틀리는 지점

**확인할 실수: 얕은 복사를 중첩 리스트의 깊은 복사로 착각함.** 정상 코드의 실행 결과는 `[1] [1, 2]`입니다.

- 정상 줄: `b = a.copy()`
- 잘못된 줄: `b = a`

두 줄을 나란히 놓으면 정상 줄 `b = a.copy()`이 `b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않습니다.` 설명과 맞고, 잘못된 줄은 `얕은 복사를 중첩 리스트의 깊은 복사로 착각함` 쪽으로 어긋납니다. 결과가 예상과 다르면 `a=[1]`부터 `[1] [1, 2]`까지 처음 어긋난 곳을 찾습니다.

## 다른 언어로 옮기기

세 언어에서 지켜야 할 핵심 동작은 `복사 범위와 공유 상태 확인`입니다. 목표는 같은 입력에 같은 결과를 내는 것으로, 각 언어의 규칙에 맞게 옮겼는지 실행 결과로 대조하세요.

| 언어   | 옮길 때 확인할 표현과 규칙               |
| ------ | ---------------------------------------- |
| C17    | 포인터 주소 복사와 버퍼 내용 복사는 다름 |
| Python | list.copy는 중첩 원소까지 복제하지 않음  |
| Rust   | clone의 구현이 무엇을 복사하는지 확인    |

C17에서는 `포인터 주소 복사와 버퍼 내용 복사는 다름` 규칙을 적용합니다. "`메모리 모델 종합 회상`" 수업의 실행 결과(`[1] [1, 2]`)를 기준으로 삼고, C는 범위와 널 검사를 자동으로 해 주지 않으므로, 코드에 사용된 접근마다 유효 범위를 직접 확인해야 합니다. Python에서는 `list.copy는 중첩 원소까지 복제하지 않음` 규칙을 따릅니다. 실패나 빈 입력은 예외로 드러내며 조용히 넘기지 마세요. Rust에서는 `clone의 구현이 무엇을 복사하는지 확인` 규칙을 따릅니다. 실행 결과가 다르면 컴파일 오류(타입, 소유권, 빌림)와 실행 때 패닉(인덱스 범위 등) 중 어느 쪽인지 메시지부터 구분하세요.

## 실습 순서

예측(`[1] [1, 2]` 맞히기) → 빈칸(`a.copy()` 채우기) → 변경(`처음 등장하는 숫자를 1에서 2로`) → 오류 수정(`얕은 복사를 중첩 리스트의 깊은 복사로 착각함` 찾기) → 독립 구현(`'메모리 모델 종합 회상' 개념을 보여주는 작은 프로그램` 만들기) 순으로 진행하세요. 각 단계의 답은 본문의 `[1] [1, 2]` 및 `a.copy()` 설명과 대조하세요.

## 스스로 설명하기

- "`메모리 모델 종합 회상`" 수업이 필요한 이유는 `복사 범위와 공유 상태 확인` 동작으로 설명해 보세요.
- 예제에서 `a.copy()` 부분 실행 직전의 상태와 직후의 출력(`[1] [1, 2]`)을 말해 보세요.
- "`얕은 복사를 중첩 리스트의 깊은 복사로 착각함`" 상황에서 어떤 입력과 출력이 어긋나는지 말해 보세요.
- 같은 `복사 범위와 공유 상태 확인` 동작을 나머지 두 언어의 어떤 표현으로 옮길지 말해 보세요.

## 핵심 요약과 복습

b는 새 바깥 리스트를 소유한 참조이므로 append는 a를 바꾸지 않습니다. Day 42 "`메모리 모델 종합 회상`" 예제의 핵심 부분은 `a.copy()`이며, 실행 결과는 `[1] [1, 2]`입니다. "`얕은 복사를 중첩 리스트의 깊은 복사로 착각함`" 여부를 확인하고 Day 42 수업을 완료하세요.
